{
  "schema_version": 1,
  "package": "codex-team-service-bootstrap",
  "version": "0.2.0",
  "assurance": "LOCAL_CORE_ONLY",
  "tests": [
    {"id": "AT-01", "status": "PASS", "claim": "install receipt URN $ref resolves in the bundled schema registry"},
    {"id": "AT-02", "status": "PASS_SIMULATED_CLEAN_PROFILES", "claim": "two clean project roots generate byte-identical contracts"},
    {"id": "AT-03", "status": "PASS", "claim": "generated role manifests contain no provider endpoint or raw thread id"},
    {"id": "AT-04", "status": "PASS_EXECUTED_HARNESS", "claim": "vm module closure executes a normal fixture, rejects fetch and dynamic import before effect, and observes 0 normal attempts / positive fake attempts / 0 provider calls"},
    {"id": "AT-05", "status": "PASS", "claim": "stage above sealed tier and Router tier mismatch fail closed with non-zero exit"},
    {"id": "AT-06", "status": "PASS_WINDOWS_CURRENT_HOST", "claim": "PowerShell child marker and exit code are observed"},
    {"id": "AT-07", "status": "PASS_CURRENT_EXACT_AND_NEGATIVE_MISSING", "claim": "four dependency manifests match exact reviewed versions and missing roots prevent runtime sealing"},
    {"id": "AT-08", "status": "PASS_V0_1_X_TO_V0_2_0_AND_CURRENT_NOOP", "claim": "versioned migration preserves backup and current schema is a hash-preserving no-op"},
    {"id": "AT-09", "status": "PASS_PROFILE_MANIFEST_ROUTER_MISMATCH_REPORTED", "claim": "11 role manifests, 67 route-kind requirements, and 64 unique source-target routes are derived; missing Router kinds are an explicit finding"},
    {"id": "AT-10", "status": "PASS_VALIDATION_ONLY", "claim": "exact simulation envelope can be validated without activation"},
    {"id": "AT-11", "status": "PASS", "claim": "repeated init is byte-idempotent and refuses edited-file overwrite"},
    {"id": "AT-12", "status": "PASS", "claim": "runtime loss returns RUNTIME_RESTORE_REQUIRED without endpoint guessing"},
    {"id": "AT-13", "status": "PASS_WINDOWS_CURRENT_HOST", "claim": "Unicode project paths and LF-only output pass"},
    {"id": "AT-14", "status": "PASS", "claim": "bootstrap commands do not mutate four dependency plugin manifests"},
    {"id": "AT-15", "status": "BLOCKED_REBUILD_FROM_EMPTY_SOURCE_DRILL", "claim": "export installation is supporting evidence but does not replace rebuild-from-empty-source"},
    {"id": "AT-16", "status": "BLOCKED_EXTERNAL_SECOND_PRINCIPAL", "claim": "another Windows principal must prove runtime read denial"},
    {"id": "AT-17", "status": "PASS", "claim": "same doctor inputs yield the same tier hash and project config cannot promote tier"}
  ],
  "claims": {
    "plugin_implemented": true,
    "plugin_installed_current_pc": true,
    "clean_profile_simulation_validated": true,
    "physical_second_pc_validated": false,
    "real_send_authorized": false,
    "host_authenticated": false,
    "service_ready": false
  }
}
