# ARCHITECTURE — 데이터 플랫폼 설계

> 이 앱의 본질은 화면이 아니라 **데이터**다. 입고·폐기·고정지출 입력이 즉시 단가·손익·추이로 전파되고,
> 그 누적이 추후 상세 리포트(월 손익·원가 추이·마진 변화)의 원천이 된다.
> 따라서 *진실의 원천은 서버(Supabase)* 이고, 모든 변경은 추적 가능한 이벤트로 남는다.

## 1. 레이어

```
┌────────────────────────────────────────────────────────────┐
│ apps/mobile (Expo SDK 54 · expo-router · kit 디자인시스템)     │
│  · 입력 환산(구매단위→기준단위) · 표시 · 낙관적 업데이트         │
│  · @sikjae/core 로 단가/손익 "미리보기"                        │
└───────────────┬────────────────────────────────────────────┘
                │ Supabase JS (rpc.*, from())
┌───────────────▼────────────────────────────────────────────┐
│ packages/db (Supabase = 진실의 원천)                          │
│  · 17 엔터티 + 파생/스냅샷 테이블                              │
│  · RLS: 매장(store_id) 단위 격리                              │
│  · E1~E7 전파 RPC = 단일 트랜잭션(원자성)                      │
│  · calc_helpers: 기준단가·로스율·고정지출률·손익 (권위 계산)    │
└──────────────────────────────────────────────────────────────┘
        ▲ 공식 일치(검증)            ▲ 타입 일치(gen types)
┌───────┴──────────────┐   ┌────────┴───────────────────────────┐
│ packages/core (순수)  │   │ packages/types (도메인 계약)         │
│  파생값 공식 + 검산    │   │  enums · entities · derived         │
└──────────────────────┘   └────────────────────────────────────┘
```

**계산이 두 곳에 사는 이유**: 서버 RPC는 *확정값*(영구 저장·정합성)을, `packages/core`는 *미리보기*(입력 즉시 피드백)와 *검산 테스트*를 담당한다. 두 곳의 공식은 반드시 일치해야 하며, 변경 시 동시에 고친다(검산 테스트가 회귀를 잡는다).

## 2. 통합 데이터 흐름 (⑤ 1장)

```
[④ 설정] 카테고리·단위·목표 기본값 ──▶ 전 페이지 참조
[① 식재료] 등록(개당용량·로스율·안전재고) + 재고(개수) + 구매옵션
   ▼ 기준단가(가중평균×로스)         ▼ 안전재고·곧소진
[② 레시피] 재료(1인분) + 추가지출 + 고정지출률 → 순이익·권장가
   ▼ 1인분 소요량(역산)              ▼ 발주 후보
[③ 발주] 후보 → 외부주문 → 발주됨 → 입고확정 → 재고·단가·이력·추이 갱신
[④ 고정지출(월)] 합계÷매출 = 고정지출률 → ② 손익 / 월 손익 리포트
```

## 3. 전파 이벤트 (E1~E7) — 정합성의 핵심

| 이벤트 | RPC | 트리거 | 변경 |
|---|---|---|---|
| E1 | `e1_confirm_inbound` | 입고 확정 | 재고·단가·이력·추이·손익·월재료비·후보 |
| E2 | `e2_discard` | 폐기 | 로스율·단가·손익·재고 |
| E3 | `e3_recipe_saved` | 레시피 저장 | 손익·추이(파랑) |
| E4 | `e4_fixed_cost_saved` | 고정지출 저장 | 전 레시피 손익(회색)·월 손익 리포트 |
| E5 | `e5_stock_adjusted` | 재고 수정·실사 | 재고·뱃지·후보 |
| E6 | `e6_recipe_calc` | 레시피 계산 | 부족분 후보 |
| E7 | `e7_place_order` | 발주 등록 | 발주 레코드·후보상태 (재고·단가 **불변**) |

**불변식**
- 재고·단가·이력은 **E1·E2·E5** 에서만 변한다. 발주 등록(E7)은 기록일 뿐.
- 손익 영향 이벤트(E1~E4)는 `profit_trends` 에 원인 색 점을 남긴다(재료=주황, 레시피=파랑, 고정=회색).
- 단가 추이(`price_trends`)는 입고(E1)마다 점 1개. **그래프는 2차여도 적재는 1차부터.**

## 4. 파생값 계산 맵 (⑤ 2.3 = `packages/core` = `calc_helpers`)

| 파생값 | 공식 | 검산 |
|---|---|---|
| 기준 단가 | 가중평균 ÷ (1−로스율) | 대파 4.71원/g |
| 실측 로스율 | 누적폐기 ÷ 누적구매 | — |
| 잔여 환산량 | 개수 × 개당용량 × (1−로스율) | 2,550g |
| 재고 뱃지 | 개수 ≤ 안전재고 / 곧소진 | — |
| 1인분 소요량 | 입력량 ÷ N | — |
| 재료 원가 | Σ(1인분량 × 기준단가) | — |
| 고정지출률 | 월합계 ÷ 월매출 | 31.3% |
| 순이익 | 판매가−세금−재료−고정−추가 | 4,014원·33.4% |
| 권장 판매가 | (재료+추가)÷(1−1/11−률−목표) | 16,000원 |
| 권장 발주 수량 | Ceil(부족 ÷ 개당용량) | — |

검산 테스트: [`packages/core/tests/verification.test.ts`](./packages/core/tests/verification.test.ts) — 전부 통과 확인.

## 5. 데이터 플랫폼 관점 (리포트 대비)

- **불변 이벤트 로그**: `inventory_events` · `order_records` · `price_trends` · `profit_trends` 는 *추가만* 하는 시계열. 삭제·덮어쓰기 금지 → 추이/감사/리포트의 원천.
- **스냅샷 우선**: 추이는 조회 시 재계산이 아니라 이벤트 시점에 점으로 적재 → 과거값이 그때의 진실로 보존된다.
- **월 집계**: `monthly_pl` 는 E1(재료비 누적)·E4(매출·고정·확정)에서 갱신되는 머티리얼라이즈 집계.
- **확장 여지(2차 스키마 무변경)**: 그래프 3종 · 알림 4종(Edge Function) · OCR(`source='ocr'`) · 구매처 병합 · 단위 시스템.

## 6. MVP 경계

1차: 수기 입력 + 계산 정확성(A·B·C·E[그래프 제외]·F[수동]·G[총액]). 2차: OCR·링크추출·그래프·푸시·시뮬레이션·구매처 병합. 상세는 [docs/source-v3](./docs/source-v3/) 및 백로그 참조.
