# ARCHITECTURE — 현재 데이터·계산·전파 구조

## 2026-09-14 연결 도메인 점검 보완

- 재료 등록·수정·삭제와 메뉴 저장은 `settings_lists`도 무효화한다. 카테고리 이동 직후 관리 화면의 사용 수와 삭제 차단이 이전 캐시를 사용하지 않는다. 공통 범위는 `apps/mobile/src/lib/queryClient.ts`가 소유한다.
- MY의 `unit_price_digits`는 `UnitPriceFormatProvider`와 `useUnitPriceFormat`을 통해 재료·구매·재고·발주·메뉴·매출의 기준단가 표시에 적용한다. 설정 변경은 표시만 바꾸며 저장 수량, 원장 금액, 세금 구성별 minor unit 반올림을 바꾸지 않는다. 통화·국가 계약은 표시 자릿수 설정과 별개다.
- 매출 메뉴 상세는 일/기간 RPC의 순이익을 다시 계산하지 않는다. 과거 명칭과 기간 부자재 총액을 보존한다. 기간 훅은 총액을 1개 기준으로 환산하고 화면이 판매 수량을 한 번 곱한다. 장부 조회 대기·실패 때 현재 메뉴를 과거 실적으로 대신 표시하지 않는다. 메뉴 손익에는 해당 메뉴의 조리 후 폐기만 직접 귀속하고, 공통 식재료 폐기와 추가 지출은 임의 배분하지 않는다. 근거가 없는 고정 목표율 20% 판정은 표시하지 않는다.
- 기간에 판매가가 여러 개면 메뉴 상세의 채널 구성은 확정 수량·비율을 표시한다. 기간 평균 판매가를 채널 수량에 곱한 금액은 실제 채널 매출로 표시하지 않는다. 과거 손익 옆의 현행 목표율은 ‘현재 목표’로 구분한다.
- `20260914000006_channel_net_sales.sql`은 채널별 메뉴/기타 매출 조회에 `net_sales`를 추가한다. 메뉴 판매의 채널별 확정 과세 스냅샷과 기타 매출의 항목별 quote가 권위이며, 국제 스냅샷 이전 기타 매출은 0191의 과거 순매출 계약을 유지한다. 채널 손익은 이 순매출에서 비용을 차감한다. 조회 실패나 순매출 필드 누락은 0으로 계산하지 않는다. 거래 원장·스냅샷의 금액과 날짜는 수정하지 않는다.
- 전체 범위와 실행 결과는 `docs/ai-review/evidence/FULL-LINKED-AUDIT-20260914.md`에서 관리한다. 독립검수·필수 게이트·비차단 기기 증빙은 각각 구분한다.

## 2026-09-13 재료 통합 — 0237·0238 현재 계약

사용자 결정에 따라 별도 부자재 기능을 재료로 통합했다. 적용 범위는 로컬 Expo·AppMap·프로토타입이며 원격 운영 DB는 포함하지 않는다. `20260913000237_unified_materials.sql`은 영업 중·브레이크 중 적용을 거부한다. 기존 부자재와 메뉴 연결 원본을 `material_retirement_archive`에 보관하고 같은 ID로 재료와 연결을 이관한다. 메뉴당 사용량은 `qty × base_servings`로 `recipe_lines.input_qty`에 저장한다. 두 가스비는 현재 메뉴에서 제외하며 월 고정 지출액은 사용자가 나중에 입력한다.

- 표시는 **재료**로 통일한다. 내부 식별자, 사용자 이름·메모와 과거 원장 문구는 보존한다. 코스트킵·상품/제품 및 쇼핑몰 범위 확대는 검토 단계다.
- `20260914000003_restore_material_inventory.sql`부터 이관 재료를 포함한 현재 재료는 모두 재고를 관리한다. 재고 관리 선택 기능은 없으며 신규 OFF 등록은 차단한다. 영업 종료 상태에서 현재 재료의 추적 플래그만 전환하고 입고·초기 재고·과거 판매 차감을 만들지 않는다. 다음 영업일의 판매·취소와 재고 수정은 기존 원장을 사용한다. 과거 동결 스냅샷의 `stock_tracking=false`는 정정·취소 시 없던 재고를 복원하지 않도록 보존한다.
- 판매 비용과 조리 폐기 비용은 별개다. 이관 포장용기는 `cost_scope=sale_only`, 일반 재료는 `production`이다. 영업일 스냅샷의 `waste_material_cost`를 신규 판매행 `unit_waste_cost`에 고정하며 과거 NULL 행은 기존 계약으로 읽는다. 원본 금액을 다시 쓰지 않는다.
- 부자재 writer의 앱 실행 권한과 새 부자재 카테고리·메뉴 extras 쓰기를 차단한다. 구형 읽기는 과거 기록 호환용이며 이전 화면 주소는 재료 관리·검색으로 연결한다.
- `20260913000238_unified_material_reads.sql`의 기간별 재료 내역은 당시 메뉴 스냅샷과 판매 수량으로 계산한다. 재고 미관리 재료도 포함하고 조리 폐기 수량을 판매 재료비에 섞지 않는다.
- 통합 전 수정 기록은 재료별 `ingredient_legacy_material_history`로 읽는다. 원본 ID·시각·전후 값을 유지하며 현재 적용 상태를 추정하지 않는다. 소유자 검증과 20건 커서를 사용한다. 앱의 ‘통합 전 수정 내역’에서 확인한다.

검증 계약: DB 시험 `56`, `65`, `67`~`72`, `79`, `81`, `99_unified_materials`, `99_unified_material_history`. 폐기한 쓰기 계약의 원본 시험은 `packages/db/tests/legacy-materials/`에 보존하고 업그레이드 시험의 0237 이전 판본에서 실행한다. 아래 부자재 설명은 이관 전 기록과 호환 경로의 배경이다.

> 이 문서는 **현재 구현된** 원장, 계산 권위, 전파 이벤트와 검산값의 단일 설명이다.
> 미래 Queue·Edge·Worker 분리는 [서버 확장 아키텍처 기획안](./docs/서버-확장-아키텍처-기획안.md),
> 브랜치·DB 배포·복구는 [운영 기획안](./docs/브랜치-DB-운영-기획안.md)을 따른다.

- 기준일: 2026-08-31
- 기준 migration: `20260831000177_operations_health_signal_separation.sql`
- 현재 물리 구조: Expo 앱 + Supabase Primary Postgres/Auth/Data API/RLS/pg_cron + 운영 상태 조회용
  `ops-health` Edge Function + GitHub Actions 10분 점검
- 미도입: Supabase Queue, 제품 BFF·Webhook용 Edge Function, 외부 Worker, Read Replica, 외부 OLAP

## 1. 책임 레이어

재료 삭제는 `20260914000004_ingredient_delete_guard.sql`의 `ingredient_delete_check`로
삭제되지 않은 연결 메뉴를 먼저 조회한다. 판매 중지(`active=false`) 메뉴도 연결에 포함한다.
상세·관리·목록 편집은 공용 삭제 창에서 연결 메뉴명과 삭제 불가 사유를 표시하며 조회 실패 시 삭제를 허용하지 않는다.
`deactivate_ingredient`는 소유권과 공통 영업 범위 잠금 안에서 연결을 다시 검사한다.
실제 삭제된 메뉴의 과거 연결은 차단 대상에서 제외하고 재료·연결·원장 기록은 보존한다.
회귀 계약은 `99_ingredient_delete_guard.sql`이 소유한다.

카테고리는 현재 항목이 연결되어 있으면 삭제할 수 없다. 재료는 `active=true`, 메뉴는
`deleted_at is null`을 현재 항목으로 본다. `20260914000005_category_delete_guard.sql`은
판매 중지 메뉴도 `settings_lists.used_count`와 삭제 검사에 포함한다. 항목을 다른 카테고리로
옮긴 뒤 빈 카테고리를 삭제하며, 실제 삭제된 메뉴 행은 보존한다. 공개 `delete_category`의
소유권·공통 영업 잠금은 유지한다. 검산은 `99_category_delete_guard.sql`이 소유한다.

메뉴 삭제(0236)는 판매 중지와 별도로 `delete_recipe(store, recipe, expected_revision)`가 처리한다.
매장 소유권과 공통 영업 범위 잠금 안에서 영업 중·브레이크 중 삭제를 거부하며, 영업 전·종료 후에만
현재 판본을 검사해 `recipes.deleted_at/deleted_by/deletion_base_revision`을 남기고 비활성화한다.
같은 작성자·삭제 기준판본의 재시도는 무변경 성공이다. 현재 메뉴·선택 목록에서 제외하되 메뉴 행,
식재료 연결, 과거 매출·재고·손익·영업일 스냅샷은 보존한다. 새 수정 요청으로 삭제 메뉴를 재활성화하거나
덮어쓸 수 없다. 검산은 `92_recipe_delete.sql`, 영업 시작 경합은 `recipe-delete-concurrency.mjs`가 소유한다.

```text
apps/mobile
  5탭 점주 UX · 입력 · 표시 · 화면별 훅 · react-query 캐시 무효화
          │
          │ Supabase Data API / 공개 RPC (JWT)
          ▼
packages/db
  Postgres 스키마 · RLS · 권위 RPC · 원장 · 스냅샷 · 감사 · Cron
          ▲                            ▲
          │ 공식 대조                  │ 생성 타입
packages/core                   packages/db/src/database.types.ts
  순수 미리보기·검산·서식        Supabase 스키마의 앱용 생성 타입

packages/types
  TaxItem·StockBadge 등 DB 생성 타입과 별개인 작은 도메인 계약
```

서버 RPC가 영구 저장되는 확정값을 만든다. `packages/core`는 입력 중 미리보기와 SQL 공식의 검산을
담으며, 앱이 서버 확정값을 다시 계산해 덮어쓰는 용도가 아니다.

## 2. 도메인 흐름

```text
매장 설정·시간대·요일별 영업 규칙
          │
          ├── 영업 시작 ──▶ business_days 스냅샷
          │                    판매가·레시피·재료단가·부자재·세금·고정지출 고정
          │
식재료·구매 옵션 ─▶ 발주 기록(E7) ─▶ 입고 확정(E1) ─▶ 재고·단가·추이
          │                                      │
          └────────────▶ 레시피(1인분) ──────────┤
                                                 ▼
판매 저장(E10) ─▶ 메뉴·채널 수량 ─▶ 재고 소진(E8) ─▶ 일 손익·원장
          │
          ├── 수량 감소/취소 ─▶ E9 보정 원장
          └── 종료된 날 정정 ─▶ amend RPC + business_day_revisions
```

반제품은 1차 범위 밖이다. `recipe_lines_no_sub_recipe` 제약과 `save_recipe`가 등록을 막으므로 현재
판매 소진은 직접 식재료 라인만 사용한다. 읽기 함수에 남은 재귀 코드는 데이터가 없어 실행되지 않으며,
2차 기능의 근거로 사용하지 않는다.

## 3. 원장·현재 상태·감사

| 책임 | 권위 데이터 | 계약 |
|---|---|---|
| 재고 변동 | `inventory_events` | append-only. 취소·정정은 반대 부호 이벤트를 추가 |
| 현재 재고 | `inventory_states.stock_total` | 식재료별 원장 합과 일치, 음수 허용 |
| 발주·입고 | `order_records` | 상태는 변경될 수 있으며 입고·취소 이벤트와 연결 |
| 단가 추이 | `price_trends` | 단가 재계산·호환 전파 경로가 남기는 시점 스냅샷 |
| 레시피 손익 추이 | `profit_trends` | 원인과 금액을 가진 시점 스냅샷 |
| 영업일 기준 | `business_days.snapshot` | 영업 시작 시 고정, 현재 설정 변경으로 과거가 움직이지 않음 |
| 판매 장부 | `daily_sales`·`daily_sales_items` | 메뉴명·판매가·단위 원가는 판매 시점 값 |
| 종료 장부 정정 | `business_day_revisions` | 전후 상세·기준 품질·사용자·시각 기록 |
| 영업 상태 전이 | `business_state_transitions` | 시작·브레이크·재개·수동/자동 종료 기록 |
| 엔터티 변경 | `entity_change_events` | 식재료·레시피 직접 변경과 파생 전파 기록(30일 보존 뒤 정기 청소 대상) |
| 매장 설정 변경 | `store_configuration_changes` | 세금·국가/가격 기준·월별 고정 지출·부자재의 저장 전후, 서버 수정 시각과 적용 기준. 무변경 저장은 제외 |

`price_trends`와 `profit_trends`는 그래프 UI의 존재와 무관하게 1차부터 적재한다. `monthly_pl`과 각종
조회 RPC는 원장·스냅샷에서 파생되는 읽기 모델이지 새로운 업무 권위가 아니다.

세금 수정 내역은 `store_configuration_history`, 고정 지출 통합 수정 내역은 `fixed_cost_change_history` RPC가 매장 소유권을 검사해 최신 순 20건과 커서·전체 건수를 반환한다. 고정 지출 설정·월별 입력은 한 시간순 원장과 분리 탭 없는 목록 화면으로 제공한다. 서버의 `scope`와 영향 월 배열은 설정·월별 상세의 문맥별 최근 내역 조회에만 쓰며, 항목 구성 변경과 대상 완료 월 재입력은 어느 문맥에서도 같은 사건 ID다. 영향 월 전체를 반환하므로 대표 월 하나만으로 과거 월을 누락하지 않는다. `20260912000204_store_configuration_history.sql` 이후 실제 저장부터 기록하며 이전 변경을 추정해 채우지 않는다. 현재/과거 손익 계산이나 영업일 스냅샷의 권위는 바꾸지 않는다. 앱 역할은 이력을 직접 쓰거나 수정·삭제할 수 없고, 공개 조회는 RLS를 적용받는 실행 역할이 담당한다. 내부 기록 함수만 제한된 definer 권한으로 append한다. 계약 시험: `packages/db/tests/63_store_configuration_history.sql`, `94_fixed_cost_item_configuration.sql`, `96_fixed_cost_history_unification.sql`.

변경 출처와 작업 종류는 별개다(`20260914000011_change_history_operations.sql`). `source_type`은 직접 입력과 입고·재료·고정 지출·세금 등의 전파 출처를 유지하고, `operation`은 `create/update/delete/unknown`, `operation_subject_type/id`는 실제 변경 대상을 전달한다. 구매 옵션 삭제는 해당 옵션의 삭제이며 부모 식재료 삭제가 아니다. 메뉴 구성 재료나 고정 지출 하위 항목 제거는 메뉴·월 설정의 수정이다. 새 식재료·메뉴 삭제는 원본 대상을 보존한 사건을 한 번 append하고, 중복 삭제는 추가 사건을 만들지 않는다. 목록·최근 변경·상세는 같은 메타데이터로 직접 사건의 등록/수정/삭제 또는 자동 갱신을 표시한다. `direct_count`는 직접 변경 사건 수이며 한 사건의 입력값과 계산 결과를 두 사건으로 세지 않는다.

과거 이력은 갱신하지 않는다. 단일 `created` 필드의 명시적인 null→값 전환이나 통합 전 동일 부자재 ID의 active true→false처럼 구조화된 근거만 읽기에서 분류한다. 근거 없는 종류는 `unknown`으로 보존하고 직접 사건이나 출처를 모르는 사건은 중립적인 ‘변경’으로 표시한다. 합계의 ‘직접 변경’은 출처가 확인된 사건만 센다. 제목·번역·현재 값으로 과거 작업을 추정하지 않는다. 설정 저장의 `source`는 설정 종류이며 자동 전파 출처가 아니다. 세금 프로필의 이전 값이 없다는 이유만으로 최초 등록으로 표시하지 않는다. 새 내부 메타데이터 helper는 앱 역할의 직접 실행을 허용하지 않는다. 계약 시험: `99_change_history_operations.sql`, `99_linked_migration_postconditions.sql`.

`20260912000207_material_configuration_history.sql`부터 부자재 수정·비활성화도 같은 원장에 `kind/source=material`로 기록한다. `save_material`·`deactivate_material`의 기존 매장 경계와 영업 범위 잠금을 유지하고, 내부 UPDATE 트리거가 이름·카테고리 ID/이름·단가·단위·메모·사용 상태의 전후 값을 append한다. 등록·무변경·과거 내역 백필은 제외한다. 영업 중/브레이크의 단가 변경은 `next_business`(영업 종료 후 적용), 그 외 변경은 `immediate`로 기록하며 실제 금액 전파는 기존 0206 계약이 담당한다. 앱 역할의 직접 원장 쓰기·트리거 실행은 금지한다. 단가/메모·무변경·삭제·페이지네이션·타 매장 ACL 시험: `packages/db/tests/66_material_configuration_history.sql`.

설정 적용 시점은 서버 영업 상태로 결정한다(`20260912000205`·`20260912000206`). 영업 전·종료 후에는 식재료 단가·메뉴 가격/구성·부자재·고정 지출·세금의 새 기준을 현재 메뉴에 즉시 적용한다. 영업 중·브레이크 중에는 편집용 저장값은 최신 상태로 유지하되, 메뉴 목록·읽기 전용 상세·판매가 시뮬레이션·추천 견적의 원가와 손익은 열린 영업일의 시작 스냅샷을 사용한다. 수동·자동 종료가 같은 `close_business_day_row`를 거치면서 새 기준을 즉시 공개하고 차이가 있는 메뉴의 손익 추이를 한 번 기록한다. 실제 입고·취소·폐기·판매의 재고 원장과 판매 중지는 대기하지 않는다.

메뉴 기준 인분·목표 순이익률도 시작 기준에 포함되므로 해당 필드만 바뀌어도 적용 대기 이력을 기록한다(0216). 실제 금액이 같으면 손익 추이는 생성하지 않는다. 세금 프로필의 항목·요율·과세 상태·채널 납부 주체 변경은 영향 메뉴에 `tax` 자동 갱신 사건을 기록하고, 메뉴 과세 예외 수정은 `direct` 사건으로 기록한다(0215). 고정지출은 같은 총액의 항목 교체도 당월 모든 메뉴에 기록하며, 다른 월 설정은 현재 메뉴 이력을 생성하지 않는다(0218). 설정 저장과 영업 상태 전환은 금액뿐 아니라 메뉴 수정 내역·설정 수정 내역·대기 요약 캐시를 함께 무효화한다.

MY의 `save_tax_configuration`은 가격 기준과 세금 프로필을 하나의 영업 범위 잠금·트랜잭션으로 저장한다(0217). 두 프로필의 ID·revision을 모두 검사하고 중간 실패 시 모두 롤백한다. 금액 원장 이후에도 포함/미포함 가격 기준은 변경할 수 있으나 국가·지역·통화·사업 locale 변경 제한은 유지한다. 유효한 메뉴 과세 예외는 새 프로필에 보존하고, 삭제·비활성화된 과세 카테고리를 참조한 예외는 매장 기본값으로 복귀한다. 기존 공개 개별 시장 저장의 제한은 유지하며, 앱은 두 요청 순차 저장을 사용하지 않는다. 원자 저장은 메뉴당 최종 전후 사건 하나를 만들고 중간 시장/세금 계산으로 가짜 손익 사건을 만들지 않는다. 검산: `75_tax_menu_propagation.sql`~`79_linked_cost_combination.sql`, `tax-config-concurrency.mjs`.

세금 변경 비교는 메뉴의 실제 과세 상태에 적용되는 항목만 사용한다(0225~0227). 적용되지 않는 면세 메뉴의 요율·납부주체 변경은 메뉴 사건을 만들지 않는다. 구성과 계산·납부 규칙은 별도 사유로 기록하며, 표시 이름·순서 또는 같은 과세 상태의 상속 전환만 바뀌면 정보성 이력으로 남기고 적용 대기를 만들지 않는다. 적용 범위를 다른 과세 상태로 넓혀도 기존 적용 메뉴의 금액·규칙이 같으면 정보성 이력이며, 새로 적용되는 메뉴만 재무 사건을 만든다. 프로필이 없는 JSON null 구성은 빈 항목으로 처리한다. 다른 월 고정지출 최초 저장도 당월 메뉴 추이를 생성하지 않는다(0226). 과거월 정정은 해당 월 monthly_pl과 설정 수정 내역에 기록하고, 현재 메뉴·식재료 가격으로 과거 날짜의 메뉴 손익 점을 합성하지 않는다. 종료 영업일 스냅샷은 보존한다(`78_fixed_composition_matrix.sql`).

고정 지출 적용 기준은 매장 설정의 최근 1·2·3개월 중 하나이며 기본값은 3개월이다(`20260914000015_fixed_cost_basis.sql`). 계산 대상 월은 포함하지 않고 직전 완료 월부터 역순으로 정확히 N개월을 사용한다. 필요한 모든 월에 고정 지출 구성과 0보다 큰 계산용 매출이 있어야 적용하며, 한 달이라도 없으면 누락 월을 0으로 평균내지 않고 적용률을 `null`로 반환한다. 다음 달에는 기준 구간도 한 달 이동하므로 새로 포함된 완료 월을 입력하기 전까지 다시 미산출이다. 이 `null`은 메뉴 목록·상세·판매가 미리보기와 매출 작성·확정 손익까지 유지하며, 고정 지출 0원으로 바꾸어 순이익을 계산하지 않는다(`20260919000125_fixed_cost_uncomputed_propagation.sql`). 메뉴 변경 사실은 `entity_change_events`에 남기되, 미산출 상태에서는 `profit_trends`에 과거 월 비율이나 0% 손익을 만들지 않는다. 필요한 월을 입력해 계산 가능해진 시점에 실제 비율로 첫 손익 기준선을 기록한다(`20260920000136_profit_trend_fixed_cost_uncomputed.sql`). 적용률은 `Σ고정 지출 ÷ Σ계산용 매출`의 가중 비율이고, 월평균 금액은 `Σ고정 지출 ÷ N`이다. 기준 개월 수와 월별 항목은 로그인한 사용자의 소유 매장 서버 설정에 저장한다.

고정 지출 항목 구성을 이미 사용한 뒤 변경하면 선택한 최근 1·2·3개월을 새 구성으로 다시 입력하는 초안을 만든다(`20260915000103_fixed_cost_reentry_workflow.sql`). 대상 월을 모두 입력하기 전에는 기존 구성·적용률·메뉴 손익을 유지하고, 마지막 월 저장 때 구성·기준·대상 월을 한 트랜잭션으로 전환한다. 진행 중 초안의 `변경 중단`은 확정 원장을 바꾸지 않는다. 설정 화면과 월별 상세의 최근 변경은 같은 통합 수정 내역으로 이동한다. 목록은 복구 버튼을 두지 않고 상세에서 서버가 반환한 `reversible`·차단 사유·조회 당시 최신 사건 ID를 사용한다. `revert_fixed_cost_change`는 잠금 뒤 최신 사건을 다시 비교해 월별 입력, 계산 기간, 항목 구성과 대상 월을 각 사건의 실제 범위로 되돌리고 새 복구 사건을 append한다. 더 최근 변경, 진행 중 초안, 현재 값 불일치가 있으면 복구를 거부한다. 구형 `save_fixed_costs`와 `revert_fixed_cost_reentry`의 앱 직접 실행 권한은 닫는다(`20260915000105_fixed_cost_history_unification.sql`).

메뉴 세금 상세는 현재 견적의 시장·구성 항목·해석된 과세 상태를 함께 표시하며 예약 MY 설정을 섞지 않는다(0228 `quote_context.treatment`). 활성일 이전에는 기존 서버 상세의 세액·적용 요율만 표시하고 저장되지 않은 법정 세율·납부 주체·과세 분류를 추정하지 않는다. 프로필 변경 없이 메뉴 과세 예외만 수정한 경우에도 종료 후 적용되도록, 종료 상태의 현재 세금 기준일은 해당 매장·선택 프로필의 메뉴 예외 적용일을 포함한다(0229). 열린 영업일 기준과 국제 세금 최초 활성일 보호는 그대로 유지한다.

메뉴 과세 설정의 `inherit_default`는 새로 저장한 ‘매장 기본값’ 선택만 표시한다(0222). 명시 과세/면세와 구별해 프로필 교체 때 새 기본값을 따라가며, 기존 행의 선택 의도를 추정하지 않는다. 미래 예약을 앞당길 때도 기존 프로필 시작일에 실제 저장된 예외·상속·판본을 승계한다(0224). 삭제된 분류는 복원하지 않는다. 세금 대기 요약은 영향 메뉴의 실제 변경 사건에 결속하며, 무관한 메뉴에 매장 전체 세금 변경을 대입하지 않는다(0223).

식재료·메뉴·입고·취소의 변경 이력은 `pending_recipe_tax_quote_for_price`로 동일한 다음 적용 세금 기준을 사용한다(0221). 세금 먼저 수정 → 다른 원가 수정과 반대 순서 모두 같은 최종 손익을 만든다. 이력 계산 변경은 과거 판매/재고 원장 날짜를 바꾸지 않는다. 마감 비교에는 세금뿐 아니라 순매출도 포함하므로, 세액이 같고 가격 기준만 달라진 경우에도 종료 후 순이익 추이를 기록한다(0220). 서버 상태 쿼리 자체만 갱신 대상에서 제외하며 `day_menu_basis` 자식 쿼리는 갱신한다. 추가 검산은 `80_close_net_only_change.sql`~`83_future_tax_override_carry.sql`과 실제 QueryClient 관측 시험이다.

`recipe_detail`은 편집용 최상위 값과 읽기용 `effective`를 구분한다. 앱은 readOnly 조회만 `effective`를 선택하고 편집은 최상위 값과 최신 revision을 사용한다. 내부 `active_menu_business_basis`·`with_effective_menu_detail`은 앱 직접 실행 권한이 없다. 저장과 종료는 매장 영업 범위 잠금으로 직렬화한다. 수동·자동 공통 종료 함수도 행 잠금 전에 같은 범위 잠금을 잡는다. 종료 시 손익 추이는 새 기준이 공개된 실제 서버 시각의 메뉴 원가 이력이며 당일 판매 손익 원장이 아니다. 따라서 다음 미개장 날짜의 세금 프로필을 현재 메뉴에 공개한 경우 추이는 새 세금을 기록하되 과거 판매/정정은 원래 날짜의 프로필을 계속 사용한다. 탭 공통 영업 상태 관찰자는 서버의 종료·날짜 변경을 감지해 메뉴와 세금 쿼리를 무효화한다. 수동 종료 응답 시 즉시 갱신하며 자동 종료는 전경 60초 폴링/복귀 시 재조회로 반영한다. DB의 적용은 종료 트랜잭션에서 즉시 완료된다.

기간별 세금 프로필과 판매 원장은 실제 영업일 기준을 유지하고, 현재 조회만 `current_tax_settings_date`로 선택한다. 날짜가 넘어가도 열린 영업일이 있으면 그 영업일의 기준을 유지하고 종료 후 새 설정을 사용한다. 마감 스냅샷·과거 세금 계산·수동 출시 활성 경계를 소급 변경하지 않는다. 저장 이력의 `application_mode=next_business`는 호환 식별자이며 화면에는 `영업 종료 후 적용`으로 표시한다. 이전 스냅샷을 채우기 위해 원장을 재작성하지 않는다. 구형 스냅샷에 월 매출/합계 메타데이터가 없으면 null로 보존하고, 부자재 단가 맵이 없으면 명시적 초안의 해당 단가를 미확인으로 반환한다. 저장 메뉴의 부자재 합계는 당시 스냅샷 값을 유지한다. 개장 시 중지됐거나 개장 후 새로 만든 메뉴처럼 시작 스냅샷에 없는 메뉴는 기존 최초 판매 기준 생성 계약을 유지한다. 계약 시험: `64_tax_application_business_state.sql`, `65_effective_menu_business_basis.sql`, `settings-application-concurrency.mjs`.


## 4. 전파 이벤트 E1~E12

E6은 제거된 번호이며 뒤 번호를 당기지 않는다.

| 이벤트 | 함수 | 뜻과 핵심 효과 |
|---|---|---|
| E1 | `e1_confirm_inbound` | 입고 확정. 멱등키, 실제 입고량, 재고+, 양 가중 기준단가·추이 갱신 |
| E2 | `e2_discard` | 식재료 폐기. 실제 폐기량을 재고와 원장에 기록 |
| E3 | `e3_recipe_saved` | 레시피 저장 뒤 권위 손익·추이 갱신 |
| E4 | `e4_fixed_cost_saved` | 월 고정지출률과 관련 레시피·월 손익 갱신 |
| E5 | `e5_stock_adjusted` | 실사·재고 수정. 목표 재고와 원장 차이를 보정 이벤트로 기록 |
| E6 | — | 없음. 1차 범위 밖 계산기 제거 이력 때문에 결번 유지 |
| E7 | `place_orders` → `e7_place_order` | 요청 키로 1~100개를 원자 저장하고 발주 기록·후보 상태만 변경. 재고·단가 불변 |
| E8 | `e8_sales_consumed` | 판매 증가분의 필요 재료 전량을 원장에 차감. 멱등, 음수 허용 |
| E9 | `e9_sales_reverted` | 판매 감소·취소분을 반대 부호 원장으로 복구 |
| E10 | `e10_sale_recorded` | 메뉴·채널·조리 폐기 판매행 저장과 E8/E9 조정 |
| E11 | `e11_inbound_reverted` | 입고 취소. 현재 입고 회차의 미반전 원본만 전량 반전, 재고·기준단가·추이 재계산 |
| E12 | `e12_order_canceled` | 미입고 발주 취소. 후보 상태 복구, 재고·단가 불변 |

공개 화면 저장 함수(`save_sale`, `amend_ended_business_day`, 각 도메인 save RPC)는 이 몸통들을
판본 검사·매장 스코프·행 잠금과 함께 호출한다. 앱 롤이 내부 몸통을 직접 실행하는 문은 닫혀 있다.

입고 취소는 발주별 현재 회차의 원본마다 반대 부호 이벤트와 `reverses_event_id`를 기록한다(20260914000009). 같은 발주의 입고→취소→재입고→취소에서 이미 반전된 회차를 다시 차감하지 않는다. 이전 연결 없는 보정은 회차 경계로 읽으며 과거 원장을 덮어쓰지 않는다. 재고 내역에서 단일 입고를 취소할 때는 현재 회차의 취소 가능 원본 수를 확인하여 여러 부분 입고를 묵시적으로 함께 취소하지 않는다. 발주 ID만 받는 기존 취소 API는 이전 취소의 지연 재시도와 새 회차 취소 의도를 구분하지 못한다. 이 요청 식별 경계와 과거 잘못된 잔액 보정은 별도 미해결 항목이다.

발주 입고의 응답 유실은 `resolve_order_inbound`로 처리한다(20260914000010). 입고 쓰기와 결과 확인은 매장 영업 범위→발주→요청 키 순으로 잠근다. 기록이 있으면 `recorded`, 없으면 같은 키를 종료하고 `not_recorded`를 반환하며 종료된 키의 늦은 입고는 45010으로 거절한다. 앱은 사용자·매장·발주별 키만 보관하고 재진입 시 이전 결과와 최신 발주 잔량을 확인한 뒤 새 작업을 받는다. 기존 수량을 자동 재전송하지 않는다. `recorded`는 그 요청이 기록됐다는 뜻이며 이후 취소 여부와 현재 재고를 대신하지 않는다. 잔여 초과 입력은 화면에서 막고 완료 안내는 서버가 실제 처리한 수량을 사용한다. 계약 시험: `99_inbound_reversal_cycles.sql`, `99_order_inbound_resolution.sql` 및 두 입고 경합 스크립트.

발주 등록은 `place_orders`가 전체 항목을 먼저 검증한 뒤 같은 트랜잭션에서 E7을 호출한다
(20260920000140). 매장·요청 키마다 payload와 결과 ID 배열을 영수증으로 보존하므로 같은 키·같은
payload의 동시/지연 재호출은 기존 ID만 반환하고, 같은 키의 다른 payload는 거절한다. 앱은
사용자·매장별 요청 키만 보관하고 응답이 불명확하면 `resolve_order_placement`로 `recorded` 또는
`not_recorded`를 확인하기 전 새 요청을 보내지 않는다. 중간 항목 실패는 첫 항목을 포함해 전부 롤백하며
발주 전후 재고·단가·재고 원장은 불변이다. 계약 시험은 `99_order_placement_idempotency.sql`,
`order-placement-concurrency.mjs`, `orderPlacementOperation.test.ts`가 소유한다.

식재료 쓰기 무결성(0195): 입고는 값 조합이 아닌 **작업별 요청 키**로 재시도만 중복 제거한다.
현재 빠른 입고 쓰기는 서버 `clock_timestamp()`를 봉인하는 `record_current_quick_inbound`를 거쳐
E7+E1을 한 트랜잭션으로 처리한다. 응답 미확인 입고는 `resolve_quick_inbound`(20260914000007)로 결과를 먼저 확인한다.
입고와 동일한 매장·요청 키 잠금 아래 원장이 있으면 `recorded`, 없으면 요청 키를 종료하고
`not_recorded`를 반환한다. 종료된 키의 늦은 입고 호출은 거절하며 확인 자체는 재고·입고 원장을
추가하지 않는다. 앱은 확인한 요청과 정확히 일치하는 임시 정보만 지우고 현재 입력을 새 키로
저장한다. 같은 수량·금액이라도 별도 입고는 허용하며 이전 입력을 자동 재전송하지 않는다.

재료 일괄 입고는 `quick_inbound_batch_preview`와 `record_current_quick_inbound_batch`
(`20260921000144_bulk_quick_inbound.sql`)를 사용한다. 1~20개 카드를 서버 배열 순서대로 미리보기하며,
같은 재료가 여러 카드에 있으면 앞 카드의 수량·금액을 다음 카드의 재고·기준단가 계산에 누적한다.
확정은 카드마다 기존 `quick_inbound`의 E7+E1을 호출하되 배치 전체를 한 트랜잭션으로 묶는다. 한 카드의
검증·실사 잠금·요청 키 충돌이 실패하면 앞 카드의 발주·재고·단가 추이도 모두 롤백한다. 배치 영수증과
닫힌 요청 키는 앱 역할에 직접 공개하지 않으며, 응답 미확인은 `resolve_quick_inbound_batch`로 기록 여부를
확인한다. `not_recorded`로 확인한 키는 닫아 늦게 도착한 쓰기를 거부한다. 현재 메뉴 계산은 성공한 E1의
최신 기준단가를 다시 읽고, 이미 봉인된 영업일·과거 확정 손익 스냅샷은 다시 쓰지 않는다.
명시적 차감·폐기는 `change_stock_quantity`에 처리 수량·확인 재고·사유·요청 키를 보낸다.
서버가 재고 행을 잠근 뒤 확인 재고가 다르면 `40001`로 거절하며, 같으면 E5 또는 사유 포함 E2
내부 함수로 기록한다. `stock_quantity_receipts`는 같은 요청의 응답 유실 재시도를 보호한다.
차감·폐기 응답이 미확인되면 앱은 사용자·매장·재료별 요청 키와 종류만 영구 보관한다.
화면 재진입 뒤에도 `resolve_stock_quantity`(20260914000008)로 결과만 확인하며 기존 수량을
재전송하지 않는다. 기록된 요청은 `recorded`, 미반영 요청은 같은 요청 잠금 아래 키를 종료하고
`not_recorded`를 반환한다. 종료 키로 늦게 도착한 쓰기는 45010으로 거절한다. 확인 후 최신
재고를 읽고 새로운 사용자의 확인을 받아 새 작업 키로 기록한다. 결과 확인 자체는 원장을 바꾸지 않는다.
식재료의 기준 차원(g/ml/ea)은 등록 후 변경할 수 없다. 같은 차원의 kg↔g·L↔ml는 입력 표기만
환산한다. 2026-09-12 사용자 결정: `ingredients.purchase_price`·`per_volume`을 직접 수정하면
구매 가격÷개당 용량을 현재 메뉴 계산용 단가로 적용한다(0214). 다음 성공한 입고 또는
입고 취소는 실제 입고 평균으로 자동 전환한다. `base_unit_price`의 실제 입고 평균과 재고 평가
공식은 유지하고, `current_ingredient_unit_price`가 현재 메뉴용 직접 수정값을 우선 읽는다.
단순 재저장·중복 입고·중복 취소는 단가 출처를 바꾸지 않는다. 현재 메뉴 원가·손익·수정 내역과
새 영업 기준에 전파하며, 영업 중/브레이크의 시작 스냅샷·과거 입고 금액·재고 원장은 변경하지 않는다.
식재료 추가·수정의 현재 정보 폼은 `contract_version: 3`이며 용량·구매 가격을 보내지 않는다(0231).
신규 등록은 내부 기준 용량 1과 가격 null로 시작하고 재고 원장을 생성하지 않는다. 이어지는
`ingredients/add-stock/[id]?initial=1` 재고 입력 페이지에서 기존 입고 RPC로 수량·금액을 확정한다.
나중에 입력하면 식재료 정보만 남는다. 정보 수정은 행 잠금·충돌 검사 후 서버의 기존 용량·가격을
유지하므로 입고 평균과 메뉴 원가를 바꾸지 않는다. v3에 숨긴 가격·용량을 보내면 거부한다.
이전 v2의 가격 필수 검사(0230), 구형 호출과 메모 전용 경로는 호환성을 유지한다.
구매 링크의 `volume`은 식재료 기준단위로 환산된 값이다. 선택적인 `base_unit` 신고가 있으면 DB가
식재료 차원과 대조해 불일치를 거부하며, 신고 생략은 기존 최소단위 RPC 호출과의 호환성을 유지한다.
생성 성공 뒤 응답이 유실된 동일 내용 재시도는 재료·구매 옵션·구매처에서 기존 ID를 반환한다
(`20260914000013_creation_retry_idempotency.sql`, `20260914000014_vendor_creation_retry.sql`). 재료는
기존 매장 영업 범위 잠금과 전체 생성 필드 일치를 사용하고, 구매 옵션·구매처는 빈 목록의 동시 생성도
직렬화하는 매장별 자문 잠금을 사용한다. 동일 재시도는 행과 수정 이력을 늘리지 않는다. 같은 이름이지만
재료 필드가 다르면 기존 중복 이름 오류를 유지하고, 구매 옵션의 금액 등 내용이 다르면 별도 옵션으로
등록한다. `save_vendor` 수정은 요청 매장의 대상 행이 없으면 ID를 성공처럼 반환하지 않고 `P0002`로
거절한다(`20260914000012_vendor_write_target_guard.sql`).

식재료 편집 충돌 방지(0196): `save_ingredient`의 전체 폼은 최초 조회값 `expected`를 보내며,
서버가 식재료 행을 잠근 뒤 현재 값과 다르면 `40001`로 거절한다. 메모는
`patch: "memo"`, `memo`, `expected_memo`만 전달하는 별도 경로로 저장하여 다른 필드를
덮어쓰지 않는다. 동일한 메모 결과의 재시도는 무변경 성공으로 처리한다. 메모 변경은 기존
정책대로 식재료 수정내역·재고 원장에 추가하지 않는다. 기존 공개 RPC 서명은 유지한다.
0197은 잠금 뒤 비활성 식재료의 전체 폼·메모 저장을 모두 거절하여 삭제 후 편집 성공을 막는다.
앱은 편집 `40001`의 코드를 보존하고 최신 상세를 재조회한다. 초안과 최초 기준은 자동으로
덮어쓰지 않으며, 사용자가 최신 내용을 확인한 뒤에만 새 CAS 기준을 채택한다. 전체 폼은
미수정 항목을 최신값으로 반영하고 수정 항목은 초안을 유지한다. 숨겨진 메모·구매처는 확인한
서버값을 보존한다. 확인은 저장이 아니며 사용자가 다시 저장해야 한다. 조회 실패·대상 없음은
초안을 유지한 채 저장을 차단하며, 새 대상 진입 시 이전 편집·비동기 응답을 분리한다.

## 5. 계산 불변식과 고정 검산값

| 항목 | 현재 공식·계약 | 고정 검산 |
|---|---|---|
| 입고 기준단가 | 실제 지출 합 ÷ 실제 입고 최소단위 합 | 대파 4,000원/1,000g = **4.00원/g** |
| 현재 메뉴 기준단가 | 구매 가격·용량 직접 변경 직후는 구매 가격÷용량, 다음 성공한 입고/취소 후는 실제 입고 평균 | 직접10원/g → 다음 입고 평균8원/g 자동 갱신 |
| 수량 가중 | 팩 수가 아니라 `팩 용량 × 입고 수량`이 분모 | `단가 × 총입고량 = 지출` |
| 로스율 | 실제 폐기 ÷ 실제 구매, 표시 전용 | 기준단가·손익에 곱하지 않음 |
| 재료 원가 | Σ(1인분 필요량 × 그 기준의 단가) | 제육볶음 **2,806.40원** |
| 세금 | 활성 시장·세금 프로필의 법정 표면 세율과 가격 기준으로 구성 항목별 계산 후 통화 minor unit 반올림 | KR 포함가 12,000원·기본세 10% → 세금 **1,091원**, 세전 순매출 **10,909원** |
| 고정지출률·구성 | 이번 달을 제외한 직전 N개 완료 월의 고정 지출 합 ÷ 같은 월 매출 합. 항목별 금액도 같은 완료 월 묶음의 키별 합 ÷ N인 월평균으로 반환·동결한다. 필요한 월이 모두 입력되지 않으면 비율·평균·항목을 미적용 | 기준 표본 **31.3%**, 12,000원 메뉴 **3,756원** |
| 순이익 | 세전 순매출 − 재료 − 부자재/추가 − 고정지출 | 기준 KR 포함가 메뉴 **4,046.60원 · 33.72%** |
| 권장 판매가 | 기존 단순 비율 미리보기는 (재료+추가) ÷ (1−세금률−고정지출률−목표율). 국제 프로필은 구성별 반올림을 포함한 서버 목표 충족 최소 양수 통화단위 가격(0205) | 기존 기준 시나리오 천원 단위 **16,000원**; 국제 가격을 천원 단위로 재반올림하지 않음 |
| 발주 부족량·수량 | 후보는 `max(0, 최소재고 − 현재 재고)`를 g/ml/개로 표시. 구매 옵션 선택 후 `ceil(부족량 ÷ 옵션 용량)`을 입력 기본값으로 사용 | 최소 발주 보정 없음. 발주 기록 전후 재고·단가는 불변이며 입고 시 반영 |

세금 포함가는 법정 세율로 역산해 매출 손익에서 세액을 차감한다. 세금 별도(`tax_exclusive`) 판매가는
세전 순매출을 그대로 유지하고 계산된 세금을 고객 결제액에 더한다. 비과세는 가격 기준이 아니라
과세 상태의 `zero_rated` 또는 `exempt`로 표현한다. 과거 과세 상태·납부 주체·추가세 구성은 해당 판매
시점 프로필 판본에 그대로 보존한다.
매출 초안의 메뉴 세금도 메뉴 1개의 반올림 세액에 수량을 곱하지 않는다. 날짜별 basis manifest에
봉인된 구성 항목의 반올림 전 금액을 채널 판매 총액에 적용한 뒤 통화 minor unit으로 반올림하며,
확정 원장의 채널별 세금 snapshot과 같은 합계를 만든다(`20260920000137`).
같은 목표 수량 재저장은 세금 이벤트를 추가하지 않으며 감소·취소는 당시 확정액의 차이만 반대 부호로
쌓는다. 플랫폼·배달 수수료는 세금이 아니라 고정 지출에서만 관리한다. 상세 계약과 5개국 기본값은
[국가·통화·세금 국제 출시 기획안](./docs/국가-통화-세금-국제출시-기획안.md)이 단일 출처다.

0205의 `recipe_draft_preview`와 `recipe_price_recommendation`은 저장 없는 현재 날짜 견적이다.
전자는 전체 초안, 후자는 서버에 저장된 레시피를 읽고 같은 서버 단가·고정지출·과세 계약으로 계산한다.
`batch`는 1인분 비교값×기준 인분이며 실제 판매 수량의 세액 확정이 아니다. 원가·고정지출 기준이
없으면 이익/추천가를 null로 유지한다. 국제 추천가는 구성별 반올림 세금 T(P)의 비감소성을 이용해
목표 미달 P보다 큰 후보 중 `(원가+T(P))/(1−고정률−목표율)` 미만을 제외한다. 세금 별도 판매가의
세금은 고객 결제액에 더해지므로 판매가 기준 순매출에서 차감하지 않는다.
통화 최소단위로 올림한 후보를 실제 세금 함수로 다시 검산한다. 2048회 상한은 `search_limit`(미확정),
허용 가격 상한 초과는 `range_exhausted`로 분리하며 어느 쪽도 임의 추천가로 대체하지 않는다.
0205/DB62는 통합 검증 중이며 이 설명은 운영 적용 완료를 뜻하지 않는다.

## 6. 재고와 부족 판정

- `stockStateOf`의 상태는 **여유(ok) / 소진 임박(low) / 소진(out)** 세 단계다.
- `stock_total <= 0`이면 소진, 그보다 크면서 최소재고 이하이거나 `soon_out`이면 소진 임박이다.
- 음수 재고를 0으로 표시하거나 원장 이벤트를 가용 재고만큼 자르지 않는다.
- 판매 부족 경고는 **판매 증가분** 필요량과 현재 재고를 비교한다. 10개→7개 수정에는 경고가 없어야 한다.
- 영업 시작 부족 목록은 메뉴 1개 필요량과 현재 재고를 비교한다. 최소재고 미달 자체는 시작 부족이 아니다.
- 부족해도 판매 버튼은 활성이다. 막는 것은 레시피의 명시적 판매 중지뿐이다.

## 7. 매출 작성일·시간대·과거 수정

`20260916000107`부터 매출 입력의 제품 수명주기는 수동 영업 시작·브레이크·종료가 아니라
**미작성 → 작성 중 → 작성 완료**다. `business_days`의 구형 상태와 전이 함수는 이관·과거 원장
호환에만 사용하며, 새 앱은 상태 버튼이나 기기 시각으로 작성일을 만들지 않는다.

1. `store_time_settings`와 `operating_rules`를 읽는 서버 시계가 매장 달력 날짜와 추천
   `sales_date`를 정한다. 앱은 이 값을 60초마다, 그리고 foreground 복귀 때 다시 읽는다.
2. `sales_calendar_days`가 기대 영업일과 휴무일을 보존한다. 판매 기록이 없는 영업일은 `미작성`,
   활성 초안이 있으면 `작성 중`, 현재 확정 판본이 있으면 `작성 완료`다. 수동 휴무·영업일 확정은
   `sales_calendar_day_revisions`에 사유·실행자·시각과 전후 판본을 append-only로 남긴다.
3. `sales_day_drafts`는 최초 작성과 완료 판본 수정의 임시저장이다. 임시저장은 재고·매출·손익 원장을
   바꾸지 않으며 revision CAS와 payload hash로 동시 편집을 막는다.
4. `finalize_sales_draft` 한 문이 `sales_day_versions`의 불변 판본, `sales_day_heads`의 현재 포인터,
   판매 E8/E9, 일 매출과 손익을 함께 확정한다. 같은 request key와 같은 payload 재시도는 기존
   영수증을 반환하고, 다른 payload는 거부한다.
5. 현재 달과 직전 달의 완료일만 수정 초안을 열 수 있다. 완료 판본을 다시 열어 덮어쓰지 않고 새
   판본을 확정하며 전후 revision과 원장을 보존한다.
6. 당시 계산 기준은 `sales_basis_versions`의 날짜별 manifest와 hash로 고정한다. 이후 현재 메뉴·세금·
   고정 지출 설정이 바뀌어도 과거 판본을 stale로 만들지 않는다. 당시 기준이 없던 이관 기록은
   `legacy_unrecorded`로 표시해 현재 기준을 과거 사실처럼 꾸미지 않는다.
7. 과거 수정이 이미 완료한 전체 재고 실사의 컷오프와 겹칠 때 판매 감소분은 기존 소비 component를
   최신순으로 상쇄한다. 판매 증가분은 현재 앱이 실제 발생 시각을 신뢰할 수 있게 입력받지 않으므로
   즉시 재차감하지 않고 모두 `pending_inventory_resolution`으로 둔다. 다음 전체 실사가 같은 목표를
   흡수한 뒤에만 완료한다. 이는 데이터 안전을 위한 현재 기능 제한이며 exact 시각 입력 경로는 제공하지 않는다.

### 7.1 판매 채널 수명주기

`20260919000128`~`20260919000131`부터 판매 채널의 권위 키는 `sales_channels.id`다. 신규 매장은
`매장(hall)`·`배달(delivery)`·`포장(takeout)` 세 채널로 시작하고 활성 채널을 최대 5개까지 유지한다.
채널 이름은 생성 뒤 수정하지 않으며 추가·삭제·복구만 허용한다. 사용 이력이 없는 채널만 물리 삭제하고,
초안·확정 판매·기타 매출·세금·판본에 한 번이라도 참조된 채널은 사용 중지해 UUID와 이름 snapshot을 보존한다.
삭제한 같은 이름을 다시 추가하면 과거 채널을 복구하며 활성 채널은 항상 1개 이상이어야 한다.

채널 변경과 `open_sales_draft`는 같은 매장 쓰기 잠금을 사용한다. `editing` 또는
`pending_inventory_resolution` 초안이 하나라도 있으면 채널 추가·삭제·복구를 서버가 거절한다. 최초
초안은 여는 시점의 활성 채널 manifest를 봉인하고, 완료 판본 수정 초안은 대상 판본의 당시 채널 manifest를
복제한다. 저장·완료는 현재 활성 여부가 아니라 초안 manifest 소속 UUID를 검증한다. 메뉴 판매 수량은
채널별 행의 합으로 재고 E8/E9와 매출을 확정하며 조리 후 폐기는 채널에 귀속하지 않는다. 기타 매출은
신규 입력부터 반드시 한 채널 UUID에 귀속한다.

일별·기간·메뉴·채널 손익은 같은 확정 채널 행과 `channel_name_snapshot`을 읽는다. 0128 이전 판매는
채널명 변경 이력이 없으므로 이관 당시 현재 이름을 `upgrade_current_name`으로 표시하고, 이후 판매에서
직접 봉인한 이름만 `sale_snapshot`으로 표시한다. 화면은 전자를 판매 당시 이름으로 단정하지 않는다.
메뉴 재료비는 채널
수량으로 직접 계산하고 고정 지출만 당일 채널 귀속 매출 비중으로 배분한다. 채널 원장이 없는 폐기 손실과
추가 지출은 채널 손익에서 제외해 `unallocated_waste_loss`·`unallocated_daily_extra`로 반환한다. 화면은
이를 `채널 미지정 비용`으로 분리하며 채널별 값을 순이익으로 오해하지 않도록 `채널 손익`이라 표시한다.
채널 귀속 매출이 없으면 고정 지출도 임의 채널에 넣지 않고 미배분으로 반환한다.
전환 전 UUID 채널 행이 없는 원장은 기본 세 채널 수량 컬럼과 `채널 미지정` 기타 매출로만 호환 표시하며
현재 채널을 추정해 과거 원장을 다시 쓰지 않는다.

MY-09 영업시간 2A는 영업 요일과 모든 영업일에 공통인 시작·종료 시각만 편집한다. 종료일은
`weekly_hours.<dow>.close_day_offset`의 `0`(당일) 또는 `1`(익일)로 시각과 분리해 저장한다. 필드가 없는
과거 규칙만 `close < open` 비교로 읽는다. 2A 저장은 숨은 요일별 시각과 브레이크를 남기지 않고 공통
시각으로 정리하며, DB는 다음 영업일과의 겹침 및 당일 종료의 역전 시각을 최종 검증한다.

앱은 매장 날짜를 기기 시계나 구형 영업 상태로 보충하지 않는다. 서버 시계 응답이 없으면 날짜 화면을
오류로 멈추고 다시 시도하게 한다. 구형 쓰기와 자동 마감 cron은 매장별 draining·freeze 영수증,
원장 checksum, 필요 시 전체 재고 실사를 통과한 뒤 차단한다.

## 8. 매장 격리와 현재 운영 경계

- 모든 업무 데이터는 `store_id`를 가진다.
- RLS와 SECURITY DEFINER 함수 내부 `assert_my_store`/명시적 매장 검사를 함께 사용한다.
- 현재 1차 계약은 `stores.owner_id UNIQUE`, 즉 계정 하나당 매장 하나다.
- 인증 계정과 영업 원장의 수명주기는 분리한다. 계정 탈퇴·매장 폐점은 `stores.owner_id = null`과
  `archived_at`으로 접근을 끊고, 매장·판매·입고·재고 행은 보존한다.
- `store_lifecycle_events`는 계정 삭제·폐점·삭제 예약·물리 삭제를 append-only로 기록한다.
  물리 삭제는 service role 전용 예약과 보존 종료·승인·백업 근거가 모두 있어야 한다.
- settings와 영업 규칙은 앱 직접 DML을 닫고 판본 있는 RPC만 허용한다.
- 원장·규칙·감사 테이블의 UPDATE/DELETE/TRUNCATE/TRIGGER/REFERENCES 권한을 제한한다.
- service role과 DB 비밀번호는 모바일 번들에 넣지 않는다.

스테이징 Supabase에는 `0177`까지 166개 migration이 적용돼 pending 0개이며, SQL Editor 앱 ACL
감사와 운영 관측의 정상 호출·Cron 강제 실패 경보·회복 종료를 확인했다. production migration은
적용하지 않았다. Supabase CLI는 P1-2에서 `2.116.0`으로 고정했다. 호스티드 원격 ACL 읽기 전용
감사의 SQL 경로는 확인했지만 직접 libpq 자격이 필요한 셸 래퍼 실측, 두 번째 관리자 MFA,
실사용 전 PITR/RPO 결정은 운영 게이트로 남는다.

## 9. 검증 구조

루트 `corepack pnpm verify`가 다음 여섯 단계를 순서대로 실행한다.

```text
타입
→ core·DB·mobile 시험
→ CLI 고정 계약·ACL 셸 보안 시험
→ 빈 DB 전체 migration + DB 36/36 + 2세션 경합 + locale parity
→ 업그레이드 경로 13/13
→ Expo 웹 번들
```

GitHub Actions는 Node 20.19.4·24에서 Docker 없는 `--no-db` 4/6을 실행하고, Node 24의
`full-db-required`가 전체 6/6을 별도로 실행한다. 두 결과와 Fable hash-chain을 결합한
`protected-gate`가 정확한 SHA에서 성공해야 보호된 `main`에 반영할 수 있다.

## 10. 확장 경계

현재는 하나의 Primary Postgres 안에서 트랜잭션 권위를 유지한다. 외부 공급사·Webhook·긴 작업이 생기면
outbox·멱등성·Queue를 먼저 도입하고, 브라우저·고정 IP·장시간 실행이 실제로 필요할 때만 Worker를
분리한다. 앱 API, Redis, 외부 Queue, Read Replica, OLAP을 달력 기준으로 선제 도입하지 않는다.

확장 구성 요소도 원장 테이블을 직접 수정하지 않고 기존 권위 RPC를 호출한다. 도입 조건과 비밀·API·
관측 경계는 [서버 확장 아키텍처 기획안](./docs/서버-확장-아키텍처-기획안.md)에 기록한다.

### 묶음 단위 템플릿 (2026-09-13)

`bundle_units`는 매장별 개수 입력 템플릿이다. 이름·양의 정수 개수·낱개 단위명(`item_unit_name`, 기본 개)·판본·삭제 표시를 보관하고
`get_bundle_units` / `save_bundle_unit` / `delete_bundle_unit` RPC로만 앱에 공개한다.
저장 전 `assert_my_store`와 매장 잠금, 행 잠금·기준 판본 검증을 수행한다. 삭제는 soft delete다.

선택 시 1묶음 수량을 입력 폼의 개 단위 용량으로 복사한다. 기존 입고·발주 RPC가 용량×수량을
최소 단위로 확정한다. 템플릿 수정·삭제는 기존 구매 옵션·발주·재고·기준단가·입력 초안을
재계산하지 않으므로 전파 E 이벤트를 만들지 않는다. 기본 저장 단위 g/ml/ea와 기존 계산 권위는 유지한다.

낱개 단위명은 1판=30모 같은 표시용 명칭이며 `ea` 계산과 무관하다. 낱개명 변경도 CAS 판본을 증가시킨다. `save_bundle_unit`의 마지막 선택 인자 생략 시 기존 이름을 보존하고 신규 값은 개로 설정한다.
