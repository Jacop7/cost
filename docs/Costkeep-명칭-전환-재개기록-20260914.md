# Costkeep 명칭 전환 재개 기록

- 기준 시각: 2026-09-14
- 작업 루트: `C:\Users\jacop\프로젝트\식자재관리앱`
- 브랜치: `codex/unify-materials-20260913`
- 기준 HEAD: `ff8ef0367d47243894f292235c2d29a8cee67eb5`
- 상태: 변경은 작업 트리에 있으며 커밋·푸시·원격 배포하지 않음

## 완료한 작업

- 앱 표시명 `코스트킵`, 코드 네임스페이스 `Costkeep`/`costkeep`, 패키지 `@costkeep/*`,
  앱 ID `com.jacop7223.costkeep`으로 현재 소스와 로컬 실행 계약을 전환했다.
- migration 0240으로 DB role·헤더·GUC·Cron·로컬 시드 이메일을 전환했다.
- migration 0241로 묶음 단위 RPC 3개의 `search_path`를 `public, pg_temp`로 고정했다.
- migration 0242로 구형 `ingredient_list(uuid)`의 앱 실행 권한을 닫고
  `ingredient_list_v2(uuid)`만 현재 재료 목록 facade로 유지했다.
- 폐기된 `save_material`·`deactivate_material`을 호출하던 호환 훅을 쓰기 차단으로 바꾸고,
  재료 통합 뒤 원가 전용 재료가 재고 경합 기대량에 포함되지 않도록 시험 기준을 바로잡았다.
- 현재 네이티브 시나리오와 터치 대상 래칫을 통합 재료 화면에 맞게 갱신했다.
- 전환 전후 데이터 행 수가 일치하고 최신 migration 0242까지 로컬 DB에 적용된 것을 확인했다.

## 검증 결과

- `corepack pnpm verify` 최신 전체 실행:
  - 타입 통과
  - fresh DB 전체 migration과 SQL 97/97 통과
  - 업그레이드 경로 25/25 통과
  - 웹 번들 통과
  - 실행 중 발견한 DB 허용 목록 기대값과 경합 기대값은 이후 수정·집중 재검증함
- 격리 DB `fresh_costkeep_final` 경합 시험: 평시·마감 경합 전 항목 통과, 시험 DB 삭제 통과
- 최종 `corepack pnpm verify --no-db`:
  - 타입 통과
  - core 269개 통과, 13개 건너뜀
  - mobile 1,429개 통과, 4개 건너뜀
  - 현재 터치 래칫과 네이티브 현재 계약 통과
  - 웹 번들 통과
- ACL: 허용 facade 85개, 실제 모바일 RPC 83개, 비-mobile 예외 2개, 미승인 RPC 0개
- 상세 로그:
  - `.codex/costkeep-rename-20260913/full-verify-after-acl-0242.log`
  - `.codex/costkeep-rename-20260913/concurrency-after-unified-filter.log`
  - `.codex/costkeep-rename-20260913/verify-no-db-final.log`

## 게이트 복구와 남은 항목

- 3표면 선언·생성물·문서 변경은 별도 커밋 경계와 새 바이트 영수증이 필요하다.
- 이력 재작성 전의 색상 결정 SHA 4개와 S3d 제품 SHA 1개는 동일 tree의 현재 조상 커밋으로
  대응시켰다. 근거는 `docs/Git-이력재작성-SHA-대응표-20260914.md`이며 과거 영수증은 수정하지 않았다.
- 네이티브 기기 캡처·영수증·글자 확대 증빙 4건은 advisory 실패다.
- Fable 독립 검수는 `PROVIDER_HARD_CAP_UNAVAILABLE`로 외부 호출 전에 중단되어 결과가 없다.
- 따라서 전체 필수 게이트와 현재 판본의 접근성 기기 검수는 완료 상태가 아니다.

## 보존 자산

- 논리 백업: `.codex/costkeep-rename-20260913/backup-before-rename/`
- `postgres.dump`: `E8B34C8FF87D4F9EE70CE311FF80D34EA22E6518C2C502E74362E3DB33B3034B`
- `globals.sql`: `5E4FC8A355A1A6FDCAD80A84DAD3D8255972432EA9AF02590FAAC0C45F9BD440`
- `baseline-counts.txt`: `69537FF6200B27973DDE72360C49DD12F2D33410C76A3085668B5C2CD8C33CFA`
- Costkeep volume: `supabase_db_costkeep`, `supabase_storage_costkeep`,
  `supabase_edge_runtime_costkeep`
- 전환 전 volume: `supabase_db_margincook`, `supabase_storage_margincook`,
  `supabase_edge_runtime_margincook`

## 다음 작업 순서

1. 작업 트리의 다른 진행 중 변경과 소유권을 확인하고 Costkeep 변경의 커밋 경계를 만든다.
2. 3표면 생성물과 바이트 영수증을 그 정확한 커밋에 결속해 재생성한다.
3. `corepack pnpm verify`를 다시 실행한다.
4. 현재 판본으로 네이티브 기기 캡처·영수증·글자 확대 증빙을 새로 수집한다.
5. Fable 제공자 하드캡 계약이 준비되면 `COSTKEEP-RENAME-20260913` 독립 검수를 재개한다.

## 로컬 재시작

프로젝트 루트에서 다음을 각각 실행한다.

```powershell
corepack pnpm db:start
node scripts/appmap/start.mjs
python -m http.server 8092 --bind 127.0.0.1 --directory .
```

AppMap은 `http://127.0.0.1:8091/appmap/`, Expo 웹은 포트 8094, 프로토타입은 포트 8092를 쓴다.
종료 시 Costkeep 컨테이너만 정지했으며 volume과 논리 백업은 보존한다.

## PC 종료 전 확인

- 확인 시각: 2026-09-14T01:43:31+09:00
- 명칭 전환 작업 idle 및 종료 준비 완료 확인. 포트 8091·8092·8094와 검증 프로세스 종료 확인.
- Costkeep 컨테이너 9개 Exited 확인, 현재 실행 중인 Docker 컨테이너 없음. 기존/새 데이터 볼륨 보존 확인.
- postgres.dump SHA-256을 위 백업 값과 대조해 일치 확인.
- 프로토타입 재시작은 저장소 루트를 제공하며 기존 /docs/prototypes/0_full-page-flow-prototype-ui-applied.html URL을 유지한다.
- 위 미완료 게이트는 그대로 남아 있으며 제품 전체 완료나 원격 배포 완료를 의미하지 않는다.
- 사용자 요청에 따라 종료 감시 자동화를 PAUSED로 전환한 다음 shutdown.exe /s /t 0으로 정상 종료 요청 예정. 강제 종료 옵션은 사용하지 않는다. 실제 전원 꺼짐은 이 기록만으로 확인하지 않는다.

- 종료 요청 결과 (2026-09-14T01:43:44+09:00): Windows가 PC 잠금 상태 때문에 강제 옵션 없는 종료를 거부함(1271). /f로 재시도하지 않았으며 PC 종료는 미완료. 종료 감시 자동화는 PAUSED 유지. 잠금 해제 후 정상 종료 필요.
