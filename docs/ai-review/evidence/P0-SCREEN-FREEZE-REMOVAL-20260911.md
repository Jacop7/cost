# P0 화면 변경 금지 해제 — 사용자 결정

- 결정일: 2026-09-11
- 권위: 현재 작업에서 사용자가 명시적으로 해당 제한을 해제함.
- 결정 직전 커밋: `535e0111737cfcd2566cfa94420409ca2f72173b`
- 범위: `three-surface-p0-check.mjs`의 제품 파일 변경 여부만으로 실패시키는 조건을 제거한다. 이 조건에는 화면뿐 아니라 공용 UI와 core 경로도 포함되어 있었다.
- 유지: 기준선 원본과 기존 실패 목록, 분류·회귀 backlog, inventory floor, 색 대비·터치·동기화·시각 검증, 게이트 파일 무결성, 타입·앱·DB·보안 시험, 독립검수, 정확한 SHA의 보호 CI와 운영 배포 가드.
- 기준선의 실측 결과는 다시 쓰지 않는다. 제거한 검사 코드의 hash 및 byte manifest에서 이번에 변경한 파일의 hash만 갱신한다. 이는 기존 측정 결과나 독립검수를 현재 코드에 대해 다시 통과했다고 주장하는 것이 아니다.
- 검사 변경 시험: 의미 없는 제품 주석 변경은 허용하도록 기존 기대값을 수정한다. dirty worktree에서 기준선 쓰기를 금지하는 시험과 게이트 hash 변조를 탐지하는 시험은 유지한다.
- 이 결정은 Fable 비용 초과 승인이나 미실행 검수의 PASS 기록이 아니다. 화면 변경 금지 해제와 운영 배포 성공은 구별한다.
