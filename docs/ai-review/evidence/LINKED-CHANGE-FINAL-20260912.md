# 연결 변경 전파 실행·검수 결과 — 2026-09-12

## 범위와 확정 동작

식재료 구매 가격·개당 용량·단가, 메뉴 판매가·기준 인분·목표 순이익률, 부자재 선택·추가·제거·금액, 고정지출 항목·금액·매출 분모, 세금 항목·요율·가격 기준·납부 주체의 연결 변경을 점검했다. 분류표는 `docs/식재료-레시피-수정내역-최종기획.md` §14가 권위이며, 표에 있다고 모든 가능한 입력을 시험했다는 뜻은 아니다.

- 영업 전·종료 후에는 현재 메뉴 기준에 반영한다. 영업 중·브레이크에는 시작 스냅샷을 보존하고 종료 후 최종 변경을 반영한다. 서버 영업 상태 전환 시 하위 메뉴 기준 쿼리도 갱신한다.
- 식재료 직접 구매 가격·용량 수정은 현재 기준단가와 연결 메뉴 원가·손익을 갱신한다. 다음 성공한 입고·부분 입고·취소는 실입고 평균으로 전환한다. 메뉴 판매가를 임의로 변경하지 않는다.
- 메뉴 기준 인분·목표율만 바뀐 경우도 반영 대기 이력에 포함한다. 같은 금액의 항목 구성 교체는 이력에 남기고 무변경 저장은 사건·판본·추이를 늘리지 않는다.
- MY 세금·국가 화면은 시장·세금의 두 판본을 검사하는 원자 저장을 공유한다. 메뉴별 명시 과세·분류 선택·기본값 상속을 구분하고 새 프로필로 유효한 선택을 승계한다.
- 적용되지 않는 세금 변경은 메뉴 대기를 만들지 않는다. 표시 이름·같은 과세 상태의 상속·다른 과세 상태로 적용 범위 확대는 실제 해당 메뉴 계산이 같으면 정보성 이력이다. 새로 영향을 받는 메뉴에는 재무 사건을 남긴다.
- 고정지출은 당월 모든 메뉴에 전파한다. 다른 월 정정은 해당 월 손익·설정 이력에 기록하고 현재 메뉴의 대기를 만들거나 현재 가격으로 과거 메뉴 추이를 합성하지 않는다.
- 과거 원장·종료 스냅샷·기존 유실 연결은 추정 복구하지 않는다. 원격 DB·Git 커밋·푸시는 이번 단계에서 수행하지 않는다.

## 구현과 독립 검수

이번 세금·영업 기준 후속 구현은 migration 0215~0229, 공통 세금 저장 훅·국가 화면·메뉴 세금 상세 및 영업 상태 캐시를 포함한다. 앞선 0208~0214 결과는 기존 AUDIT/EDGE/PRICE 보고서에 보존한다. 대량의 기존 작업 트리 변경 전체를 이번 구현으로 분류하지 않는다.

사용자가 요청한 **이번 검사 한정 Astra Ultra**에서 P2 8건을 발견·수정하고 재검증했다. 기본 모델 계획은 변경하지 않았다.

| Astra 발견 | 처리 |
| --- | --- |
| 세액은 같고 순매출만 바뀔 때 종료 추이 누락 | 0220 · 시험 80 |
| 세금 먼저 변경 후 원가 수정 이력의 날짜 기준 불일치 | 0221 · 시험 81 |
| 영업 전환 시 메뉴 기준 하위 캐시 누락 | queryClient · 실제 useDayMenuBasis 시험 |
| 국가 화면의 시장 단독 저장 | 공통 InternationalTaxScreen · 국가 화면 시험 |
| 기본값 선택이 이후 기본값을 상속하지 않음 | 0222 · 시험 82 |
| 영향 없는 메뉴에도 매장 전체 세금 대기 표시 | 0223 · 시험 82 |
| 미래 예약을 앞당길 때 메뉴 과세 선택 유실 | 0224 · 시험 83 |
| 추가세 적용 범위 확장 시 기존 적용 메뉴의 대기 오탐 | 0227 · 시험 85 |

원문: `LINKED-CHANGE-ASTRA-ULTRA-20260912.md`, `LINKED-CHANGE-ASTRA-ULTRA-20260912-ADDENDUM.md`.

지정 Claude 브라우저 R7~R10의 추가 지적도 반영했다. 0225는 비적용 세금·같은 과세 상속의 대기와 구성 라벨을 구분하고, 0226은 다른 월의 가짜 현재 메뉴 추이를 막는다. 전체 시험이 찾아낸 프로필 없음 JSON null 오류는 0227로 해결했다. R10에서 신규 P1/P2 없음, R9의 과거월 정정 정책·시험 공백 해소를 확인했다. 브라우저 검수는 공식 CLI Fable 게이트 통과를 대신하지 않는다.

R10-1의 primary 적용 범위 가정은 실제 계약과 대조했다. 0179 schema의 primary 구성 CHECK는 `applies_to_treatments=['taxable']`만 허용하고, 0181 계산 함수 170~171행과 core `internationalTax.ts` 78~80행도 primary를 taxable에만 적용한다. 비교 helper와 계산기가 같은 판정이므로 지적의 가정은 현재 계약에 해당하지 않는다. R10-2의 범위 축소 정보성 이력은 확장과 같은 의도된 동작이다.

## 실행 증거

로그·백업의 로컬 경로는 `.codex/material-history-20260912/`다.

| 검사 | 결과·증거 |
| --- | --- |
| 0227 후보 전체 SQL | 87/87 파일 통과 · linked-0227-db-final.log |
| 추가세 범위 반례 | 수정 전 실패 → 수정 후 25단언 통과 · scope-projection-before/after.log |
| 고정지출 과거·미래월 및 4상태 | 30단언 통과 · fixed-past-month-final.log |
| 세금 실제 두 연결 경합 | 두 writer CAS, 저장→마감, 마감→저장 통과 · linked-0227-concurrency.log |
| 실제 화면 후속 수정 0228~0229 | SQL88/88 · linked-0229-db-all.log, UI·계약·캐시70/70 · tax-current-final-ui.log, 타입 통과 · 0229-type.log, 세금 경합 통과 · 0229-tax-concurrency.log |
| 국가 화면·영업 상태 하위 메뉴 재조회 | 9/9 통과 · astra-country-ui-second.log |
| 최종 DB 타입 재생성·타입 검사 | 통과 · final-types.ts, final-typecheck.log |
| 별도 ACL 감사 | 내부 v3 invoker 2개·세금 조회 helper 3개·원자 저장 RPC의 정확한 시그니처를 DB16/34 계약과 동기화. 실제 DB metric 22개, 미승인 RPC/원장 직접 쓰기 0 확인 · final-acl-after.log |
| ACL 반례·셸 검사 | fresh DB에서만 권한 확대 반례 3개 탐지·rollback 확인, 셸 비밀번호·argv 격리 회귀 통과 · final-acl-negative.log, final-acl-shell.log |
| 최신 새 DB 전체 생성·시드 | 0227까지 통과 · fresh-0227-final.log |
| 새 DB 후속 게이트 | SQL87/87, ACL, 판매·설정·식재료·메뉴·세금 경합, migration 앵커·이전 응답 계약, 실제 앱 폼 DB 왕복, locale/국제 세금 parity13개 통과 · final-db-gate.log, final-db-gate-remaining.log |
| 소스 터치 검사 | 래칫 PASS·회귀 66/66. 기존 미측정 항목 유지, 커밋 결속 증거 아님 · touch-final-audit.log, touch-final-regression.log |
| 전체 verify 원 실행 | 종료(exit 1): ① 타입·② 시험·⑥ 웹 번들 통과. ③ 커밋 바이트 증빙, ④ 중간 후보 JSON null seed, ⑤ CRLF 업그레이드 실패 · astra-full-verify-final.log. 원 실행을 6/6 통과로 바꾸지 않는다. ④는 위 별도 최신 새 DB 재검증으로 해소했고, ⑤ 후속 결과는 아래에 기록한다. |
| ⑤ 실패 경로 후속 재검증 | 공식 시나리오23을 그대로 추출해 fresh DB 생성부터 최신까지 재실행, SQL47~50·77·86 통과 및 임시 DB 정리 완료(exit 0) · crlf-upgrade-final.log. 다른 23개 경로는 원 실행에서 통과했으며 전체 명령을 다시 6/6 실행한 결과는 아니다. |

## 적용·잔여 검증

개발 DB에 0215~0227 적용을 완료했다. 백업은 `before-tax-linked-0215-1789203529468.sql`, 실행·검증 영수증은 `tax-linked-local-receipt.json`이다. 기존 데이터 18개 테이블의 행 수·해시 불변을 확인했다(새 기본값 컬럼 inherit_default만 비교에서 제외). 관련 함수 24개의 본문·소유자·보안 모드·권한은 두 격리 DB와 일치한다. 두 기존 함수의 CRLF/LF 및 ACL 배열 순서 차이 때문에 최초 원시 비교는 실패했고, 줄바꿈만 LF로 정규화하고 ACL을 집합으로 비교하여 일치를 확인했다. 원시 해시 차이와 최초 실패 로그는 보존하며 바이트 동일로 표현하지 않는다. PostgREST 스키마 캐시를 갱신했다. 실제 사용자 시험 저장·과거 데이터 재계산은 하지 않았다.

권한 감사 목록 동기화는 Claude R11에서 DB16/34의 정확한 계약과 일치, 신규 P1/P2 없음으로 확인했다. 별도 인벤토리 강화·중복 상한의 단일 출처화 권고는 후속 사항이다.

실제 Expo 조회에서 ROOT가 현재 세액 1,091원/판매가 12,000원과 예약 MY 요율 8.6580%가 혼합되는 문제를 발견했다. 메뉴 세금 상세를 현재 quote·시장·구성·해석된 treatment로 통일하고, 0228로 현재 과세 분류를 같은 RPC 문맥에 추가했다. 시험 86에서 프로필 변경 없이 메뉴 과세 선택만 변경하면 종료 후에도 0원으로 남는 추가 반례를 확인했고, 0229로 종료 후 기준일에 해당 프로필의 메뉴 예외 적용일을 포함했다. 수정 전 실패 → 22단언 통과를 확인했다. 두 변경은 같은 일회성 Astra 검사의 후속 메시지 및 Claude R12에서 신규 P1/P2 없음으로 재검수했다.

0228~0229도 백업 `before-current-tax-context-0228-1789204026232.sql` 후 개발 DB에 적용했다. 최종 버전은 **20260912000229**, 영수증 `current-tax-context-local-receipt.json`의 기존 데이터 18개 테이블 불변 및 정규화한 함수·권한 일치를 확인했다. Expo 실제 세금 상세는 1,091원·세금 제외 10,909원·적용 요율 9.0909%로 일치한다. 사용자 데이터로 저장/영업 전환을 시험하지 않았다.

미해결 제한: 과거 참고 가격 수정·유실된 부자재 연결은 소급 추정하지 않았으며, 구형 시장 단독 RPC는 초기 설정 호환을 위해 남아 있으나 실제 앱 진입은 원자 저장만 쓴다. 이 보고서는 실기기·접근성 전체 검수나 운영 배포 승인이 아니다. 커밋 바이트 증빙은 아래 후속 실행에서 해결했다.

최종 업그레이드 검사에서 0188의 기존 함수가 CRLF일 때 0217의 다중 행 앵커가 맞지 않는 추가 오류를 발견했다. 원격 미배포 0217의 market 함수 정의·앵커·치환문을 CRLF→LF로만 정규화했으며 정확히 1개라는 가드는 유지했다. 시나리오 23에 SQL77 원자 세금 저장과 SQL86 종료 후 과세 반영 시험을 추가했다. 기존 개발 DB에는 0217을 다시 적용하지 않았다. 격리 DB 트랜잭션에서 수정된 변환을 실행하고 rollback한 결과 개발 DB의 정규화된 함수 본문·소유자·권한과 동일했다(`217-normalization-equivalence.json`). 같은 일회성 Astra 후속 검수에서도 새 P1/P2 없음과 CRLF/LF 동일 동작을 확인했다.

최종 작업 트리 소스·시험·검수·실행 로그 해시는 `.codex/material-history-20260912/final-linked-source-manifest.json`에 보존한다. 이 목록은 미커밋 작업 트리 증거이며 커밋 결속 3표면·실기기 증빙을 대신하지 않는다.

Claude R13에서도 0217 정규화와 시나리오23 보강에 신규 P1/P2 없음으로 검수했다. R13이 읽은 시점에는 실행 로그가 미완료였으므로 실행 통과는 Claude 판정으로 인용하지 않는다. 이후 ROOT가 종료 코드 0·행동 시험 6파일 통과·임시 DB 정리를 직접 확인했다. 줄바꿈 관용구 통일 및 향후 SQL 언어 함수까지 CRLF 주입을 넓히는 P4 권고는 후속 사항으로 보존한다.

앞선 R6의 미세 반올림 경계 추가 검토, 마감 추이 원인의 식재료명·단가 출처 표시, R12의 면세 화면에서 비적용 primary 법정 세율 행 표시 등 세부 UX 권고는 이번 완료 범위에 포함하지 않는다. 기본 세금 구성은 현재 견적에서 읽으므로 예약 값 혼합은 해소됐지만, 이 권고까지 해결했다고 표현하지 않는다.

## 커밋 증빙과 Android 조합 검사

구현·관련 시험·기록 128파일을 `da9763c7fa24d6650fa6f7f133411537f12240f9`에 커밋했다. 별도 사용자 작업은 포함하지 않았다. 권위 작업 루트에서 별도 작업 파일을 보존하여 깨끗한 상태를 만든 뒤, 공식 생성기를 `--write --expect-commit=da9763c7fa24d6650fa6f7f133411537f12240f9`로 실행했다. 레지스트리와 반응형 검사기의 기존 두 해시만 갱신했고, 218개 present 산출물·13개 음성 계약 검사 통과 후 `f9b3345`로 커밋했다. 보존 파일을 복원하고 기존 목록 7,547개 파일의 해시 일치를 확인했다. 개발 서버 8091·8092·8094를 복원하고 각 HTTP 200을 확인했다. 원격 푸시는 하지 않았다.

Android API 35 에뮬레이터의 실제 Expo/Hermes 런타임에서 서로 분리된 시험 계정·매장 4개를 사용했다. [66개 경우별 기대값·관측값](LINKED-CHANGE-EMULATOR-20260912.json)을 보존한다. 각 상태에서 순차 수정의 누적 결과까지 검사했으며, 66건 모두 통과했다.

| 조합 | 확인 내용 |
| --- | --- |
| 영업 전·영업 중·브레이크·영업 종료 | 전/종료는 즉시 갱신, 중/브레이크는 시작 기준 유지와 대기 표시 |
| 식재료 구매 가격·개당 용량·둘 다 변경 | 연결 메뉴 원가 갱신, 판매가 유지, 같은 값 재저장 시 이력 증가 없음 |
| 부자재 단가 변경·기존 연결 삭제와 새 항목 추가·새 항목 재가격 변경 | 최종 연결 구성과 수량에 따른 비용, 누적 수정 반영 |
| 고정지출 항목 교체·추가·삭제 | 최종 매장 공통 비율과 다른 메뉴 전파 |
| 세금 요율 변경·추가세 추가·삭제 | 현재 금액 또는 영업 종료 후 최종 견적 |
| 메뉴 판매가·기준 인분·목표율·식재료 사용량 동시 변경 | 변경 조합 보존, 기준 인분 환산 |
| 영업 중·브레이크에서 종료 | 대기 해소와 최종 원가·부자재·고정지출·세금 동시 반영 |
| 후속 입고 | 직접 수정 단가에서 실제 입고 평균으로 전환, 메뉴 원가 자동 갱신 |

이 66건은 네이티브 런타임의 인증된 제품 RPC·실제 공통 캐시 무효화·렌더링된 메뉴의 상태/판매가를 함께 검사한 결과다. 모든 건을 터치로 입력한 것은 아니다. 별도로 실제 ADB 터치·숫자 입력·저장으로 구매 가격 20,000원/2,000g을 적용하여 메뉴 식재료비 1,000원·순이익 9,000원을 확인했다. 5인분/1인분 탭, 세금 자세히 보기와 스크롤, 수정 내역 뱃지·사유, 기존 계정 복귀까지 7개 캡처 단언을 통과했다. 5인분에서는 식재료 5,000원·부자재 5,000원·고정지출 7,500원·세금 12,500원·순이익 45,000원, 1인분 복귀 시 각각 1/5 금액을 확인했다.

첫 비교에서는 public 47개 테이블·기존 1,874행의 해시가 유지됐다. 추가 터치 검사 이후 저장한 덤프에서는 기존 1행이 달라져 무변경 검사를 실패로 보존했다. 해당 행은 사용자가 동시에 편집한 대파이며 개당 용량 1000→2000과 수정 시각만 바뀌었다. 나머지 기존 1,873행의 해시는 유지됐다. 따라서 후속 실행까지 기존 데이터 전체가 불변이었다고 주장하지 않는다. 원시 비교와 행 차이는 `emulator-final-evidence.json`·`emulator-user-data-delta.json`에 보존한다. 시험 원장은 별도 시험 매장에 보존했으며 기존 데모 계정·제육볶음 화면으로 복귀했다. 원시 실행·XML·PNG·백업은 `.codex/material-history-20260912/`에 보존하고 인증 정보와 덤프는 커밋하지 않는다. 이 결과는 에뮬레이터 기능 검증이며 실물 기기·iOS·글자 확대 접근성 검증을 대신하지 않는다.

추가로 MY 세율 20→10%, 고정지출 10,000→20,000원, 부자재 단가 500→700원을 실제 입력·저장해 연결 메뉴를 확인했다. 터치/캡처 단언은 총 10건이며 각각 세금 1,364원, 고정지출 3,000원, 연결 수량 2개의 부자재 1,400원을 확인했다. 앞의 7건에 이 3건을 더한 수치다.

## 구매 가격 필수·영업 중 수정 안내 최종 반영

사용자 결정에 따라 구매 가격을 현재 앱 추가·전체 수정의 필수 값으로 변경했다. 앱 v2와 서버 0230은 누락/null/문자/음수를 거부하고 명시적 0원은 허용한다. 기존 null 자료와 구형 호출·메모 경로는 호환성을 유지하며 임의 가격 backfill은 하지 않는다. 0230 적용 전 SQL87 실패, 적용 후 영업 전·중·브레이크·종료 24단언 및 최신 격리 DB 전체 89/89 통과를 확인했다. 백업 후 개발 DB에도 적용했고 기존 18개 테이블 및 함수 권한 비교 영수증은 `required-price-local-receipt.json`이다.

식재료·메뉴·부자재·당월 고정지출 수정 진입에 영업 중/브레이크 확인창을 공통 적용했다. 세금은 기존 저장 확인창의 문구를 통일했다. 식재료 이력 상세에서 숨겨졌던 상태 뱃지와 직접/자동 뱃지를 표시한다. 독립 Claude R14는 신규 P1/P2 없음으로 검수했다. 구형 RPC 호환, 0원 안내, 재고 처리 진입, partial 문구 통합, 월 경계에 관한 P4 의견은 R14 원문에 보존하며 모두 구현 완료한 것으로 보지 않는다.

Android 실제 터치 후 6개 후속 캡처에서 구매 가격 공란의 저장 비활성화, 메뉴 수정 확인창, 저장 뒤 식재료/메뉴의 대기 표시, 식재료 이력 목록/상세 뱃지를 확인했다. 영업 중 20,000→22,000원 저장 후 식재료 현재 단가는 11원/g이고 메뉴 매출용 식재료비는 시작 기준 1,000원을 유지했다. 후속 캡처는 f9b3345 이후 미커밋 소스로, HEAD 표기를 커밋 결속 증빙으로 인용하지 않는다. 잘못된 RPC 인자를 쓴 시험 보조 도구의 debug overlay 캡처는 실패 자료로 별도 보존하고 통과 건수에서 제외했다. 기존 계정으로 복귀했다.

최종 앱 시험은 **116파일·1,352개 통과, 기존 7개 건너뜀**이다(`required-price-mobile-clean.log`). 타입 검사 및 3표면 레지스트리 동기화도 통과했다. 전체 verify는 앞선 후보에서 시작했고 실행 중 이번 변경이 추가됐으므로 그 결과를 최종 후보의 동일 SHA 6/6 증빙으로 사용하지 않는다. 필수 검사와 네이티브 advisory는 별도로 구분한다.

후속 웹 export, 문서 그래프 실행, 터치 래칫 및 검사기 회귀 66개도 통과했다. 부자재 확인 모달의 조건부 형제 구조 1건은 정적 미측정으로 추가 기록했으며 측정 통과로 바꾸지 않았다. 새 레지스트리 변경 때문에 현재 커밋 바이트 검사는 해시 불일치 1건을 반환한다(`required-price-byte-current.log`). 다음 관련 변경 커밋 이후 공식 생성기의 깨끗한 작업 트리 절차로 갱신해야 하며, f9b3345의 이전 바이트 통과를 이번 변경의 통과로 전용하지 않는다.

## 2026-09-12 커밋 대상 최종 후속 상태

- 식재료 정보 저장과 최초 입고 분리(v3), 안전재고 부족량 기반 발주, 미입력 재고 표시·이동을 반영했다.
- 공통 최근 수정 카드의 한 줄 수정 일시와 실제 대기 변경 시각(0233), 메뉴 상세 중복 안내 삭제, 수정/닫기 액션 팝업을 반영했다.
- 개별 실행: DB 91/91, 앱 1,348 통과·7 건너뜀(최종 점 메뉴 변경 전). 점 메뉴 변경 후 관련 앱 시험 44개·타입·웹 export·프로토타입 수정/닫기·터치 래칫·3표면 동기화 통과.
- 전체 verify는 작업 도중 시작되어 여러 후속 소스 변경을 포함하므로 최종 커밋 전체 통과 증거가 아니다. 현재 3표면 바이트 증빙의 레지스트리 해시는 갱신이 필요하며 원격 CI·배포 승인을 완료했다고 주장하지 않는다. 이전 Claude/기기 증거는 각각 기록된 당시 범위에만 유효하다.
