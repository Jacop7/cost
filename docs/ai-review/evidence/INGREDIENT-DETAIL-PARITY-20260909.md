# 식재료 추가·상세 사용자 수정 반영 / 2026-09-09

## 기준 및 범위

- 기준 HEAD: `0671a3ddd290b541f945c5f2597f57baf5a39e6c`.
- 사용자 지정 원본: 권위 루트 `docs/prototypes/0_full-page-flow-prototype-ui-applied.html`의
  `ingredient_add`, `ingredient_detail`. 원본 파일은 변경하지 않았다.
- 읽은 원본 바이트 SHA256: `7ce8bd4968232965ef85a458c03a5a0a73a0790b62f4c2e51a86fff7203fb383`.
  이 해시는 이번 로컬 입력 판본의 영수증이며 새 제품 해시 계약이 아니다.
- 변경은 실제 Expo 컴포넌트이며 `/appmap`용 별도 화면을 만들지 않았다.

## 반영

- 추가: 기존 공용 Field/Input/Select에 opt-in stackedForm 규격 적용. 용량·단위 50:50,
  단가 미리보기→구매 가격→안전재고/최소 발주→구매 링크 안내 순서.
- 추가·수정 폼의 기본 거래처·메모 입력 제거. 수정 시 기존 기록값은 보존한다.
  MY 구매처 관리 진입 메뉴 제거. 기존 구매·입고 기록/DB 스키마/구매 옵션 기능은 삭제하지 않았다.
- 정렬·카테고리·단위는 SelectionRow 공유: 박스 없이 60px 최소 행 높이, 구분선, 우측 체크.
  무게/부피/개수 제목은 표시하지 않되 수정·구매옵션의 기준단위별 선택 제한은 유지한다.
- 상세: 이름→재고 요약→구매 링크→기준 단가·최근 입고→재고 내역 순서.
  로스율 요약 카드는 이 화면에서 제거하고 원장/관련 상세 기능은 보존.
- 구매 링크/입고/재고는 최대 3건 미리보기. 4건 이상 전체보기, 구매 링크 없음은 추가 버튼.
  구매 링크 행은 프로토타입처럼 읽기 전용이며 수정 메뉴의 관리 진입은 유지.
- 최저·최고 동가 표식은 최신 최초 일치 입고에만 표시. 서버 확정 단가와 부분 입고의
  실제 수령량·결제액, 음수 잔량의 14px/빨강/800 강조는 보존.

## 검증

- 모바일 전체 시험: 65파일, 632/632 PASS. 타입 검사 PASS.
- 상세 한정 Sol 독립 검수: 초기 3 Finding(행의 편집 이동/접근성 이름/동가 중복표식) 반영,
  관련 27/27 직접 재실행 및 재검수 PASS. 픽셀·네이티브 검수는 그 판정 범위 밖.
- Chromium 실제 Expo에서 상단/600/1200 스크롤 위치 확인. 정렬형 카테고리/단위는
  390px 화면에서 행 너비 350px·높이 60px 확인.
- 상세 390px에서 수평 넘침 0. 320px에서는 기존 RecentChangeRow의 말줄임이 유지됨.
  320px 텍스트 200%는 웹 DOM 확대 확인일 뿐 Android/iOS 실측이 아니다.
- 캡처/재현 스크립트: 작업 worktree `.tmp/ingredient-add-parity`,
  `.tmp/ingredient-detail-parity`, `.tmp/ingredient-detail-reference.mjs`.

## 판정 경계

- 이번 Expo 캡처는 대파의 로컬 조회+AppMap 구매 링크 샘플, 원본은 고춧가루 예시다.
  품명·수량·가격·기록 수와 실제 최신 수정 이력에 따른 카드 높이는 같다고 주장하지 않는다.
- 기존 TYPE 행간, 공용 AppHeader/Badge/Icon/최근 수정 표시 계약은 보존했다.
  따라서 원본과 픽셀 완전 동일 판정이 아니라 지정 화면의 구조·배치 반영과 기능 회귀 확인이다.
- 전체 `pnpm verify`/DB/네이티브 게이트 및 P3 전체 페이지 종결은 이번에 실행·선언하지 않았다.

## 후속 사용자 결정 — 기준 단가의 전체보기

- 기준 단가 카드 하단은 기록 수와 무관하게 `전체보기`를 항상 표시한다.
  구매 링크/재고 내역의 4건 이상 조건은 그대로다.
- 클릭은 기존 `/ingredients/purchases/${g.id}` 구매 이력 화면으로 연결한다.
- 0/1/4건 버튼 표시 및 실제 상세 host의 대상 ID 전달 포함 24/24, 타입 검사 PASS.
  Chromium에서도 버튼 클릭 후 구매 이력 경로와 제목을 확인했다.

## 후속 사용자 결정 — 구매 행·배지·자세히보기

- 금액은 공용 PurchaseAmount로 구매처 바로 아래 별도 표시한다. 최근 입고·구매 이력은
  실수령 총액을, 구매 링크·옵션은 해당 상품 금액을 표시한다.
- `총 20kg` / `(10kg × 2개)`는 두 줄이다. packSummaryParts를 추출하되 기존 packSummary의
  합친 문장 및 부분 입고 수령량·결제액 계산은 보존했다.
- 최저·최고는 날짜 왼쪽 배지로 이동. 상세의 동일 극값은 최초 일치 기록만 표시한다.
- 상세 세 카드 버튼 문구는 `자세히보기`. 구매 링크는 1개 이상 표시(0개는 추가),
  기준 단가는 항상 표시, 재고 내역은 기존 4건 이상 조건을 유지한다.
- 위 변경: 모바일 전체 65파일 636/636 PASS, Sol 한정 검수 37/37 직접 PASS.
- 그 뒤 공통 Badge/StatusBadge 글자만 13→12px. 행간18px, 작은/기본 배지 높이18/20px는 유지.
  배지 시험 2/2 PASS 및 Chromium 실제 최고 배지 font12px/line18px/height18px 확인.
