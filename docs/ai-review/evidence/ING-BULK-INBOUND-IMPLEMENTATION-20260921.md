# ING-BULK-INBOUND-20260921 구현·검증 증거

## 구현 결과

- 재료 메인의 동작 버튼을 `+ 추가`로 바꾸고 아이콘 없는 `재료 일괄 입고` / `재료 등록` 메뉴를 연결했다.
- `ING-12 재료 일괄 입고`에서 최대 20개 카드를 추가·삭제하며 재료명, 구매처, 결제금액,
  입고량, 서버가 계산한 입고 후 단가를 다룬다.
- 미선택 구매처와 저장된 구매 옵션을 모두 지원하며, 구매 옵션의 기본 용량·금액을 사용자가 수정할
  수 있다.
- 배치 저장은 DB RPC 한 트랜잭션에서 각 카드의 E7 발주 기록과 E1 입고 원장을 함께 만든다.
  하나라도 실패하면 전체를 되돌리고, 같은 요청 키 재호출은 중복 입고 없이 기존 결과를 반환한다.
- 응답 유실 뒤 결과 조회와 앱 저널 복구를 구현했다. 저장·복구 중에는 뒤로가기와 화면 이탈을 막고,
  결과가 확정된 뒤에만 상세 화면으로 이동한다.

## 구현 범위

- 모바일: `BulkInboundScreen`, 배치 작업 저널, 도메인 훅·쿼리 무효화, Expo 라우트, 재료 메인 진입 메뉴
- DB: `20260921000144_bulk_quick_inbound.sql`, RLS·RPC ACL, 미리보기, 원자 저장, 완료 요청 조회
- 시험: 모바일 UI·복구·중복 입력, DB 단위·ACL·동시성·멱등성, AppMap ING-12 샘플과 실제 Expo 연결

## 검증

- `corepack pnpm verify`: 6/6 통과
  - 타입 검사 통과
  - DB 124/124, core 281 통과(13 제외), mobile 1,790 통과(4 제외)
  - CLI·ACL·문서·디자인 계약 통과
  - 새 DB 전체 migration·DB 스위트·2세션 경합·locale parity 통과
  - 업그레이드 경로 26/26 통과
  - Expo 웹 번들 통과
- Astra Ultra 독립검수: 최종 대상 `41d68c03e1a1902bb6c0f354faeb85e5e0ee8db2`, `READY`, R0~R3 없음
- 원래 작업 브랜치 반영 뒤 코드·DB·시험 파일이 위 검수 대상과 동일함을 diff 0으로 확인했다.
- 등록 [Claude 브라우저 채팅](https://claude.ai/cowork/cse_01N6B7WyewbSWEy6NsiXP68R) 구현 검수:
  `READY`, R0~R2 없음. R3 네 건을 모두 반영했다.
  - 진입 복구 실패 뒤 `이전 요청 다시 확인` 버튼과 재실행 시험
  - `useDeferredValue` 기반 미리보기 지연과 최신 미리보기 전 저장 차단
  - 마지막 카드 삭제 뒤 새 빈 카드 유지로 계획·구현 통일
  - 같은 재료·다른 구매처 가중단가, 미리보기/확정 일치, 서로 다른 요청 키 실제 잠금 경합 시험
- 최종 보완분 검증: 모바일 타입 검사, 일괄 입고 UI·저널 15/15, 기존 migration이 적용된 fresh DB의
  `35_bulk_quick_inbound.sql` 전체 단언, same-key·distinct-key 2세션 경합을 통과했다.
- 같은 Claude 브라우저 채팅의 반영본 재검수: `READY`, `findings: none`,
  `BULK-IMPL-01`~`04` 모두 `CLOSED`.
- 공식 `pnpm fable:review` round 1은 외부 호출 전에 `PROVIDER_HARD_CAP_UNAVAILABLE`로 중단됐다.
  Fable 공식 회차가 성공한 것으로 표시하지 않는다.

## 남은 비차단 증거

네이티브 기기 캡처·영수증·통합·글자 확대 증거 4종은 운영 정책대로 `ADVISORY_FAIL`이다. 소스 터치
크기·대비·네이티브 검사기 회귀시험은 필수 게이트에서 통과했지만, 실제 iOS 제스처와 기기 글자 확대
검수를 완료했다고 보지는 않는다.
