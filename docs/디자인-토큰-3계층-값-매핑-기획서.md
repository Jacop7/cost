# 디자인 토큰 3계층 값 매핑 기획서 (2판)

> 작성: 솔라(Claude Opus) · 2026-09-04
> 상태: **검수 대기** — 초판(`a36adc4`)은 솔·페이블 양쪽에서 `CHANGES_REQUIRED`
> 선행: `docs/디자인-토큰-집행-계획.md` §7 `P1`
> 인용 가능한 측정 산출물 — **이 문서는 아래 5개 밖의 수치를 인용하지 않는다**
> - `docs/prototypes/full-page-flow-prototype-design-audit.json` — 축 8종 · target 182 · `DS-20260904-015`
> - `docs/prototypes/full-page-flow-prototype-i18n-stress.json` — 글로벌 4패스 · target 182 · `DS-20260904-015`
> - `docs/prototypes/full-page-flow-prototype-render-audit.json` — 렌더 계약 4패스 · target 185
> - `docs/prototypes/full-page-flow-prototype-token-map-check.json` — 매핑 충족 판정
> - `docs/prototypes/full-page-flow-prototype-contrast-fix.json` — WCAG 대비와 최소 통과 색
> - `docs/token-adoption-audit.json` — 앱 토큰 채택 · 파일 185

---

## 0. 초판에서 무엇이 틀렸는가

초판을 솔(Codex)과 페이블에게 각각 보냈고 **둘 다 독립적으로 같은 곳을 찍었다.**
솔 Major 8 · Minor 1, 페이블 Major 4 · Minor 8. 반박 0건.

**핵심은 값 선택이 아니라 재는 단위였다.**

| 초판의 주장 | 실제 |
|---|---|
| 1·2·3·6px 은 "광학 보정" 이므로 별도 `nudge` 스케일이 필요하다 | **1px·6px 관측의 대부분이 브라우저 기본 버튼 padding 이었다.** `.expo-more` 는 `<button>` 인데 padding 선언이 아예 없다 |
| `.edit-form-label` 의 `margin-bottom:2px` 은 baseline 보정이다 | 실제 선언은 `margin:0 2px 7px` — **2px 은 좌우**다. 집계 키에서 변(side)을 버려서 생긴 오독 |
| 오차는 대부분 "덜 늘림" 이라 결과가 보수적이다 | **반대다.** 덜 늘렸다면 실제 번역은 그보다 길고, 그때의 잘림 0 은 낙관적이다 |
| 뱃지용 `12px` 을 스케일에 추가한다 | UI 가이드가 Badge·Chip·Filter 를 `captionSm 13/600` 으로 **이미 계약**했고 별도 뱃지 글자 토큰을 만들지 말라고 명시한다 |
| 하단 탭 라벨에 글자 확대 상한을 둔다 | WCAG 1.4.4 는 라벨을 포함한 텍스트가 200%까지 커질 수 있어야 한다고 본다. **아이콘은 라벨의 대체재가 아니다** |
| 가이드의 그림자 4종이라 새로 만들 것이 없다 | 가이드의 4종은 `card·sheet·fab·sliderThumb` 이고 실측한 `bottomBar` 는 그중에 없다 |
| 미매핑 0건이면 `P1` 완료 | 앱 감사는 파일별 **개수**만 저장하고 property/value/location 을 안 낸다. 모션·z-index 도 소스에 실재한다 |

`PRT-194` 에서 **측정 단위에 관한 것을 전부 고치고 다시 쟀다.** 이 2판은 그 위에 선다.
값 결정에 관한 지적은 아래 §4~6 에서 처리한다.

---

## 1. 측정이 말하는 것

### 1.0 수치의 단위 — 먼저 읽어야 한다

**첫째, 모든 "회" 는 소스 선언 수가 아니라 렌더 관측 수다.** 활성 182 target 을 하나씩 열어
그때 화면에 있는 요소를 세므로, 여러 화면에 공통으로 뜨는 것은 뜨는 화면 수만큼 곱해진다.
`padding 104px` 이 182회인 것은 선언이 182개라는 뜻이 아니라 **선언 하나가 182개 화면에
뜬다**는 뜻이다. 그래서 **수렴 결정은 전부 "관계" 기준으로 내렸고, 관측은 영향 범위를
가늠하는 보조 지표로만 썼다.**

**둘째, 값에는 출처가 있다.** 모든 축이 `source` 를 함께 남긴다 —
`author`(이 요소에 걸린 규칙이 선언) · `inline`(요소 style 속성) ·
`inherited`(여기 선언은 없지만 **조상의 작성자 선언이 내려온 것**) ·
`ua`(아무도 선언하지 않은 브라우저 기본값).

| 축 | author | inline | inherited | ua |
|---|---:|---:|---:|---:|
| 간격 | 16,471 | 126 | — | **1,804 (9.8%)** |
| 색 | 8,069 | 61 | **2,718** | **0** |
| 반경 | 2,460 | 6 | — | 0 |

**`ua` 만 디자인 값이 아니다.** 아무도 고르지 않았으므로 토큰 후보가 될 수 없다.
2판 초고는 `inherited` 와 `ua` 를 한 칸에 묶어 "색 `ua` 2,718" 이라고 적었는데,
**그 2,718 은 전부 상속된 작성자 색이고 진짜 브라우저 기본색은 0건이다.**
'아무도 고르지 않은 값' 이라는 정의를 스스로 어긴 셈이라 축을 넷으로 나눴다.

간격의 `ua` 1,804 는 진짜다 — padding 은 상속되지 않으므로 선언이 없으면 브라우저가
정한 것이다. 초판은 이걸 섞어 세는 바람에 `nudge` 라는 스케일을 만들 뻔했다(§4.7).

### 1.1 디자인 축 (390×844, 활성 182 target, `author` 기준)

| 축 | 종류 | 관측 | 핵심 소견 |
|---|---:|---:|---|
| 타이포 | — | 6,974 | 카드×슬롯 268조합 중 **41조합이 한 카드 안에서 갈린다**(행간 포함) |
| 행간 | 18 | 6,974 | **`normal` 이 5,908회(84.7%)** — 선언 없이 브라우저 기본을 쓴다 |
| 자간 | 2 | 6,974 | `normal` 6,899 · `-0.3px` 75 |
| 색 | 35 | 8,069 | 같은 역할에 미세하게 다른 값이 갈려 있다 |
| 간격 | 25값 | 16,471 | `space` 스케일 안 **37.0%** · 밖 63.0% |
| 반경 | 20 | 2,460 | 같은 형태에 2~6개 값 |
| 그림자 | 6 | 405 | 실질 4역할이나 가이드 4종과 이름이 하나 어긋난다 |
| 컨트롤 높이 | — | 2,158 | shell 기준으로 다시 쟀다 |
| 아이콘 | 17 | 697 | 같은 `›` 가 17 · 20 · 21px |

### 1.2 글로벌 스트레스 (320×720, 4패스)

**하단 탭 라벨의 여유율** — 결정에 직접 쓰이므로 `i18n-stress` 의 `base` 패스에서
보존 측정한다. 초판은 이 수치를 즉석 스크립트로 내서 인용했고, 그건 이 문서가 스스로
세운 인용 규칙 위반이었다.

| 라벨 | 글자 폭 | 상자 폭 | 여유 |
|---|---:|---:|---:|
| 식재료 | 33.7px | 64px | 89.8% |
| 레시피 | 33.7px | 64px | 89.8% |
| 발주 | 22.5px | 64px | 184.6% |
| **매출관리** | **45px** | **64px** | **42.4%** |
| MY | 20.5px | 64px | 212% |

탭바 높이 60px, 탭 5개가 320px 을 나눠 가지므로 탭당 64px, 모두 `nowrap` 13px 이다.
**`매출관리` 의 여유 42.4% 는 `w150`(+50%)를 못 버틴다** — 그런데 `w150` 잘림 목록에
탭 라벨이 없었다. 페이블이 이 모순을 손으로 짚었고, `atRisk` 판정을 넣자 도구가 같은
답을 냈다 — 그 라벨은 15.6px **덜 늘어난 채** 여유 0 이었다. **모순은 제품이 아니라
측정에 있었다.**


| 패스 | 잘리는 요소 | 확대 정확도 ±1% | 덜 늘어남 | **atRisk** |
|---|---:|---:|---:|---:|
| `base` 한국어 원문 | **0** | — | — | — |
| `w130` 번역 폭 +30% | 0 | 6,830 / 6,986 (97.8%) | 106 | **106** |
| `w150` 번역 폭 +50% | 2종 | 6,564 / 6,986 (94.0%) | 332 | **302** |
| `w130t2` +30% & 글자 200% | 182 target | 6,830 (97.8%) | 106 | 106 |

`w150` 만 두 열의 수가 다르다 — **덜 늘어난 332건 중 302건이 `atRisk`**, 나머지 30건은
모자란 폭이 그 host 의 남은 여유 안에 들어간다. `w130` 은 106건 전부가 `atRisk` 라 두 열이
같을 뿐이다. **두 열은 같은 수가 아니며, 같아 보일 때도 우연이다.**

**`atRisk` 를 반드시 함께 읽어야 한다.** 덜 늘어난 요소의 "잘림 0" 은 보수적이 아니라
낙관적이다 — 실제 번역은 그보다 길다. `atRisk` 는 그중 **모자란 폭이 그 상자의 남은
여유보다 큰 것**, 곧 제대로 늘렸다면 넘쳤을 것을 센다.

그래서 이 문서는 다음과 같이만 말한다.

- **`base`(한국어 320px)는 깨끗하다.** 이건 확정이다 — 확대를 하지 않았으므로 오차가 없다.
- **`w130`·`w150` 의 "잘림 0" 은 관측이 그랬다는 뜻이고, `atRisk` 는 그 관측이 왜
  미덥지 않은지를 말한다.** `atRisk` 의 정의대로라면 이들은 **제대로 늘렸을 때 넘쳤을
  요소**다. 그러므로 "번역 +30% 로는 아무 데도 안 깨진다" 는 초판·2판의 문장은 틀렸다.
  **+30% 에서 8클래스 106건이 깨질 것으로 판정된다**(§1.3).
- `w130t2`(번역 +30% & 글자 200%)는 182 target 전부에서 무언가 잘린다. **이건 오차와
  무관하게 확정이다** — 이미 잘린 것을 덜 늘렸다고 안 잘리게 되지는 않는다.

**결론 — 글자 확대가 지배 변수인 것은 맞지만, 번역 길이도 이미 문제다.**

### 1.3 `+30%` 에서 깨질 것으로 판정된 8클래스 (106건)

`atRisk` 를 클래스로 묶으면 어디를 고쳐야 하는지가 바로 나온다.
2판까지는 이 목록이 문서 어디에도 없었고 §6 은 `w130t2` 자리만 다뤘다.

| 클래스 | 건수 | 최대 부족 | 예 | 배정할 계약 |
|---|---:|---:|---|---|
| `sales-menu-sub` | 30 | 36.9px | `매장 15 · 배달 8 · 포장 2` | **열거형 요약** — 줄바꿈 허용 + 채널명 축약 규칙(§6.9) |
| `small` | 32 | 6px | `기준 단가` · `김치찌개 · 된장찌개 ·` | 행 보조 정보 — 줄바꿈 허용 + 행 `min-height` 확장 |
| `filter-text` | 12 | 17.3px | `최근 3개월` · `전체` | 필터 칩 — 칩 폭을 내용에 맞추고 줄은 가로 스크롤 유지 |
| `strong` | 12 | 15.6px | `불판 가스비` · `돼지고기 앞다리` | 재료·항목 이름 — 줄바꿈 허용(고유명사 계약, §6.3) |
| `row-title` | 9 | 48.3px | `폐기 · 조리 후` | 행 제목 — 줄바꿈 허용 + 행 높이 확장 |
| `row-sub` | 6 | 29.7px | `영업 종료 후 남은 재료` | 행 보조 — 같은 계약 |
| `callout` | 3 | 3.1px | `가게의 월 고정비를 매출…` | 문단 — 줄바꿈은 이미 되므로 **좌우 여백 부족**이다(§6.10) |
| `confirm-copy` | 2 | 6.2px | `영업 원장은 보존되지만…` | 같은 문단 계약 |

`+50%` 에서는 여기에 **`app-tab-label` 182건**(최대 15.6px)이 더해진다 — 탭 라벨은
`nowrap` 이고 상자가 64px 로 고정이라 줄바꿈으로 흡수할 수 없다. §6.1 이 그 자리다.
`option-link-name`(3건 27.7px) · `expo-status`(3건 7px) · `title`(1건 66.1px)도 추가된다.

**`sales-menu-sub` 가 가장 위험하다.** `매장 15 · 배달 8 · 포장 2` 는 영어로
`Dine-in 15 · Delivery 8 · Takeout 2` 가 되어 폭이 두 배 이상 된다 — +30% 로도 모자란
자리다. 채널명을 아이콘·약어로 줄이거나 줄바꿈을 허용해야 한다.

---

## 2. 3계층 배치 원칙

| 계층 | 무엇을 담는가 | 정본 | 이름이 답해야 하는 질문 |
|---|---|---|---|
| **기본(primitive)** | 의미 없는 원시 값. 팔레트 · 스케일 | `tokens.ts` | "이 값은 무엇인가" |
| **의미(semantic)** | 원시 값에 붙인 역할. 별칭이지 새 팔레트가 아니다 | 가이드 §1.1 매핑표 | "이 값을 **왜** 쓰는가" |
| **컴포넌트(component)** | 한 부품의 규격. 의미 토큰의 조합 + 동작 계약 | 실행서 `C-01`~`C-21` | "이 부품은 **어떻게** 생겼는가" |

1. **한 값이 두 계층에 동시에 살지 않는다.** 중복이 곧 두 번째 정본이다.
2. **화면 CSS 는 컴포넌트 층만 참조한다.** 앱은 이미 이 규칙이 깨져 있다 —
   제품 화면 170개가 `T.` 를 1,667회 직접 참조한다.
3. **값을 좁히는 모든 결정에 근거를 붙인다.**
4. **기존 계약과 충돌하는 제안은 계약 개정 절차를 함께 제시한다.** 초판은 뱃지 `12px` 을
   그냥 신설했는데, 가이드가 이미 `captionSm 13/600` 으로 계약해 둔 자리였다.
   "새 표준을 만들지 않는다" 는 이 문서의 지위와 정면으로 어긋난다.

---

## 3. 수렴 판정 기준 — 속성마다 다르다

| 속성 | 판정 기준 | 안전 방향 |
|---|---|---|
| 간격 | **무엇과 무엇 사이인가** + **어느 변인가** | 가로 padding 은 줄이는 방향이 안전. 늘리면 본문 폭이 준다. 세로는 폭과 무관 |
| 타이포 | 위계상 어느 역할인가 + **글자 200% 확대에서의 결과** | 줄이는 방향이 안전 |
| 행간 | 역할이 같으면 값도 같아야 한다 | 웹·RN 결과가 갈리는 `normal` 을 없애는 방향 |
| 반경 | 컴포넌트 형태 | 폭에 영향 없음. 자유 |
| 컨트롤 높이 | 터치 최소 44px 충족 + 같은 역할인가 | 세로라 폭 무관. 키우는 방향이 안전 |
| 색 | **대비 검산 통과 여부** | 폭에 영향 없음. 단 **대비를 재기 전에는 자유가 아니다**(§9-3) |

**"안전" 은 재측정 없이 진행해도 좋다는 뜻이다.** 반대 방향은 `P2` 후 글로벌 재측정에서
`base` 잘림 0 유지와 **`atRisk` 신규 0 · 유지 항목의 부족 폭 악화 0** 을 확인한 뒤에만
확정한다. 초판은 "`w130` 잘림 0" 을 게이트로 삼았는데 `atRisk` 가 106건이므로 그 게이트는
아무것도 막지 못한다. **그리고 `atRisk` 를 개수로만 비교하면 106건이 다른 106건으로
바뀌어도 통과한다** — 이 문서가 §0 에서 지적한 "target 개수만 같으면 악화를 놓친다" 와
같은 함정이다. 그래서 게이트는 **ID 양방향 대조 + 부족 폭 크기 비교**로 한다.
현재 106건은 회귀 비교용 **알려진 기준선**이지 승인이 아니다.

---

## 4. 기본(primitive) 층

### 4.1 색 팔레트 — author 35종 → 24종

`tokens.ts` 가 값의 정본이므로(가이드 §1.2) 프로토타입 쪽을 수렴시킨다.
아래는 **`author` 선언만** 센 것이다. 나머지 2,718 관측은 `inherited` — 조상의 작성자
선언이 내려온 것이라 새 결정이 아니다. **진짜 브라우저 기본색은 0건**이다.

| 역할 | 프로토타입 실측 | author 관측 | 결정 |
|---|---|---:|---|
| 잉크 | `rgb(25,31,40)` | 743 | `color.ink.900` |
| 잉크2 | `rgb(51,61,75)` | 57 | `color.ink.700` |
| 보조 | `rgb(78,89,104)` | 1,188 | `color.ink.500` |
| 약한 | `rgb(139,149,161)` | 1,490 | `color.ink.300` |
| 흰 표면 | `rgb(255,255,255)` | 1,633 | `color.white` |
| 보조 표면 | `rgb(249,250,251)` | 529 | `color.gray.50` |
| 앱 배경 · 내부 선 | `rgb(242,244,246)` | 18 + 161 | `color.gray.100` — 역할 둘, 값 하나(§4.2) |
| 외곽선 | `rgb(229,232,235)` | 571 + 182 | `color.gray.200` |
| 강한 선 | `rgb(213,219,225)` | 75 | `color.gray.300` |
| Primary | `rgb(49,130,246)` | 349 + 180 + 59 | `color.blue.500` |
| 정보 tint | `rgb(234,243,255)` `rgb(244,248,255)` | 98 + 27 | **2 → 1** |
| 정보 선 | `rgb(184,212,255)` `rgb(159,196,255)` `rgb(201,221,255)` | 27 + 12 + 10 | **3 → 1** |
| 위험 | `rgb(240,68,82)` | 205 | `color.red.500` |
| 위험 tint | `rgb(254,236,236)` `rgb(255,240,241)` | 38 + 29 | **2 → 1** |
| 위험 선 | `rgb(255,198,198)` | 2 | 유지 |
| 긍정 | `rgb(14,159,110)` | 39 | `tokens.ts` 는 `#15B374` — §4.4 |
| 긍정 tint | `rgb(231,247,240)` `rgb(232,248,241)` | 11 + 6 | **2 → 1** |
| 주의 | `rgb(183,110,0)` `rgb(224,122,0)` `rgb(233,121,0)` | 12 + 18 + 3 | **3 → 1** |
| 주의 tint | `rgb(255,244,229)` | 12 | 유지 |
| 스크림 | `rgba(0,0,0,.42)` | 182 | `color.scrim` |
| 핸들 | `rgb(229,232,235)` `rgb(209,214,219)` `rgb(217,222,228)` | 182 + 4 + 3 | **3 → 1** |
| 도넛 | `rgb(122,134,148)` `rgb(197,204,211)` | 1 + 1 | 데이터 시각화 팔레트로 분리 |

**단 이 수렴은 §9-3 이 풀리기 전까지 확정할 수 없다.** 3 → 1 로 합칠 때 **어느 값으로**
합치는지가 대비 결과를 바꾼다. 목표값을 §8 `D-1` 에 묶어 둔다. → **[`D-1` 대기]**

### 4.1a 대비 — **텍스트의 35.2%가 AA 를 못 넘는다**

2판까지 이 축을 재지 않았고, 그래서 색 수렴을 전부 `D-1` 뒤로 미뤄 뒀다.
이제 잰다. 결과는 색 수렴보다 훨씬 큰 문제다.

| | 값 |
|---|---:|
| 전경·배경 조합 | 34 |
| **AA 실패 조합** | **20** |
| 텍스트 관측 | 6,974 |
| **AA 실패 관측** | **2,453 (35.2%)** |

기준은 WCAG 2.x AA — 일반 텍스트 4.5:1, 큰 글자(24px 이상 또는 18.66px 이상 & 700 이상)
3:1. 배경은 투명하지 않은 가장 가까운 조상의 배경색이고 반투명은 알파 합성한다.

**가장 큰 것부터.**

| 조합 | 관측 | 현재 | AA 최소 | 어디에 |
|---|---:|---:|---|---|
| `#8B95A1` on `#FFFFFF` | **1,325** | **3.04:1** | 글자 `#6C7886` | 날짜 · 보조 메타 · 단위 (`--ter`) |
| `#FFFFFF` on `#3182F6` | 173 | 3.71:1 | **배경** `#1470F5` | FAB · 완료/저장 버튼 |
| `#8B95A1` on `#F9FAFB` | 161 | 2.91:1 | 글자 `#6A7582` | 카테고리 뱃지 |
| `#3182F6` on `#F2F4F6` | 145 | 3.37:1 | 글자 `#0A68EE` | 필수 표시 `*` |
| `#3182F6` on `#FFFFFF` | 139 | 3.71:1 | 글자 `#1470F5` | 링크 · 최저가 |
| `#F04452` on `#FFFFFF` | 138 | 3.71:1 | 글자 `#EB1325` | 음수 · 최고가 |
| `#8B95A1` on `#F2F4F6` | 135 | **2.76:1** | 글자 `#66717E` | 탭 라벨 |
| `#0E9F6E` on `#FFFFFF` | 22 | 3.39:1 | 글자 `#0C875E` | 순이익 |
| `#FFFFFF` on `#D1D6DB` | 3 | **1.46:1** | 배경 `#5D5D5D` | **비활성 Primary — 사실상 안 보인다** |

세 가지가 눈에 띈다.

1. **`--ter`(`#8B95A1`) 하나가 실패 관측의 절반이 넘는다**(1,325 + 161 + 135 = 1,621).
   보조 글자 한 토큰만 진하게 해도 AA 실패의 66%가 사라진다.
2. **흰 글자가 실패하는 자리는 글자가 아니라 배경을 움직여야 한다.**
   채움 버튼의 흰 글자를 회색으로 만드는 것은 답이 아니다. 측정기도 그렇게 낸다.
3. **비활성 Primary 위의 흰 글자가 1.46:1 이다.** 이건 수렴 대상이 아니라 결함이다.

수정안은 **색상·채도를 유지한 채 명도만** 움직여 기준을 처음 넘는 지점이다.
색을 바꾸는 것이 아니라 같은 색의 진하기를 조절하는 것이므로 브랜드 정체성이 덜 흔들린다.

### 4.2 값이 같아도 역할이 둘이면 의미 토큰은 둘이다

`rgb(242,244,246)` 은 앱 배경이자 내부 구분선이다. 지금 값이 같다고 하나로 두면, 나중에
배경만 바꾸려 할 때 선까지 따라 바뀐다. 기본 층에서는 `color.gray.100` 하나,
의미 층에서 `surface.app` 과 `line.inner` 두 별칭을 건다.

### 4.3 결함 — `<button>` UA 리셋 누락 **(고쳤다 · `PRT-194`)**

초판은 이걸 발견해 놓고 `P2` 로 미뤘다. 솔의 지적대로 **그건 늦다** — 이 결함은 색만이
아니라 테두리 두께와 1·6px padding 까지 `P1` 입력값을 오염시키므로, 오염된 값을 먼저
매핑하면 매핑표에 허위 역할이 남는다. `P0` 로 올려 먼저 고치고 전부 다시 쟀다.

```
BUTTON.sales-menu-row  ← border:0·background·color:inherit 누락
  color rgb(0,0,0) · border-top 2px rgb(0,0,0) · background rgb(239,239,239)
```

`.sales-menu-row`(analytics·day 35회)와 `button.edit-form-row`(my_hours 계열 12회) 두 곳.
다른 행 버튼은 제대로 리셋해 뒀다. 고친 뒤 `#EFEFEF` 배경과 검정 테두리가 사라졌고,
**렌더 감사 위반은 그대로 5건**(`KD-001` 4 + `KD-002` 1)이라 회귀가 없다.

### 4.4 실행서 §3.1 표기 오류 3건

| 실행서 §3.1 | `tokens.ts` | 프로토타입 |
|---|---|---|
| `T.green` = `#0E9F6E` | `#15B374` | `#0E9F6E` |
| `T.amberText` = `#B76E00` | `#E07A00` | `#B76E00` · `#E07A00` · `#E97900` |
| `T.blueTint` = `#EAF3FF` | `#EBF3FE` | `#EAF3FF` |

실행서와 프로토타입이 한 짝, 코드가 다른 짝이다. **이제 대비를 재므로 답할 수 있다.**

| 역할 | 문서·프로토타입 | 대비 | 코드(`tokens.ts`) | 대비 | 더 나은 쪽 | AA 최소 |
|---|---|---:|---|---:|---|---|
| `green` | `#0E9F6E` | 3.39:1 | `#15B374` | **2.72:1** | **문서** | `#0C875E` |
| `amberText` | `#B76E00` | **4.00:1** | `#E07A00` | 3.02:1 | **문서** | `#AB6700` |
| `blueTint` | `#EAF3FF` | — | `#EBF3FE` | — | 배경색이라 전경과 짝지어야 비교된다 |

**둘 다 문서·프로토타입 값이 코드 값보다 대비가 낫고, 그런데도 둘 다 AA 를 못 넘는다.**
그러므로 "어느 쪽으로 맞출까" 는 잘못된 질문이었다. 답은 **어느 쪽도 아니고, 둘 다
AA 최소값까지 진하게 해야 한다**이다. → **[`D-1′` 대기 — 값은 나왔고 승인만 남았다]**

### 4.5 타이포 스케일 — **6단계 유지**

초판은 뱃지 때문에 `12px` 을 추가해 7단계로 만들자고 했다. **철회한다.**
UI 가이드가 Badge·Chip·Filter 를 `captionSm 13/600` 으로 이미 계약했고 별도 뱃지 글자
토큰을 만들지 말라고 명시한다. 이 문서는 새 표준을 만드는 문서가 아니다(§2-4).

| 스케일 밖 | author 관측 | 어디에 | 결정 |
|---|---:|---|---|
| `12px` | 241 | 뱃지 89 · 상태 58 · 카테고리 38 · 세금 라벨 28 · 기타 28 | **`13px` 로 수렴** — 가이드 계약을 따른다 |
| `15px` | 210 | 행 라벨·값 176 · 버튼·입력 34 | 라벨 → `14`, 버튼·입력 → `16` |
| `19px` | 112 | `h2` 110 = **Sheet·팝업 제목** | `title 20/800` 으로 수렴 |
| `10px` | 5 | `profit-badge` | `13px` (뱃지 역할) |
| `11px` | 3 | `순이익률` | `13px` |
| `13.3333px` | 30 | `<input type=checkbox>` UA 기본값 | 텍스트 아님. 제외 |

**확정 스케일 — `22 / 20 / 18 / 16 / 14 / 13` 6단계.** 가이드와 `TYPE` 을 그대로 쓴다.

뱃지를 `13px` 로 올리면 폭이 약 8% 는다. 초판은 이걸 "안전 문제" 로 프레이밍했는데
**근거가 없었다** — 글로벌 측정상 뱃지는 `w150` 까지 잘리지 않는다(`recent-change>expo-status`
가 `w150` `atRisk` 에 7px 로 한 건 있을 뿐이다). 순수한 디자인 판단이고, 가이드 계약이
이미 답을 정해 뒀으므로 여는 결정이 아니다. **초판의 `D-3` 은 폐기한다.**

### 4.6 같은 크기에서 굵기가 갈리는 자리

| 자리 | 실측 | 실행서 §3.2 역할 | 결정 |
|---|---|---|---|
| `card b` | `22/700` 3회 | Display `22/800` | `800` 으로 |
| `detail-name` | `20/700` 6회 | Title `20/800` | `800` 으로 |
| `order-head strong` `stock-add-name` | `18/800` 16회 | Header `18/700` | `700` 으로 |
| 뱃지류 | `12/700` 과 `12/800` 혼재 | Badge = `captionSm 13/600` | `600` 으로 |

`800` 은 실행서 §3.2 상 "제목 · 핵심값 · 최종 합계" 전용이다.

`R-TY-SIZE-15-PROSE`의 안내 문단 4자리는 소스 전수 확인 결과 모두 직접 `<Text>`에
`fontSize`·`lineHeight`만 두고 **`fontWeight`를 선언하지 않았다**
(`Sheet:77` · `MyAccount:64/:74` · `ShortageWarningSheet:44`). 따라서 현행 렌더 굵기는
RN 기본 `normal`인 **400**이고 700이 아니다. `TYPE.body 16/22` 크기·행간 배정과 별개로,
기획서에 아직 400 역할이 없는 문제는 S3 타이포 역할 적용 때 함께 처리한다.

**그리고 굵기 `900` 이 188회 렌더된다.** 188건 전부 `<b>`·`<strong>` 에 `font-weight` 를
선언하지 않아 브라우저 기본 `bolder` 가 부모(600·700·800)에 대해 900 으로 해석된 것이다.
Pretendard 는 900 face 가 없으므로 800 face 로 그려져 눈에 보이는 차이는 없지만,
**부모가 600 인데 800 face 가 선택되는** 자리가 생긴다.

렌더 감사가 이걸 못 잡은 것은 결함이 아니다 — 그 검사는 "누가 900 을 **선언**했나" 를 묻고,
`bolder` 파생은 명시적으로 예외 처리해 뒀다(`[L6]`). 다만 그 결과 **"무엇이 900 으로
렌더되나" 를 아무도 안 보고 있었다.** 두 질문은 다르고 둘 다 필요하다.
어느 역할 굵기로 선언할지는 → **[`D-7` 대기]**

### 4.7 간격 스케일 — **하나로 충분하다 (`nudge` 폐기)**

초판은 `space` 와 `nudge` 두 스케일을 제안했다. **철회한다.** 분리 근거였던 1·6px 이
대부분 브라우저 기본값이었다.

| 값 | 초판이 본 관측 | `author` 만 | 실제 정체 |
|---:|---:|---:|---|
| `1px` | 953 | **31** | 나머지는 `<button>` 기본 `padding:1px 6px` |
| `6px` | 1,098 | **368** | 나머지는 같은 UA padding |
| `2px` | 1,104 | 912 | 나머지는 `<input>` 기본 padding |

`author` 선언만 남기고 **변(side)까지 보면 넷의 성격이 서로 다르다.**
초판은 "전부 `space` 역할" 이라고 일괄 수렴시켰는데, `nudge` 를 폐기하는 것과
**값을 4/8px 로 바꿔도 된다는 것은 다른 결론**이다.

| 값 | author | 대표 관계 | 판정 |
|---:|---:|---|---|
| `1` | 31 | `content>category-note` `mt` 20 · `div>sheet-section` `mr/ml` 8 | **컴포넌트 소유.** 4관계 31관측뿐이라 스케일로 승격할 근거가 없다 |
| `2` | 912 | `stock-option-choice` **`pr/pl`** 336 · 라벨 **`mr/ml`** 444 · `category-arrows` `gap` 96 | **컴포넌트 소유 광학 보정.** 라벨 좌우 2px 은 입력 상자 자신의 안쪽 여백만큼 라벨을 밀어 **글자 시작선을 맞추는** 값이다. 4px 로 늘리면 시작선이 어긋난다. 칩 좌우 인셋도 늘리면 본문 폭이 주는 방향이라 §3 에 걸린다 |
| `3` | 971 | `expo-row-value>small` `mt` 169 · `row-main>row-sub` `mt` 75 · `values>small` `mt` 69 | **`space.4` 로 수렴.** 거의 전부 `margin-top` 이고 값↔보조 설명이라는 단일 역할이며 **세로**라 폭에 영향이 없다. 의도된 시각 변화 |
| `6` | 368 | `expo-section-head>small` `ml` 103 · `row-main>row-top` `gap` 60 | **`space.8` 로 수렴.** 대부분 flex 가로 `gap` 이라 **폭이 는다** → `P2` 후 `atRisk` 증가 0 확인 필요 |

**`nudge` 라는 별도 스케일은 필요 없다**(초판 `D-4` 폐기). 다만 그 결론이
"1·2px 을 `space` 로 보낸다" 는 뜻은 아니다 — 둘은 **컴포넌트가 소유하는 광학 보정**이지
레이아웃 여백이 아니다. 값 스케일이 아니라 **허용 컴포넌트·속성·사유가 고정된 예외 목록**
으로 관리한다.

나머지 역할 중복 수렴안.

| 현재 | author | 대표 관계 | → | 방향 |
|---:|---:|---|---:|---|
| `15` | 1,430 | `card>expo-rows` 182 · `card>row` 150 | `12` | 줄임(안전) |
| `14` | 1,951 | `category-list>category-row` 192 · `content>card` 142 | `12` | 줄임(안전) |
| `13` | 422 | `edit-form-row>edit-form-box` 116 | `12` | 줄임(안전) |
| `22` | — | 앱 전용 · `gap` 6자리 **전부 가로** | `24` | 늘림 · 가로 → `S3b` (`PRT-210` 에 채움) |
| `18` | — | 앱 전용 · `marginTop` 14 중심 (세로 23 · 가로 4) | `16` | 줄임(안전) → `S3a` (`PRT-210` 에 채움) |
| `11` | 875 | `content>card` `mb` 316 · `div>stock-option-choice` 336 | `12` | 늘림 — **세로·좌우 혼재이므로 변별로 나눠 확인 필요** → **변별 완료**(`PRT-210`): 앱 78 세로 · 8 가로 |
| `10` | 1,276 | `expo-rows>expo-row` `gap` 307 | `8` | 줄임(안전) |
| `9` | 356 | `overlay>sheet` 162 | `8` | 줄임(안전) |
| `7` | 868 | `expo-pick-title>badge` `pr/pl` 156 · `expo-card-top>expo-status` 76 | `8` | **늘림 · 가로** → `P2` 후 재측정 필요 |
| `5` | 464 | `expo-card-top>expo-category` 76 | `4` | 줄임(안전) |

`104px`(`phone>content` `pb`) · `44px`(`callout` `pl`) · `40px`(빈 상태)은 단일 역할의
고유값이고 컴포넌트 층이 소유한다.

**정정(`PRT-210`).** `18` 과 `22` 는 프로토타입 매핑표 `converge.space` 에 목적지가
있는데도 이 표에 행이 없었다 — `PRT-206` 이 "근거 없는 기존 결정 주장" 으로 잡은 자리다.
두 행을 채웠다. author 관측 수를 적지 못한 것은 **프로토타입에 그 값이 없기 때문**이다
(앱에만 있다). 없는 수를 지어내지 않고 `—` 로 둔다.

**결과 — author 고유값 25 → `space` 6단계(4·8·12·16·20·24) + 컴포넌트 고유값 3개.**

**정정(`PRT-206`).** 이전 판은 `32` 를 넣어 7단계라고 적었다. `tokens.ts` 의 `space` 는
**4~24 여섯 단계**이고 `32` 는 없다. `PRT-196` 이 매핑표에서 `space.32` 를 빼면서
§7.1 에 그 사실을 적었는데 **이 줄은 고치지 않았다** — §4.9 의 `radius.sm=6` 과 같은
종류의 잔여다. `PRT-248`은 이를 범용 스케일 확장 대신 역할별 `COMPONENT.emptyState.*` 형상으로 분류해 현행 값을 보존한다.

### 4.8 행간 — 새로 드러난 축

초판은 이 축을 아예 다루지 않았고, 그래서 "미매핑 0" 이라고 말할 자격이 없었다.

**타이포 관측의 84.7%(5,908회)가 `line-height: normal`** 이다. 선언이 없다는 뜻이다.
`normal` 은 글꼴 메트릭에 따라 달라지고 **웹과 React Native 의 결과가 같지 않다.**
게다가 같은 역할이 두 값을 갖는다.

| 역할 | `normal` | 명시 선언 |
|---|---:|---|
| `16px/700` | 1,648 | `21.6px`(=1.35) 491 · `22px` 1 |
| `13px/600` | 561 | `18.2px`(=1.4) 170 |
| `14px/600` | 362 | `19.6px`(=1.4) 127 · `20px` 30 |
| `19px/700` | 90 | `26.6px`(=1.4) 21 |
| `12px/700` | 48 | `12px`(=1.0) 58 |

명시 선언은 **1.35~1.4 에 몰려 있고 뱃지만 1.0** 이다.

```
lineHeight   tight 1.0 · default 1.4 · relaxed 1.5
letterSpacing  none 0 · titleTight −0.3px
```

`relaxed 1.5` 는 가이드가 요구하는 긴 본문용이다. 1.35 와 1.4 를 나눌지는 → **[`D-8` 대기]**

### 4.9 반경 — 20값 → 5단계 + 고유값 1

| 형태 | 현재 값 | → |
|---|---|---|
| 뱃지 · 상태 | `5` `6` `7` | `radius.sm = 8` |
| 버튼 · Field · 작은 표면 | `8` `9` `10` `11` `12` `13` | `radius.md = 12` |
| 카드 | `14` `16` | `radius.lg = 16` |
| 시트 상단 | `17` `18` `20` `22` | `radius.xl = 20` |
| pill · 원 | `28` `999` `50%` | `radius.full` / `radius.circle` |
| 핸들 · 밑줄 · 트랙 | `2` `3` `4` `15` | `radius.full` |

반경은 폭에 영향이 없으므로 자유다. `radius` 토큰은 `tokens.ts` 에 있으나 앱에서 **0회**
쓰인다 — 값 수렴만으로 이 0 이 1 이 되지 않는다(§7).

**정정 두 건(`PRT-206`).**
① `radius.sm` 을 **6 → 8** 로 고쳤다. `tokens.ts` 의 `sm` 은 8 이고, `PRT-196` 에서
매핑표를 정본 값으로 다시 잡으면서 "뱃지 5·6·7 → `radius.sm=8`" 이라고 §7.1 에 적어
놓고도 **이 표와 §6.4 뱃지 계약은 6 인 채로 남겨 두었다.** 두 문서가 다른 값을 들고
있었다 — 페이블이 앱 매핑표와 대조하다 짚었다. **코드가 정본이므로 8 이다.**
② 핸들 `3`·`4` 를 컴포넌트 고유값에서 **`radius.full`** 로 옮겼다. 넷 다 "짧은 변의
절반" 이라 완전한 알약이 되는 값이고, 같은 표가 이미 `28`·`999`·`50%` 를 `radius.full`
로 보내고 있었다. 페이블 — **"`3` 은 의도를 말하지 않고 `radius.full` 은 알약이라고
말한다."** 앱에만 있는 `2`(활성 탭 밑줄)·`15`(스위치 트랙)도 같은 형태라 함께 넣었다.

### 4.10 그림자 — 4역할이되 가이드 4종과 하나가 어긋난다

| 실측 | 관측 | 역할 |
|---|---:|---|
| `0 1px 3px rgba(0,0,0,.04)` | 366 | `shadow.card` ✅ 가이드에 있음 |
| `0 18px 50px rgba(25,31,40,.24)` / `.22` | 20 / 1 | `shadow.sheet` ✅ **2 → 1** |
| `0 7px 20px rgba(49,130,246,.34)` | 16 | `shadow.fab` ✅ |
| `0 -8px 30px rgba(25,31,40,.12)` | 1 | **`bottomBar` — 가이드 §1.5 의 4종에 없다** |
| `0 10px 28px rgba(25,31,40,.18)` | 1 | 중복 → `sheet` 로 흡수 |

초판은 "가이드가 이미 4종을 계약했으니 새로 만들 것이 없다" 고 썼는데 **틀렸다.**
가이드의 4종은 `card · sheet · fab · sliderThumb` 이고 `bottomBar` 는 그중에 없다.
게다가 `sliderThumb` 은 이번 측정에서 **한 번도 관측되지 않았다.**

- `bottomBar` 1회 — 제거할지, `sheet` 에 흡수할지, 새 역할로 승인할지 → **[`D-9` 대기]**
- ~~`sliderThumb` — 계약에는 있는데 프로토타입에 없다. 미사용 토큰인지 미구현인지 확인 필요.~~
  **앱에 있다(`PRT-207`).** `apps/mobile/src/components/kit/Slider.tsx:50` 의 손잡이가
  그 그림자다. **미사용 토큰이 아니라 프로토타입 미구현이었다.**

**둘 중 하나를 정하기 전까지 그림자도 미매핑 0 이 아니다.**

**앱 자료가 `D-9` 의 절반을 답했다(`PRT-207`).** 앱 감사에서 `shadowColor: '#000'` 세 자리를
찾았는데(§7.3 `R-CL-SHADOW`), 그 셋이 바로 이 표의 열린 항목들이다.

| 앱 자리 | 값 | 이 표의 어느 행인가 |
|---|---|---|
| `Slider.tsx:50` | 손잡이 그림자 | **`sliderThumb`** — "관측되지 않았다" 가 프로토타입 한정이었다 |
| `Sheet.tsx:30` | `offset 0,-8` · `opacity .15` · `radius 40` | **`bottomBar`**(`0 -8px 30px`)의 RN 형태 |
| `MyNotificationsScreen.tsx:34` | 스위치 노브 | 표에 없는 네 번째 역할 |

페이블 지적 — "`D-9` 의 두 질문이 앱 자료로 절반 답해졌으니 `D-9` 와 `D-11` 그림자 항목을
한 봉투로 묶어라." 그렇게 한다. 소유자에게 가는 질문은 **"그림자 역할을 몇 개로 둘 것인가"**
하나이고, `bottomBar` 존치·`sliderThumb` 구현·노브 역할 신설이 그 안의 선택지다.
`tokens.ts` 에는 `shadow.card` 하나뿐이므로 어느 답이든 정본 확장(`D-11`)을 거친다.

### 4.11 측정하지 않은 축은 만들지 않는다 — 다만 없다고도 말하지 않는다

모션(duration·easing)과 z-index 는 P1a/P1b 측정에 없었다. 값을 지어내지 않는다.

**그러나 초판은 여기서 한 걸음 더 나갔다** — 이 두 축을 비워 둔 채 "모든 값 미매핑 0" 이라고
적었다. 실제 소스에는 `z-index` 와 `slide`·`fade` 모션이 실재한다. **비어 있는 칸은
채우라는 신호를 주지만, 지어낸 값은 정본처럼 읽힌다** — 비워 두는 것은 맞고,
그 상태로 "완료" 라고 말한 것이 틀렸다. §7 의 범위 분할로 처리한다.

**P1c 실측(`PRT-256`).** 앱 178개 TS/TSX 파일을 AST로 훑어 6개를 보존했다.
`Modal animationType="fade"` 4개는 화면에 직접 만든 하단 시트,
`animationType="slide"` 1개는 공용 `Sheet`, `zIndex:30` 1개는 공용 `FAB`다.
직접 선언한 duration/easing과 Animated·Reanimated 호출은 0개다. 따라서 React Native preset의
내부 시간값을 추정해 primitive로 만들지 않고, 현재 값은
`COMPONENT.inlineSheet.animationType`·`COMPONENT.sheet.animationType`·`COMPONENT.fab.zIndex`
세 역할이 소유한다. 같은 역할의 두 번째 구현이나 사용자 지정 duration/easing이 생기면 중앙 토큰을
다시 검토한다. 원시 선언·파일·행·열·scope 해시는
`full-page-flow-prototype-motion-layer-audit.json`이 보존한다.

---

## 5. 의미(semantic) 층

값은 여기에 적지 않는다. 적으면 두 번째 정본이 된다.

### 5.1 표면 · 선 · 글자

| 의미 토큰 | 기본 참조 | 사용 규칙 |
|---|---|---|
| `surface.app` | `color.gray.100` | 화면 바탕 |
| `surface.default` | `color.white` | Card · Field · Layer |
| `surface.subtle` | `color.gray.50` | 중립 결과 · 요약 · 비활성 행동 |
| `surface.scrim` | `color.scrim` | Layer 뒤 |
| `line.outer` | `color.gray.200` | Card 외곽 · Field 경계 |
| `line.inner` | `color.gray.100` | 연속 Row · LayerFooter 상단 |
| `line.strong` | `color.gray.300` | 결과 카드 · 강조 경계 |
| `text.primary` | `color.ink.900` | 제목 · 핵심값 · 유형명 |
| `text.secondary` | `color.ink.700` | 읽어야 하는 본문 |
| `text.tertiary` | `color.ink.500` | 라벨 · 설명 · 맥락 |
| `text.disabled` | `color.ink.300` | placeholder · 날짜 · 비핵심 아이콘 |
| `text.onColor` | `color.white` | 채움 버튼 · FAB 위 글자 |

### 5.2 상태

| 의미 토큰 | 기본 참조 | 사용 규칙 |
|---|---|---|
| `accent.primary` / `.tint` / `.line` | `color.blue.*` | 대표 행동 · 명시적 링크 · 정보 Notice |
| `status.danger` / `.tint` / `.line` | `color.red.*` | 음수 · 삭제 · 철회 · 목표 미달 |
| `status.success` / `.tint` | `color.green.*` | 완료 · 정상 |
| `status.warning` / `.tint` | `color.amber.*` | 확인이 필요한 상태 |

실행서 §3.1 의 잠금을 승계한다 — 유형명은 상태색이 아니라 `text.primary`,
변화량만 의미색. 상태를 색만으로 구분하지 않는다.

### 5.3 타이포 역할 — 가이드의 7역할 그대로

| 의미 토큰 | 크기 / 굵기 / 행간 | 적용 | 흡수하는 실측 |
|---|---|---|---|
| `type.display` | `22 / 800 / 1.4` | 화면당 가장 중요한 값 | `22/700` 3회 |
| `type.title` | `20 / 800 / 1.4` | 화면 · **Sheet · Dialog 제목** | `19/700` 110회 · `19/800` 1 · `20/700` 6 |
| `type.header` | `18 / 700 / 1.4` | 앱 헤더 · Section 제목 | `18/800` 16회 |
| `type.body` | `16 / 700 / 1.4` | 행 제목 · 강조 본문 · 핵심 입력값 | `15px` 버튼 · 입력 34회 |
| `type.bodyWeak` | `16 / 600 / 1.5` | 일반 본문 · 긴 문장 | |
| `type.caption` | `14 / 600 / 1.4` | FieldLabel · 설명 · 단위 · **행 라벨·값** | `15px` 176회 |
| `type.captionSm` | `13 / 600 / 1.0` | 날짜 · Filter · **Badge · Chip** | `12px` 241회 · `10px` 5 · `11px` 3 |

`captionSm` 이 뱃지와 날짜·필터를 함께 맡는다는 것이 가이드의 기존 계약이다.
행간이 `1.0` 인 것은 뱃지가 단일 행이기 때문이고, 날짜·필터도 단일 행이다.
**긴 문장에 `captionSm` 을 쓰면 안 된다**는 규칙이 여기서 따라 나온다. → **[`D-8` 대기]**

### 5.4 간격 역할 — 값이 아니라 관계에 이름을 붙인다

| 의미 토큰 | 값 | 관계 |
|---|---:|---|
| `gap.valueUnit` | `space.4` | 제목-보조문구 · **값-보조 설명(3px 수렴)**. 좌우 인셋은 여기 넣지 않는다 — 세로 간격과 같은 역할이 아니다 |
| `gap.inline` | `space.8` | 라벨-입력 · 아이콘-텍스트 · 버튼 사이 · 인라인 gap(6px 수렴) |
| `gap.stack` | `space.12` | Card 사이 · Row 기본 상하 |
| `gap.gutter` | `space.16` | 모바일 본문 좌우. **320~359px 에서는 `space.12`** |
| `gap.section` | `space.20` | FieldGroup · Section 사이 |
| `gap.block` | `space.24` | 큰 업무 구획 · Layer 제목과 독립 본문 |
| `pad.card` | `space.12` | Card 내부 (15px 수렴) |
| `pad.row` | `space.12` | Row 상하 (14px 수렴) |
| `pad.field` | `space.12` | Field 내부 (13px 수렴) |

---

## 6. 컴포넌트(component) 층 — 글로벌 계약 포함

### 6.1 글자 확대 계약 — **상한을 두지 않는다**

초판은 하단 탭 라벨에 확대 상한(`captionSm` 고정)을 제안했다. **철회한다.**

솔과 페이블이 같은 근거를 들었다 — WCAG 1.4.4 는 조작 컨트롤과 **라벨을 포함한** 텍스트가
200%까지 커질 수 있어야 한다고 보고, WCAG2ICT 는 비웹 소프트웨어도 플랫폼 확대를 가능한
범위까지 따라야 한다고 본다. 저장소 자체도 "200%에서 제목·값·입력·행동이 사라지지 않아야
한다" 고 계약해 뒀다(가이드 §160). **아이콘은 저시력 사용자가 작은 라벨을 읽을 필요를
대체하지 않는다.** 페이블의 표현대로 "잘려서 못 읽는 것 vs 작아도 온전한 것" 은 잘못된
양자택일이고, 플랫폼은 세 번째 답을 갖고 있다 — iOS 는 탭 라벨을 접근성 크기에서 키우지
않는 대신 길게 누르면 확대 라벨을 띄우고(Large Content Viewer), RN 은 `maxFontSizeMultiplier`
가 그 상한이다. **즉 상한 자체는 선례가 있지만, 선례는 항상 보상 수단과 함께 온다.**

| 자리 | `w130t2` | 최대 넘침 | 계약안 |
|---|---:|---:|---|
| `app-tab.app-tab-label` | 182 | 39px | **2줄 허용 + 탭바 높이 동적 확장.** 상한을 둘 경우 길게 누르기 확대(Large Content Viewer 상당) 또는 활성 탭만 라벨 표시를 **보상 수단으로 함께** 제공한다 |
| `header.title` | 38 | 161px | 확대를 따라가되 **2줄까지 늘리고 헤더 높이를 늘린다.** 자르지 않는다 |
| `header.small` | 1 | 105px | **숨기지 않는다.** 제목 아래 줄로 내린다 — "확대 시 숨김" 은 WCAG 1.4.4(내용 손실 없이 200%)에 정면으로 걸린다 |
| `arrow` · `sales-qty-stepper.b` | 3 + 1 | 31px | 문자 기호는 **아이콘 축**이다. 확대를 따라가지 않는다(§6.7) |
| `expo-row-copy.strong` · `detail-list-copy.*` · `sales-material-sheet-total.span` | 각 1 | 20px | 행 보조 정보는 **줄바꿈 허용** |
| `stock-option-copy.strong` | 1 | 47px | 고유명사 — §6.3 |

**`header.title` 2줄이 161px 넘침을 흡수하는지는 아직 측정하지 않았다.**
`P2` 에서 확인한다. → **[`D-2` 대기]**

### 6.2 헤더 제목 말줄임 계약

실행서 §3.9 는 "핵심값에는 말줄임을 사용하지 않는다" 고만 정했고 제목에 대해서는 말이 없다.

- 화면 제목은 확대를 따라가되 **2줄까지 늘린다.** 헤더 높이가 늘어난다.
- 보조 문구(`.title small`)는 숨기지 않고 **제목 아래 줄로 내린다.**
- Sheet · Dialog 제목은 **말줄임하지 않는다.** 확인창 제목은 무엇을 확인하는지가 전부다.

### 6.3 번역되지 않는 고유명사 슬롯

`stock-option-copy.strong` 이 `Asia/Singapore` · `America/New_York` 에서 47px 넘친다.
타임존 · 통화 코드 · 구매처명은 **번역해도 짧아지지 않고**, 축약하면 식별 불가능해진다.

**계약** — 고유명사 슬롯은 축약하지 않고 **줄바꿈을 허용**한다.
`.stock-option-choice` · `.expo-row-copy strong` · 구매처명 슬롯에 적용한다.

### 6.4 뱃지 — 6형태를 1형태로

| 클래스 | 크기/굵기 | padding | radius | 관측 |
|---|---|---|---|---:|
| `expo-status` | `12/700` | `4 7` | `6` | 58 |
| `expo-category` | `12/700` | `5 7` | `7` | 38 |
| `badge` | `12/800` | `4 7` | `6` | 89 |
| `expo-target` | `13/700` | `4 7` | `5` | 28 |
| `detail-chip` · `detail-tag` | `13/700` · `12/700` | `5 8` | `6` | 12 |
| `profit-badge` | `10/800` | — | — | 5 |

**하나로 합친다** — `type.captionSm`(`13/600/1.0`) · padding `space.4 / space.8` ·
`radius.sm`(**8**). 가이드의 기존 Badge 계약을 그대로 쓴다(§4.5). `PRT-206` 정정 — 이전 판은 `6` 으로 적었으나 `tokens.ts` 의 `sm` 은 8 이다(§4.9).

**글로벌에서는 형태가 아니라 색만 달라야 한다.** 형태가 다르면 번역 길이에 따라 어떤
뱃지는 깨지고 어떤 뱃지는 안 깨지는 상태가 되고, 그건 재현도 예측도 안 된다.

가로 padding 이 `7 → 8` 로 **느는** 방향이므로(§3 기준) `P2` 후 글로벌 재측정에서
`atRisk` 증가 0 을 확인한다.

### 6.5 검색 결과 인라인 뱃지

검색 결과에서 뱃지가 이름 **뒤에 인라인으로** 붙는다(`expo-pick-title > badge`, 156회).
이름이 길어지면 뱃지가 밀린다. `.expo-card-top` 처럼 오른쪽 끝에 고정되는 구조와 다르다.

**계약** — 뱃지는 이름과 같은 줄에 인라인으로 두지 않는다. 오른쪽 끝 고정이거나
이름 아래 줄이거나 둘 중 하나다.

### 6.6 컨트롤 높이 — 4역할 (shell 기준 재측정 완료)

초판은 `<input>` 자신의 높이로 결정했고, 그 한계를 §9 에 적어 놓고도 §6 에서 확정했다.
솔의 지적대로 **한계를 기록한 것이 그 수치를 결정 근거로 만들어 주지는 않는다.**
`PRT-194` 에서 **감싼 조작 상자를 컨트롤로 보도록 고쳐 다시 쟀다** — `h21` 83→3,
`h20` 207→192(허용 예외인 `28×20` 순서 이동 화살표만 잔존).

| 역할 | 재측정 값 | → | 근거 |
|---|---|---:|---|
| 대표 행동 · Sheet 액션 | `48` `47` `46` | `48` | 실행서 §3.6 |
| 독립 IconButton | `44` `43` `42` | `44` | 터치 최소 |
| 보조 컨트롤 · Filter | `41` `40` `38` `36` `34` | `38` | 실행서 §3.6 |
| 인라인 스텝퍼 · 작은 토글 | `31` `30` `28` | `32` | 터치는 부모가 44px 로 확보 |

행 높이는 컨트롤이 아니다 — 실행서 §3.4 의 `92 / 76 / 60` 을 그대로 쓰고,
`73` `63` `62` `55` `56` 을 거기로 수렴한다. `104` `106` `108` 은 카드 높이로
컴포넌트가 소유한다. 정수가 아닌 높이(`66.78` `95.19` `96.19`)는 `min-height` 에 내용이
더해진 결과지 정해 둔 값이 아니므로 매핑 대상이 아니다.

**`min-height` 로 계약하고 `height` 로 고정하지 않는다.** 실행서 §3.6 이 "긴 번역은 내부
줄바꿈과 높이 확장으로 처리한다" 고 이미 정했는데, 높이를 고정하면 그 규칙을 지킬 수 없다.

"터치는 부모가 44px 로 확보한다" 는 §6.7 의 계약이고 **아직 측정으로 뒷받침되지 않았다.**
`P2` 에서 확인한다.

### 6.7 터치 영역

44px 미만이 61종 · 733회다. 상위는 `(no-card).button 28×20` 192회(카테고리 순서 이동),
`card.expense-delete 30×30` 81회, `card.row-more 34×34` 18회.

카테고리 순서 이동 `28×20` 은 실행서 §3.6 이 **명시적으로 허용한 예외**다.
나머지는 시각 크기를 유지하되 `::before` 확장으로 **터치 영역만** 44px 을 확보한다 —
이 방식이 실제로 44px 을 만드는지는 `P2` 에서 측정한다.

### 6.8 아이콘 — 3단계로 고정

같은 chevron `›` 이 `17px`(70회) · `20px`(126회) · `21px`(18회)로 갈리고,
삭제 `×` 는 `18px`(142) · `20px`(81) · `21px`(1)이다.

**`icon.sm 16` · `icon.md 20` · `icon.lg 24`.**

`KD-001`(글자 200% 에서 `.header-icon` 40×40 안의 `＋` 가 53px 이 되어 넘침)은 여기서
풀린다 — **아이콘은 글자 확대를 따라가지 않는다.** 아이콘은 고정 크기를 쓰고 확대 대상은
라벨 텍스트뿐이다. §6.1 의 `arrow`(`12px`, 10회)와 단위 기호도 같은 규칙이며,
초판이 `arrow` 를 뱃지 타이포로 흡수한 것은 이 규칙과 충돌했으므로 철회한다.

### 6.9 열거형 요약 — `sales-menu-sub`

`매장 15 · 배달 8 · 포장 2` 형식이 `+30%` 에서 30건, 최대 36.9px 모자란다.
영어로는 `Dine-in 15 · Delivery 8 · Takeout 2` 가 되어 폭이 두 배를 넘는다.
**한국어 채널명이 2글자라서 성립하던 형식이다.**

**계약** — 셋 중 하나를 고른다.
- (가) 줄바꿈 허용 + 행 높이 확장. 가장 안전하고 세로 공간을 쓴다.
- (나) 채널명을 아이콘으로 바꾸고 숫자만 남긴다. 폭이 확실히 줄지만 아이콘 학습이 필요하다.
- (다) 상위 2개만 보이고 나머지는 `+n` 으로 접는다. 정보가 준다.

솔라 권고는 (가)다 — 세로는 §3 기준상 안전한 방향이고, 이 자리는 목록 행이라
높이가 늘어도 다른 것을 밀지 않는다. → **[`D-2` 대기]**

### 6.10 문단 여백 — `callout` · `confirm-copy` · `p`

`callout` 3건(≤3.1px) · `confirm-copy` 2건(≤6.2px)은 줄바꿈이 이미 되는 문단인데도
모자란다. **줄바꿈이 되는데 넘친다는 것은 좌우 여백이 부족하다는 뜻이다.**
`callout` 은 좌측 `44px` 아이콘 들여쓰기를 갖고 있어(§4.7) 320px 에서 본문 폭이
특히 좁다.

**계약** — 문단은 좌우 여백을 `gap.gutter` 로 두되 320~359px 에서는 `space.12` 로 줄인다
(실행서 §3.3 이 이미 정한 규칙). `callout` 의 아이콘 들여쓰기는 좁은 화면에서
아이콘을 첫 줄 위로 올려 본문 폭을 회복한다.

---

## 7. 범위 — `P1` 을 셋으로 나눈다

초판은 "프로토타입과 앱의 모든 현재 값" 을 매핑한다고 선언했지만, 실제 산출물은 그 범위를
받치지 못한다. `token-adoption-audit` 은 앱 인라인 선언을 **파일별 개수로만** 저장하고
property/value/location 을 내지 않는다. 실제로 앱 탭 레이아웃에는 이 문서 어느 표에도 없는
`12.5px` 이 있다. 모션·z-index 도 소스에 실재한다.

| 단계 | 범위 | 완료 조건 | 판정 산출물 |
|---|---|---|---|
| **`P1a`** | 프로토타입 8축 값 매핑 | 미매핑 0건 · 매핑표 오류 0 · 낡은 항목 0 · **열린 결정이 붙든 축 0개** | `token-map-check.json` — **부분 충족**(§7.1) |
| **`P1b`** | 앱 값 매핑 | `S3c` 적용 커밋 `dbb89c1`에서 토큰 정의 21건을 별도 `definitions`로 보존하고 사용처 **2,345 선언**을 전수 재측정했다(앱 범위 해시 `5842e326…41aea`). 65개 규칙으로 미분류 0, defect 0, `pendingApproval` 32건이다. Android 정확 증거는 확보했고 iOS 네이티브 증거가 남아 W1은 `REOPEN`이다 | `full-page-flow-prototype-app-map-check.json` |
| **`P1c`** | 모션 · 레이어(z-index) 축 | 앱 178파일 AST 전수 · 선언 6(모션 5/레이어 1) · 세 컴포넌트 역할 배정 · 미매핑 0 · duration/easing 추정 0 | `full-page-flow-prototype-motion-layer-audit.json` — **충족** (`PRT-256`) |

### 7.1 "미매핑 0" 은 "확정 0" 이 아니다

검사기가 미매핑 0 을 내지만 **그것만으로 `P1a` 가 끝났다고 말할 수 없다.**
열린 결정(§8)의 값을 잠정 `primitive` 로 넣어 두는 것만으로 미매핑은 0 이 되기 때문이다.
그래서 검사기가 **열린 결정이 어느 축을 붙들고 있는지** 함께 낸다.

검사기가 세 가지를 갈라 낸다.

| 상태 | 축 | 왜 |
|---|---|---|
| **확정** | `space` · `radius` · `typeSize` | 목적지가 전부 `tokens.ts` 에 실재하고 열린 결정도 없다 |
| **잠정** — 열린 결정이 값을 붙들고 있다 | `color`(`D-1`) · `typeWeight`(`D-7`) · `lineHeight`(`D-8`) · `shadow`(`D-9`) · `icon`·`control`(`D-11`) | 결정이 닫혀야 값이 정해진다 |
| **정본 확장 필요** — `tokens.ts` 에 없는 값 | `lineHeight` · `letterSpacing` · `iconSize` · `controlHeight` · `shadow` | 권위 변경이므로 승인이 선행(`D-11`) |

**`P1a` 는 확정 축 셋에 대해서만 충족이다.**

2판 초고는 `space.32` · `radius.sm=6` · `radius.circle` 을 `primitive` 에 적었는데,
`tokens.ts` 는 `space` 4~24 이고 `radius.sm` 은 **8**이며 `circle` 이 없다.
**이 문서가 하지 않겠다고 한 바로 그 일 — 두 번째 경쟁 표준 만들기 — 을 하고 있었다.**
매핑표를 `primitive`(코드에 실재) 와 `proposedTokensExtension`(없는 값, 승인 필요)으로
갈랐고, 반경 목적지는 전부 `tokens.ts` 값으로 다시 잡았다(뱃지 5·6·7 → `radius.sm=8`).
`radius.circle` 은 형태이지 스케일이 아니므로 `componentOwned` 로 내렸다.

**앱 쪽 선언 인벤토리(최신 제품 재측정).** `dbb89c1`에서 2,345 선언을 전수 보존했다.
`S3c`가 하드코딩 간격 5건과 그림자 묶음 3건을 정본 참조로 치환해 실행 우주에서 8건이 빠졌고,
앱 입력 범위 해시는 `5842e326…41aea`다.

| 그룹 | 선언 | 고유 | 공용 컴포넌트 | 제품 화면 |
|---|---:|---:|---:|---:|
| 간격 | 632 | **76** | 35 | 597 |
| 타이포 | 1,401 | 24 | 83 | 1,318 |
| 크기 | 201 | 51 | 22 | 179 |
| 반경 | 94 | 7 | 10 | 84 |
| 색(hex) | 25 | 12 | 8 | 17 |

S2·S3a·S4·S3b 실행으로 간격 고유 선언은 164종에서 **76종**, 반경은 20종에서 **7종**으로
줄었다. 팔레트 밖 범주색(`#7C5CE0`·`#F0EDFB`·`#179E6B`)도 S2의 역할 토큰으로 이동했다.
남은 리터럴은 아래 W1 규칙표에서 primitive·componentOwned·defect·pendingApproval로 전수 배정한다.

**하나의 공식 문서를 유지하되 완료 게이트는 세 범위로 분리한다.**
P2는 선행 배정 뒤 S1→S2→S3a→S4→S3b 순서로 실행했고, 최종 제품 SHA에서 W1을 다시
측정해 배정표를 갱신했다. iOS 네이티브 증거가 들어오기 전까지 W1과 S4a의 최종 종결만 보류한다.

| 단계 | 완료 조건 | 판정 산출물 |
|---|---|---|
| `P2` | **`P1b` 완료 후 시작.** 치환 후 승인된 변화만 존재하고 비의도 변화 0. `base` 잘림 0 유지 · `atRisk` **신규 0 · 유지 항목 악화 0**(개수가 아니라 ID·크기 대조) | `render-audit` · `i18n-stress` |
| `P3` | `KD-001` · `KD-002` 가 `known.json` 에서 사라진다 | 게이트가 낡은 예외를 강제로 지운다 |
| `P4` | 시범 3화면 제품층 인라인 typography · spacing · radius **0건** | `token-adoption-audit.json` |

완료 판정은 **토큰 종류 수가 아니라 제품층 인라인 건수**다. 제품 화면 170개가 `T.` 를
1,667회 직접 참조하면서 크기·간격은 인라인으로 박는다(타이포 1,461 · 간격 1,558).
공용 컴포넌트 14개는 각 94건이다. **비율이 아니라 자릿수가 다르다.**

### 7.3 `P1b` 배정 결과 — 최신 사용처 2,345건이 다섯 통에 들어갔다

토큰 정본 `tokens.ts`의 정의 21건(fontSize 7 · fontWeight 7 · lineHeight 7)은 별도
`definitions` 인벤토리로 보존한다. 최신 사용처 행 2,345개는 손으로 적지 않고
**규칙 65개**로 적었다(`-app-token-map.json`). `PRT-227`이 복구한 음수 숫자도 포함한다.
검사기가 규칙을 순서대로 태워 모든 선언이 정확히 한 통에 들어가는지 확인한다
(`-app-map-check.mjs` → `-app-map-check.json`). 게이트가 그 결과를 감사·매핑표 SHA 에 묶는다.

> ⚠ **이 수치는 손으로 적지 않는다** (솔 검수 `W1 R2 F01`). 아래 블록은 검사기가
> `-app-map-check.json` 에서 **생성**하고, `doc-claims` 의 `C-W1-BLOCK` 이 문서의 표식 사이와
> **글자 그대로** 대조한다. 규칙 하나의 건수만 어긋나도 FAIL 이고, 같은 통 안의 `+2/−2`
> 상쇄로도 숨을 수 없다. 고치는 법은 하나다 — 검사기가 찍어 주는 블록을 붙여 넣는 것.
>
> 초판이 이 자리를 손으로 적어 순서 영향 `1,810`(실제 929) · 체인 `12`(실제 10) ·
> `pendingApproval 46`(실제 44) · `CONTROL-BOX 82`(실제 84) · `CATEGORY-ACCENT 22`(실제 20) 을
> 그대로 달고 있었고, `doc-claims` 는 **문서를 읽지도 않아** 전부 통과시켰다.

<!-- W1-수치: 자동 생성 -->
선언 **2,345** · 미분류 **0** · 다중 일치 **774**(고유 충돌 체인 **6**개)

| 통 | 선언 |
|---|---:|
| `primitive` | **2,064** |
| `componentOwned` | **249** |
| `defect` | **0** |
| `pendingApproval` | **32** |
| `approvedException` | **0** |

| 규칙 | 통 | 선언 |
|---|---|---:|
| `R-TY-SIZE-PRIM` | primitive | 705 |
| `R-TY-WEIGHT-PRIM` | primitive | 662 |
| `R-SP-PRIM` | primitive | 581 |
| `R-RD-PRIM` | primitive | 92 |
| `R-SZ-FLEX-RESET` | componentOwned | 65 |
| `R-SZ-APPHEADER-ACTION` | componentOwned | 24 |
| `R-SZ-FIXED-COL` | componentOwned | 20 |
| `R-TY-LETTERSPACING-UNDECIDED` | pendingApproval | 15 |
| `R-CL-PRIM` | primitive | 14 |
| `R-SP-EDGE-INSET` | componentOwned | 11 |
| `R-SZ-HAIRLINE` | componentOwned | 10 |
| `R-SZ-HANDLE` | componentOwned | 10 |
| `R-TY-LETTERSPACING-TITLE-TIGHT` | primitive | 10 |
| `R-TY-SIZE-OFFSCALE` | pendingApproval | 9 |
| `R-SZ-RECIPE-EDITOR-TOUCH` | componentOwned | 8 |
| `R-CL-CHART` | pendingApproval | 6 |
| `R-SP-OPTICAL-ICON` | componentOwned | 6 |
| `R-SZ-CONTROL-LEGEND` | componentOwned | 6 |
| `R-SZ-MINWIDTH` | componentOwned | 6 |
| `R-SP-RESET` | componentOwned | 5 |
| `R-SP-FORM-AUXILIARY-OVERLAP` | componentOwned | 4 |
| `R-SZ-CONTROL-QUANTITY` | componentOwned | 4 |
| `R-SZ-CONTROL-RADIO` | componentOwned | 4 |
| `R-SZ-CONTROL-ROW-OVERFLOW` | componentOwned | 3 |
| `R-SZ-ROW-CONDITION` | componentOwned | 3 |
| `R-SZ-ROW-PROFIT-METRIC` | componentOwned | 3 |
| `R-CL-NEAR-PALETTE` | pendingApproval | 2 |
| `R-RD-LEGEND-DOT` | componentOwned | 2 |
| `R-SP-EMPTY-COMPACT` | componentOwned | 2 |
| `R-SP-EMPTY-SHEET` | componentOwned | 2 |
| `R-SP-EMPTY-STANDARD-H` | componentOwned | 2 |
| `R-SP-EMPTY-STANDARD-V` | componentOwned | 2 |
| `R-SP-QUANTITY-TOUCH-ENVELOPE` | componentOwned | 2 |
| `R-SZ-APPHEADER-RIGHT-TOUCH` | componentOwned | 2 |
| `R-SZ-CATEGORY-REORDER-TOUCH` | componentOwned | 2 |
| `R-SZ-CONTROL-ACCOUNT-ICON` | componentOwned | 2 |
| `R-SZ-CONTROL-CHANGE-ICON` | componentOwned | 2 |
| `R-SZ-CONTROL-SLIDER` | componentOwned | 2 |
| `R-SZ-CONTROL-STORE-ICON` | componentOwned | 2 |
| `R-SZ-CONTROL-SWITCH-THUMB` | componentOwned | 2 |
| `R-SZ-CONTROL-SWITCH-TRACK` | componentOwned | 2 |
| `R-SZ-CONTROL-TAX-REMOVE` | componentOwned | 2 |
| `R-SZ-ROW-PROFIT-ACTION` | componentOwned | 2 |
| `R-SZ-ROW-SALES-METRIC` | componentOwned | 2 |
| `R-SZ-ROW-STOCK-ACTION` | componentOwned | 2 |
| `R-SZ-SALES-QUANTITY-ACTION` | componentOwned | 2 |
| `R-SP-DIVIDER-INSET` | componentOwned | 1 |
| `R-SP-EMPTY-DAY-DETAIL` | componentOwned | 1 |
| `R-SP-GAP-CONTROL-CLUSTER` | componentOwned | 1 |
| `R-SP-GRID-GUTTER` | componentOwned | 1 |
| `R-SP-PROFIT-SECTION-LABEL-OVERLAP` | componentOwned | 1 |
| `R-SP-ROW-OVERFLOW-TOUCH-BOX` | componentOwned | 1 |
| `R-SP-SESSION-GATE-H` | componentOwned | 1 |
| `R-SP-TABBAR-INSET` | componentOwned | 1 |
| `R-SP-TIGHT-CAPTION` | componentOwned | 1 |
| `R-SP-UNIT-NUDGE` | componentOwned | 1 |
| `R-SZ-BUTTON-SM-MINHEIGHT` | componentOwned | 1 |
| `R-SZ-ORDER-DETAIL-TOUCH` | componentOwned | 1 |
| `R-SZ-ROW-APPHEADER` | componentOwned | 1 |
| `R-SZ-ROW-MEMO-INPUT` | componentOwned | 1 |
| `R-SZ-ROW-SALES-BANNER` | componentOwned | 1 |
| `R-SZ-ROW-SALES-OPTION` | componentOwned | 1 |
| `R-SZ-SALES-ACTION-ROW` | componentOwned | 1 |
| `R-SZ-SALES-MANAGE-ACTION` | componentOwned | 1 |
| `R-SZ-SALES-SALE-ACTION` | componentOwned | 1 |

| 충돌 체인 | 선언 |
|---|---:|
| `R-TY-SIZE-PRIM > R-TY-SIZE-OFFSCALE` | 705 |
| `R-SZ-FLEX-RESET > R-SZ-MINWIDTH` | 65 |
| `R-SZ-APPHEADER-RIGHT-TOUCH > R-SZ-MINWIDTH` | 1 |
| `R-SZ-RECIPE-EDITOR-TOUCH > R-SZ-MINWIDTH` | 1 |
| `R-SP-QUANTITY-TOUCH-ENVELOPE > R-SP-PRIM` | 1 |
| `R-SZ-SALES-QUANTITY-ACTION > R-SZ-MINWIDTH` | 1 |
<!-- /W1-수치 -->

**`semantic` 통을 없앴다** (솔 검수 `W1 R1 F02`). 초판은 "지금은 비어 있고 `P4` 치환 뒤에
채워진다" 고 적었는데 **그 기전이 성립하지 않는다.** 감사기는 숫자 리터럴만 선언으로 모으므로
`fontSize: 16` 을 `TYPE.body` 로 바꾸면 그 선언은 `semantic` 으로 **이동하는 것이 아니라
입력에서 사라진다.** 영원히 0 인 통을 두고 "아직 안 채워졌다" 고 적는 것은 분류가 아니라
변명이다. `W1` 의 범위를 정직하게 **하드코딩 리터럴의 배정**으로 좁히고, 의미 토큰 채택률은
같은 감사기가 이미 모으는 `tokenAccess` 로 따로 본다. `P1b` 완료 조건은
**"다섯 통 합 = 2,345 · 정의 21은 별도 · 미분류 0 · `approvedException` 0"** 이다.

**규칙 순서가 배정을 바꾼다 — 774건이 순서에 걸려 있다.** 규칙은 순서대로 평가하고
첫 일치가 이기는데, 한 선언이 규칙 둘 이상에 걸리면 그 배정의 근거는 규칙이 아니라
**배치**다. 검사기가 다중 일치 수를 세고, 순서에 의존하는 쌍의 **앞선 규칙에 `순서근거` 가
없으면 FAIL** 한다. 최종 측정은 **774건**, **고유 충돌 체인 6개**다(규칙 67개 기준) —
`R-TY-SIZE-PRIM > R-TY-SIZE-OFFSCALE` 705 가 가장 크다. 순서를 바꾸면 그 705건이
`primitive` 에서 `pendingApproval` 로 옮겨 간다. **"순서는 장식" 이라고 가정했으면 틀렸을 자리다.**

`PRT-214` 에서 이 검사를 조였다 — 승자 규칙은 자기가 이기는 규칙을 `precedes` 에 **정확한
id 로** 적어야 하고, 검사기가 실제 충돌과 **양방향**으로 대조하며, `순서근거` 문장이 그 id 를
**이름으로 대지 않으면 FAIL** 한다. 아무 문장이나 적혀 있으면 통과하던 검사였다(솔 검수 `F04`).
표본 상한 50건도 없앴다 — 열 체인이 전부 산출물에 남는다.

**`defect`의 축 방향은 더 이상 수기 서술을 믿지 않는다**(솔 검수 `W1 R2 F03`).
축이 있는 모든 defect는 `targetValue`·선언별 `targetMap`·프로토타입 수렴표
`protoConverge`·폐쇄된 런타임 파생식 `targetDerived` 중 정확히 하나를 가져야 한다.
검사기가 감사 산출물의 현재값과 목적지로 선언별 `shrink/same/grow`를 계산하고, 한 규칙에
여러 방향이 있으면 `mixed`로 만든다. 작성자가 적은 `delta`와 다르면 FAIL한다.
`PRT-220`에서는 이미 닫힌 D-11의 lineHeight·control·row 사용처 172건을 승인 대기로 돌리는
우회를 폐기했다. 선언별 숫자 목적지는 defect로, 값 자체가 컴포넌트 형상인 자리는 이름 있는
componentOwned로 배정한다. `pendingApprovalContract`가 아래 네 규칙 외 재진입을 막는다.

#### 7.3.1 사람이 정해야 하는 것 — `pendingApproval` 32건

새 제품 결정 없이는 목적지를 정할 수 없는 네 규칙만 남긴다. 검수 의견의 `32건`은 이전
측정치(OFFSCALE 11 · CHART 2)를 합친 값이었다. 최종 `d4fba6b` 감사에서는 OFFSCALE 9 · CHART 6이다.
음수 자간 25건 중 승인된 `titleTight -0.3` 10건은 primitive로, 나머지 15건은 역할별 토큰 추가
또는 수렴 결정이 필요해 이 통으로 들어왔다. `#B0B8C1` 1건은 `T.gray400`과 exact라
`PRT-249`에서 primitive로 정정했다. 실제 합계는 **32건**이다.
수치를 맞추려고 현행 선언을 누락하지 않는다.

| ID | 질문 | 선언 | 성격 |
|---|---|---:|---|
| `R-TY-SIZE-OFFSCALE` | 13.5 · 14.5 · 17 · 25 를 어디로 | **9** | 스케일 밖 |
| `R-CL-CHART` | 차트 색이 팔레트엔 2단계, 앱은 **4단계**. 게다가 chart.1·chart.2 가 `tokens.ts` 에 없다 | **6** | 팔레트 |
| `R-CL-NEAR-PALETTE` | 정본에 없는 근접색 둘(`#D5DAE0` · `#FFF9F0`) | **2** | `D-1′` |
| `R-TY-LETTERSPACING-UNDECIDED` | `-0.6/-0.5/-0.4/-0.2`를 역할별 토큰으로 추가할지 `titleTight -0.3`으로 수렴할지 | **15** | 타이포 역할 |

#### 7.3.1a `PRT-248`에서 분류로 닫은 15건

새 값이나 새 팔레트를 정하지 않고 소스의 직접 관계로 닫을 수 있는 두 묶음을 먼저 분리했다.

| 규칙 | 닫은 판단 | 근거 | 영향 |
|---|---|---|---|
| `R-SP-CAPTION-GAP-VIEW` 5건 | `space.xs=4` defect · `S3c` 세로 증가 | 다섯 `marginTop`은 모두 앞 콘텐츠와 다음 값·안내·행동 블록 사이의 간격이다. 안내 행 안 아이콘↔문구는 별도 `gap`이 이미 소유한다 | `PRT-250`에서 2→4px 적용 |
| `R-SP-EMPTY-STATE` 10건 | 역할별 `COMPONENT.emptyState.*` 및 `COMPONENT.sessionGate.*` | 48/32 표준형, 40 목록형, 28 시트형, 72 매출 상세형, 세션 셸 32를 서로 다른 형상으로 확인했다. 범용 `space`를 24 위로 늘리지 않는다 | 현행 값 보존 · 수렴 후보 |

#### 7.3.1b `PRT-248` 페이블 독립 검수 후속

페이블은 `bdc5aa7`을 별도 clean worktree에서 재현해 PASS로 판정했다. 다섯 간격의 역할 판독,
빈 상태·세션 형상의 componentOwned 분리, 당시 승인 대기 33건의 소유권, Android 증거 유지가 모두
타당하다고 확인했다. 비차단 조건 둘은 다음처럼 반영한다.

- 이미 종결된 `S3a`를 다시 가리키지 않는다. 새 defect 5건과 그림자 3건은
  **`S3c`(W1 잔여 defect 8건)** 한 단위로 실행했다. `design-token-s3c-known.json`의
  기준선 `4a0debd`에서 exact before→after 8건만 허용하고, 승인 밖 앱 변경·간격 방향·그림자
  bundle drift를 `design-token-s3c-diff.mjs`와 음성 시험 5건으로 막는다. 구현은
  `dbb89c1`(`PRT-250`), 재측정 결과는 선언 2,345 · defect 0 · 미분류 0이다.
- 빈 상태·세션 6규칙은 현재 형상을 보존하되 `convergenceCandidate: true`와 후속 질문을
  기계 판독 필드로 남긴다. componentOwned 분류가 수렴 기회를 숨기는 종결 표식이 아니다.

#### 7.3.1c `PRT-220`에서 닫힌 질문 3건

| 규칙 | 닫은 판단 | 근거 | 닫은 주체 |
|---|---|---|---|
| `R-SP-SECTION-LABEL-BOTTOM` 3건 | `space.xs=4` defect | 섹션 라벨↔다음 콘텐츠 관계를 기존 §간격 계약이 이미 잠갔다 | 솔라 제안 · 페이블 동의 · **솔 컨펌 2026-09-05** |
| `R-TY-SIZE-15-PROSE` 4건 | `TYPE.body=16/22` defect | 네 자리 모두 안내 문단이며 새 역할 없이 기존 body에 대응한다. 굵기는 별도 확인 결과 4/4 기본 400 | 솔라 제안 · 페이블 동의 · **솔 컨펌 2026-09-05** |
| `R-RD-LEGEND-DOT` 2건 | `COMPONENT.legend.swatch.radius` | 원으로 바꾸는 새 제품 결정을 추정하지 않고 현행 둥근 사각형을 보존한다 | 솔라 제안 · 페이블 동의 · **솔 컨펌 2026-09-05** |

#### 7.3.2 솔라 분류·대응 몫 — `PRT-210`·`PRT-220`에서 닫음

| 이전 규칙 | 갈라진 곳 | 선언 | 목적지 · 방향 |
|---|---|---:|---|
| `R-SP-CONVERGE-UNDOCUMENTED` 119 | `R-SP-CONVERGE-11-V` | 78 | `12` · `S3a`(세로) |
| | `R-SP-CONVERGE-11-H` | 8 | `12` · **`S3b`**(가로) |
| | `R-SP-CONVERGE-18` | 27 | `16` · `S3a`(줄임) |
| | `R-SP-CONVERGE-22` | 6 | `24` · **`S3b`**(전부 가로) |
| `R-SP-SCROLL-TOP` 17 | `R-SP-SCROLL-TOP` | 17 | `scrollStart` · `S4` |
| `R-SP-INLINE-TIGHT` 10 | `R-SP-GAP-LABEL-CHEVRON` | 8 | `gap.inline`(8) · **`S3b`** |
| | `R-SP-GAP-CONTROL-CLUSTER` | 2 | `gap.controlCluster` · 컴포넌트 소유 |

**셋 다 "질문" 이 아니라 "관계 확인" 이었다.**
`11` 은 §4.7 이 "세로·좌우 혼재이므로 **변별로 나눠 확인 필요**" 라고 명시적으로 열어 둔
자리였고, 축을 재서 갈랐다(세로 78 · 가로 8). `18`·`22` 는 프로토타입 매핑표에 목적지가
있는데 §4.7 이 옮겨 적기를 빠뜨린 것이라 그 두 행을 채웠다.
스크롤 상단 17자리는 **한 글자도 다르지 않은 같은 모양**이라 수렴할 것이 없었고, 없는 것은
이름이었다 — 결정 7-2 가 이름 붙인 `scrollEnd` 의 반대편이므로 `scrollStart` 로 같은 봉투에
넣었다. **값은 여기서 정하지 않는다.**
인라인 1·2px 은 관계가 **둘**이었다 — 라벨↔셰브런 8자리(§5.4 가 "아이콘-텍스트" 로 이미
계약한 관계라 새 이름을 만들어 계약을 피하지 않았다)와 아이콘 버튼끼리 2자리(텍스트가 없어
그 계약에 안 들어간다 · 헤더 우측 묶음과 순서 이동 버튼 묶음).

**`PRT-220`~`PRT-221` 역할 대응.** lineHeight 사용처 66건 중 같은 스타일 객체에 직접
fontSize 짝이 있는 65건은 확정표 12·13→18 / 14→20 / 15·16→22 / 18→24 / 20→26 /
22→28의 `targetDerived`로 검사기가 계산한다. 짝이 없는 `EmptyState` 안내문 1건은 별도
규칙으로 드러내 `TYPE.caption.lineHeight=20`에 배정했다. `tokens.ts` 정본 정의 7건은
수렴 대상이 아니므로 별도 `definitions` 인벤토리로 빠졌다. control 82건은 small 20선언→32,
medium 10선언→38, 헤더 action 22선언과 구성요소 고유값 30선언으로 갈랐다. rowMinHeight
24건은 ListRow 8건→60/76(`S4`, mixed), 나머지 16건→명명된 컴포넌트 소유값으로 갈랐다.
추가로 섹션 라벨 3건→space.xs 4, 안내 본문 4건→TYPE.body 16, 범례 반경 2건→컴포넌트
소유로 닫았다. `PRT-220`의 3,653건 통 이동과 `PRT-221`의 정의 21건 분리는 성격이 다르므로
한 대차로 합치지 않는다.

**팔레트 밖 색은 값이 아니라 역할로 갈렸다.** `#7C5CE0`+`#F0EDFB` 처럼 **본색+tint 쌍이
범주마다 하나씩** 일관되게 붙어 있는 22건은 브랜드 축 후보다 — 누군가 골랐다는 흔적이다.
반면 `#D5DAE0`은 스위치 off 트랙의 고유값이고 현 정본에는 이 값이나 `gray.300` 역할이 없다.
`#B0B8C1`은 `T.gray400`과 exact인데 근접색으로 잘못 분류했던 것이므로 `PRT-249`에서 정정했다.
`#179E6B` 은 초판에서 "팔레트에 없는 색" 으로만 적었는데, 실제로는 범주 타일 초록 쌍의
전경이었다 — `green.500` 과 나란한 세 번째 초록이 아니라 **다른 역할**이다.
**`defect` 로 분류하는 것도 "이 색을 없앤다" 는 결정이므로 솔라가 정하지 않는다.**

---

### 7.2 실행 순서 — 두 검수가 확정한 것

초판의 `S1`→`S4` 순서는 승인되지 않았다. 솔이 "폭 증가를 먼저 하면 `S4` 보상 계약이
적용되기 전에 `S3` 게이트가 실패할 가능성이 크다" 며 `S3` 를 둘로 쪼갤 것을 요구했고,
페이블이 여기에 "`S1` 도 `P1b` 뒤" 를 더했다.

| 순서 | 단계 | 내용 | 완료 게이트 |
|---|---|---|---|
| 1 | `W0` | 게이트 보정 | 새로 넣은 검사마다 **실제로 FAIL 하는 음성 시험** 한 건 |
| 2 | `W1` = `P1b` | 앱 선언 전수 배정 | 선언 수 = 다섯 통의 합 · 미분류 0 · **"승인 대기 예외" 통 0** · 입력이 결속돼 있을 것 |
| 3 | `S1` | `tokens.ts` 확장 | 정본 변경 승인(`D-11`) 후. RN 의미로 정의 |
| 3.5 | `S4a` | 접근성 — `hitSlop` 보정 | 아래 §7.2a |
| 4 | `S2` | 색 AA 수렴 | 대비 도구 통과 + **기하 diff 0** |
| 5 | `S3a` | **축소·동일 크기** 치환 | 반대 축 diff 0 · 기준선은 `S2` 완료 스냅샷 |
| 6 | `S4` | 컴포넌트 계약 | **base 상태 높이 diff 0** · 쌍 단위 줄바꿈 · 재구성 이관 |
| 7 | `S3b` | **폭 증가** 치환 | `atRisk` 신규 0 · 악화 0 + 대비 도구 재실행 |
| 8 | `S3c` | **완료** — W1 잔여 defect 8건 | `dbb89c1` · exact diff PASS · 시험 5/5 · 재측정 defect 0 |

### 7.2a `S4a` — 접근성 보정과 그 완료 조건 (`PRT-215`)

`S4a` 는 처음에는 **시각 변화 0의 `hitSlop` 보정**으로 계획했다. 그러나 네이티브 측정에서
`hitSlop`이 부모 frame에 잘려 실제 44dp가 되지 않는 소비처가 반복되면 그 계약은 성립하지 않는다.
공용 `Button size="sm"`은 5개가 넘는 서로 다른 소비 문맥에서 같은 부모 clipping을 재현했다.
소유자는 2026-09-06 시안집 별책 12에서 **12-4 "버튼 44 · 칩 줄 44"**를 확정했다. 누르는 것이
목적인 `Button sm`은 `36→44`·`hitSlop:0`, `AppHeader`·시트 아이콘 action은 투명 `44×44`
상자(아이콘 크기 유지), `ConditionRow`는 `38→44`로 한다. `FilterButton`은 시각 32를 유지하고
세로 `hitSlop:6`을 쓴다. 반대로 선택 표식인 `Chip`·`SortChip`은 기존 약 34px 형상과
`hitSlop:6`을 유지하고, 그 부모 줄만 최소 44px를 보장한다. 이처럼 소유자가 승인한 시각 frame
보정은 `S4b`로 분리하고, 그 밖의 화면 diff는 0을 요구한다.

완료 조건 —

1. `touch-target-audit` 미달 0 · `touch-target-known.json` 의 미달 목록 비움(양방향 래칫이라 비워야 통과)
2. 판정불가 목록은 그대로 래칫 유지
3. 같은 부모 안 이웃 `pressable` 과의 간격 절반을 넘는 `hitSlop` 금지 — 정적 규칙 + 음성 시험
4. 승인된 `S4b` 기하 변경 외 화면 diff 0
5. 측정은 clean checkout 에서 `--expect-commit` 으로
6. **네이티브 렌더 실측** (솔 검수 `S1 R5 F03`) — Android·iOS 에서 실제 `pressable` 과 그
   **부모 frame** 을 재어, 부모 경계로 잘린 뒤의 유효 영역이 **양축 44 이상**인지 확인하고
  이웃 `pressable` 과의 **중첩과 우선순위**까지 본다. Windows에서 실행 가능한 Android와
   iOS 실기기 개발 빌드의 증거를 따로 보존하며, 한 플랫폼의 통과를 다른 플랫폼 증거로 대체하지 않는다.

> 6 이 왜 필요한가. 감사기 자신이 "부모 경계로 잘리는지, 이웃 터치 영역과 겹치는지는 **정적
> 분석으로 못 본다**" 고 적고 있다. 3 의 `간격/2` 규칙은 **같은 부모의 일반 flow 형제**만
> 일부 덮는다. 부모 경계 · 서로 다른 부모 · `absolute` 요소 · z-order · 스크롤과 시트 경계에서는
> 선언상 44 여도 실제로 눌리는 영역이 작거나 겹칠 수 있다. **1·2·4 는 6 을 통과한 뒤에 확정한다.**

**`S3a` 와 `S3b` 의 정의 — 문구 정정 (`PRT-214`).** 기준은 **폭**이다.
`S3a` 는 **폭이 늘지 않는** 치환이고(축소 · 동일 · **세로 증가**), `S3b` 는 **폭이 느는** 치환이다.

> 초판 산문은 `S3a` 를 "줄이거나 같은 크기" 로만 적어 **세로 증가를 어디에도 넣지 않았다.**
> 그런데 같은 표의 `S3a` 완료 조건이 이미 "**반대 축** diff 0" 이라 세로 증가를 전제하고 있다.
> 새 결정이 아니라 완료 조건과 산문을 맞추는 정정이다. 규칙 24개 중 다섯이 세로 증가라
> 이 자리를 비워 두면 어느 단계에도 못 들어간다. 검사기가 이 계약을 열거값으로 강제한다 —
> `S3b` 인데 `axis` 가 세로이거나 `delta` 가 늘지 않으면 FAIL, `S3a` 인데 가로로 늘면 FAIL.
>
> **`R-SZ-ROW-MINH`는 `PRT-220`에서 `S4`로 배정했다.** 24건을 구조로 전수 확인해 ListRow
> 8건만 60/76의 선언별 `targetMap`으로 만들고, 나머지 16건은 이름 있는 컴포넌트 소유값으로
> 갈랐다. ListRow에는 55·56·70·72→60/76 증가와 82→76 축소가 함께 있어 계산 delta는
> `mixed`이며, 실제 행 밀도 변화는 S4 렌더 감사가 판정한다.

`S3b` 는 폭을 늘리는 치환이다(가로 `gap` 6→8,
타이포 12→13, 버튼 15→16). 늘어난 폭을 받아 줄 줄바꿈·최소높이 계약이 `S4` 에서 깔린
뒤에 해야 `atRisk` 가 튀지 않는다. **`S3b` 가 `S4` 뒤에 있는 이유가 그것이다.**

**`S1` 이 `P1b` 뒤인 이유.** `tokens.ts` 가 정본이므로, 앱의 간격 164종이 어느 단계를
필요로 하는지 모르는 채 스케일을 박으면 앱 매핑에서 `12.5px` 같은 값이 나올 때 정본을
다시 뜯게 된다. **프로토타입은 fixture 이고 앱이 제품이다** — 제품의 값을 다 보기 전에
정본을 확정하지 않는다.

**기준선은 단계마다 다시 뜬다.** `S3` 의 비교 대상은 초판 백업이 아니라 `S2` 완료본
스냅샷이다. 백업 브랜치는 원복용이지 기준선용이 아니다.

**`S4` 착수 조건 — 재구성 이관.** `atRisk` 키에 `hostIndex`(DOM 순서)가 들어 있으므로,
§6.5(뱃지를 이름 아래 줄로)나 §6.9(`sales-menu-sub` 쌍 묶기)처럼 DOM 을 재구성하면
형제들의 색인이 밀려 **수십 개 키가 한꺼번에 사라지고 수십 개가 새로 나타난다.**
지금 장부(`resolved`)로는 그 전부에 사유를 한 줄씩 써야 하고, 그러면 사유가 형식적으로
변한다 — 장부가 무력화되는 경로다. 그래서 `S4` 전에 **재구성 이관** 항목이 있어야 한다.
옛 키 → 새 키 대응을 한 묶음으로 등록하고, 게이트가 **대응된 쌍의 부족 폭이 악화되지
않았는지** 확인한 뒤 통과시킨다.

---

## 8. 열린 결정 — 누가 정하는가

재검수에서 **결정의 소유자**를 가려 주었다. 그 판정에 따라 넷을 닫고 셋을 남긴다.
본문에서 남은 결정에 걸린 곳은 **[`D-n` 대기]** 로 표시했다.

### 8.1 닫은 것 — 솔라가 집행한다

**`D-7` · 상속된 `bolder` 188건. → 닫는다.**
전역 `b`/`strong` 규칙을 만들지 않는다. **selector 역할별로 매핑한다.**
`<b>` 가 값인 자리(`expo-row-value b`)는 `type.body`(700), 문장 안 강조인 자리
(`expo-recipe-row b`)는 부모와 같은 굵기를 명시한다. 역할 감사 결과를 `P2` 에 싣는다.

**`D-8` · 행간. → 부분적으로 닫는다.**
`captionSm` 이 크기·굵기를 공유한다고 **행간까지 공유해야 하는 것은 아니다.**
- 뱃지의 `1.0` 은 **컴포넌트 소유**로 내린다(단일 행이 뱃지의 성질이다).
- 날짜·필터는 **기본 행간(`1.4`)** 을 쓴다.
- 남는 것은 `1.35` 와 `1.4` 를 나눌지인데, 실측 선언이 둘 다 있으나 `1.35` 는
  `16px/700` 한 역할뿐이므로 **`1.4` 로 합친다.**
값 자체(`tight`/`default`/`relaxed`)를 `tokens.ts` 에 넣는 것은 `D-11` 이다.

**`D-9` · 그림자. → 부분적으로 닫는다.**
미사용 여부와 기존 역할 중복 판정은 측정으로 결정된다.
- `0 10px 28px rgba(25,31,40,.18)` 1회 → `sheet` 와 중복이므로 **흡수**한다.
- `sliderThumb` 은 이번 측정에서 **0회** — 프로토타입에 그 부품이 없다. 미구현이다.
- `bottomBar` 1회를 **새 역할로 추가하는 것**은 정본 확장이므로 `D-11` 로 넘긴다.

**`D-10` · 단계 순서. → 닫는다.** 기술적 의존성 문제다. **`P1b` → 일반 `P2`.**
예외는 기존 권위와 무관하게 확정된 단독 정정뿐이다(`PRT-194` 의 UA 리셋이 그 예).

**`D-1` 의 절반. → 닫는다.** **프로토타입 색을 `tokens.ts` 값으로 수렴시키는 집행은
솔라가 한다.** 정본은 이미 코드다(가이드 §1.2). 아래 `D-1′` 만 남는다.

### 8.2 닫힌 것 — 제품 소유자 결정 (2026-09-05 · `PRT-208`)

**세 결정이 모두 닫혔다.** 소유자가 시안집(**11문항** · 솔 4차 검수 반영)을 보고
추천 세트를 전부 수용했다. 아래가 그 결정의 정본이며, 이후 단계는 이 표만 본다.

| 문항 | 결정 | 실행 단계 |
|---|---|---|
| 1 | **1-3** `text.tertiary = #66717E` · `text.disabled = #8B95A1` 로 **이름을 나눈다** | `S2` |
| 2 | **2-4** `brand.primary #3182F6` 유지 · `action.primary`·`text.link` `#1470F5` · `text.required` `#0A68EE` | `S2` |
| 3 | **3-1** 하단 탭 라벨 2줄 허용 + 탭바 동적 높이 | `S4` |
| 4 | **4-1** `sales-menu-sub` 줄바꿈 허용 + 행 높이 확장 | `S4` |
| 5 | **5-3** `fontSize:15` 역할별 — 라벨·값 → 14(`S3a`) · 버튼·입력 → 16(`S3b`) | `S3a`/`S3b` |
| 6 | **6-2** `TYPE` 역할마다 **절대 `lineHeight`** 저장 (RN 은 배수가 아니라 dp) | `S1` |
| 7 | **7-2** `scrollEnd` / `scrollEnd.fab` **두 역할**, 값은 좌표계별 파생 | `S4` |
| 8 | **8-2** MY 허브 타일 단색 수렴 — `component.myHubTile.*` | `S2` |
| 9 | **9-1(수정)** `tokens.ts` **6축 확장 승인** | `S1` |
| 10 | **10-1** 레시피 → 메뉴 개명. **토큰과 무관하므로 별도 `PRT`** | 범위 밖 |
| 11 | **11-3** `controlVisualHeight` 32/38 유지 · 40 은 `COMPONENT.appHeader.iconButton.visualSize` 가 소유 | `S1` |

**`D-1′` → 닫힘.** 문항 1·2 로 답했다. 소유자의 말 그대로 —
**"로고는 브랜드 식별, 버튼은 접근 가능한 상호작용이라는 역할 차이가 있습니다.
미세한 색 차이는 의도된 구분이며, 버튼·링크의 AA 통과가 더 중요합니다."**
비활성 Primary(`#D1D6DB` 위 흰 글자 1.46:1)는 열린 결정이 아니라 결함이므로 함께 고친다.

**`D-2` → 닫힘.** 문항 3·4 로 답했다. 세로 확장을 택하되 **재측정 없이 안전하다고
보지 않는다** — 둘 다 `S4` 렌더 감사 대상이고, 탭바 높이는 문항 7 의 좌표계별 공식을
따라 **이중 반영하지 않는다.**

**`D-11` → 닫힘.** 문항 9 로 답했다. 확장 축은
`lineHeight`(절대값) · `letterSpacing`(`titleTight −0.3` 만) · `iconSize` 16/20/24 ·
`controlVisualHeight` 32/38 · `minTouchTarget` 44(시각 높이와 **분리**) ·
`rowMinHeight` 60/76/92 · `shadow`(card · sheet · fab · sliderThumb · switchThumb).
**`bottomBar` 는 이름을 쓰지 않는다** — 실측상 하단 탭바에 그림자가 없고,
`Sheet.tsx:30` 의 위쪽 그림자를 그 이름으로 부르면 역할 오분류다.

**`D-9` 잔여 → 닫힘.** `sliderThumb` 은 미사용 토큰이 아니라 **프로토타입 미구현**이었다
(`Slider.tsx:50` 에 실재한다 · `PRT-207`). 그림자 확장에 포함한다.

**색 결정의 경계값 셋.** 승인된 값 중 셋이 AA 기준선에 **여유 없이** 붙어 있다 —
흰 글자 on `#1470F5` **4.50** · `#66717E` on `#F2F4F6` **4.50** ·
`#0A68EE` on `#F2F4F6` **4.51**. 지금은 통과하지만 **표면 색을 한 톤이라도 바꾸면 셋 다
떨어진다.** 소유자 판단은 "그대로 두고 경계값으로 표시" 이므로,
**표면 색(`#FFFFFF` · `#F9FAFB` · `#F2F4F6`)을 바꾸는 변경은 이 셋의 재검산을 동반한다** 를
계약으로 남긴다.

**소유자가 정하지 않은 것.** 시안집 10문항으로 닫히지 않는 승인 대기가 **146건** 남아 있다 —
출처 없는 간격 수렴 119(`R-SP-CONVERGE-UNDOCUMENTED`) · 스크롤 상단 여유 17 ·
인라인 간격 10. 이것은 소유자 결정이 아니라 **솔라가 분류를 마저 해야 하는 것**이고,
`S1` 과 병행한다.

> 초판·2판에서 폐기하거나 결론이 난 것 — `D-3`(뱃지 12px) · `D-4`(nudge 스케일) ·
> `D-5`(19px 승격) · `D-6`(모션 범위). 순서대로 §4.5 · §4.7 · §5.3 · §4.11 · §7.

### 8.2a 부록 — `text.link` 값 정정 (2026-09-05 · `PRT-210`)

**결정 2-4 의 방향은 그대로이고 값만 바뀐다.** 결정문은 `action.primary` 와 `text.link` 에
같은 `#1470F5` 를 줬는데 **의무가 다르다** — `action.primary` 는 **배경**이라 그 위 흰 글자
(4.50)와 표면 대비만 지키면 되지만, `text.link` 는 **전경 글자**라 자기가 놓이는 모든 표면에서
4.5:1 이어야 한다. `#1470F5` 는 흰 표면에서만 4.50 이고 `#F9FAFB` 4.31 · `#F2F4F6` 4.08 ·
안내 배너 tint `#EBF3FE` 4.03 으로 미달이다(`PRT-209` 의 대비 게이트가 잡았다).

| 역할 | 이전 | **정정** | `#FFFFFF` | `#F9FAFB` | `#F2F4F6` | `#EBF3FE` |
|---|---|---|---:|---:|---:|---:|
| `text.link` | `#1470F5` | **`#1465DB`** | 5.37 | 5.13 | 4.87 | 4.80 |
| `text.linkPressed` | — | **`#0E5FD6`** | 5.79 | 5.54 | 5.25 | 5.18 |
| `text.required` | `#0A68EE` | **`#1465DB`** | 5.37 | 5.13 | 4.87 | 4.80 |

**새 색이 아니다.** `#1465DB` 는 결정문이 이미 `action.onTint`·`action.primaryPressed` 로
든 값이다. **역할 이름 셋은 그대로 둔다** — 이름이 계약이고 값이 우연히 같은 것이다.
`#0A68EE` 도 뺀 이유는 `#F2F4F6` 위 4.51(경계)·tint 위 4.44(미달)이기 때문이다.

**경계값이 넷에서 둘로 줄었다.** 남는 둘은 흰 글자 on `action.primary` **4.50** 과
`text.tertiary #66717E` on `#F2F4F6` **4.50** 이고, 소유자 판단대로 "그대로 두고 경계값으로
표시" 한다. 대비 게이트가 매 커밋 절대 기준으로 재검산하고, `열린` 목록은 **0** 이다.

> 근거: 페이블 판정(`W1` 종결 회차)이고, 인용된 여덟 값을 솔라가 독립 검산해 소수점 둘째
> 자리까지 일치했다. `D-1′` 이 로고와 버튼의 역할 차이로 닫혔던 것과 **같은 모양의 정정**이다.

---

### 8.2b 부록 — 문항 11 결정 (2026-09-05 · `PRT-211` · `S1`)

**소유자 결정 `11-3`.** 승인된 `controlVisualHeight` 는 **32 · 38 두 단계 그대로**이고,
헤더의 맨 아이콘 버튼 **40 은 범용 스케일이 아니라 컴포넌트가 소유한다** —
`COMPONENT.appHeader.iconButton.visualSize = 40`. **시각 변화 0** 이다.

§8.3 ③ 이 물었던 자리다. 앱 실측이 40(12자리) · 38(5) · 34(9) 셋인데 40 과 38 은 모양이
다르다 — 40 은 **배경도 모서리도 없는 맨 아이콘 버튼**(헤더 액션), 38 은 **배경과 둥근 모서리가
있는 타일**(MY 허브 범주 · 수량 조절). 솔 판정 그대로 —

> "40 이 필요한 근거가 '헤더 맨 아이콘 버튼' 이라는 **컴포넌트 역할**이라면, 범용
> `controlVisualHeight` 에 세 번째 숫자를 넣는 것보다 컴포넌트가 소유하는 것이 3계층 원칙에
> 맞습니다 — `2-4`·`8-2` 에서 이미 같은 방식을 택했습니다."

**완료 조건을 감사기로 돌린 결과** — "40 사용처가 전부 헤더 액션인가":

| | 자리 | 판정 |
|---|---:|---|
| `AppHeader` 의 뒤로 가기 · `right=` 슬롯 | **11** | ✓ 헤더 액션 |
| `SalesHomeScreen:363` — `AppHeader` 를 쓰지 않은 **손수 만든 헤더 줄** | 1 | ✓ 역할은 같다 · 별도 정리 대상 |
| `DiscardHistoryScreen:204`(27×40) · `MyTaxScreen:241`(32×40) | 2 | **묶음에서 제외** — 정사각이 아니라 세로만 40 이다 |

**터치 영역은 아직 못 지킨다.** 12자리 중 다섯만 44 이상이고(`hitSlop` 2 또는 6),
**일곱 자리는 `hitSlop` 이 없어 40 에 머문다.** 결정 11-3 의 "44 미달분만 보정" 이 이 일곱이고
`S4` 컴포넌트 계약에서 채운다. `34 → 32` 축소는 별도 소형 컨트롤 수렴으로 처리하며
`hitSlop >= 6`(32 + 12 = 44)이 원칙이다 — **`MyVendorsScreen:129` 는 `hitSlop 4` 로 지금도 42 미달**이다.

> **솔의 `F01` 정정을 그대로 받는다.** 솔라가 보기를 만들며 "`34→32` 는 안전 · `hitSlop` 은
> 그대로" 라고 쓴 것은 **재기 전에 쓴 말**이었다. 40 자리 열두 곳의 `hitSlop` 이 2·6·없음으로
> 갈리고 34 자리 중 하나는 이미 미달이다. 이 회차에 같은 종류의 실수를 네 번째로 했다 —
> §9 6b·6c·6d 와 같은 뿌리다.

---

### 8.2c 부록 — 검수자는 소유자 확정값을 다시 계산하지 않는다 (2026-09-05 · `PRT-212` · `S1`)

**결정 6-2 의 행간 절대값 `28 / 26 / 24 / 22 / 22 / 20 / 18` 은 소유자가 확정한 값이다.**
`PRT-211` 초판이 이 값을 `round(fontSize × 1.4)` 로 다시 계산해 `31 / 28 / 25` 로 덮었다.
솔 검수 `R1 F02` 가 잡았고, 페이블은 처음에 ×1.4 재계산을 **승인**했다가 뒤집었다 —

> "솔이 맞고 제가 틀렸습니다. 검수자가 소유자 확정값을 다시 계산해 덮을 권한이 없습니다."

여기서 얻은 규칙을 결정문에 남긴다. §4.8 의 `1.4` 는 **앞 단계의 수렴 방향**이었고,
소유자 결정 6-2 는 그 뒤에 **역할별 절대값을 직접 지정했다.** 후행 소유자 결정이 선행
권고를 이긴다 —

* 검수자(솔·페이블)와 작업자(솔라) 누구도 **소유자가 확정한 값을 재계산하거나 반올림해
  덮지 않는다.** 값이 이상해 보이면 되묻는 것이지 고치는 것이 아니다.
* 확정값이 담긴 자리(`tokens.ts` 의 `TYPE.lineHeight`)에는 **왜 재계산하면 안 되는지**를
  주석으로 남긴다. 규칙은 사람이 아니라 코드 옆에 있어야 다음 사람에게 닿는다.
* 이 §8.2 계열 구간은 `scripts/design-token-seal.json` 에 **해시로 봉인된다.** 결정이
  바뀌면 봉인도 함께 갱신해야 하고, 봉인 갱신이 곧 승인 기록이다(솔 검수 `R3 F03`).
* **게이트 PASS는 값이 맞다는 뜻이 아니다.** 게이트는 자신이 가진 계약만 검증한다.
  목적지 표를 새로 넣거나 바꾼 검수는 표의 각 값을 소스 짝과 소유자 확정표에 투영해 읽고,
  그 대응을 뒤집는 음성 시험까지 확인한다. `PRT-220`의 수기 표는 독립 재검산 결과 직접
  fontSize 짝 72/72가 확정표와 일치했지만, 당시 검사기는 틀린 값으로 바꿔도 `mixed`만 같으면
  통과했다. `PRT-221`에서 값이 봉인된 결정적 파생으로 바꿔 그 잠재 경로를 닫았다.
* **검수자의 재검산도 같은 선언을 읽었다는 증거가 먼저다.** 근접 줄 검색으로 앞 요소의
  `fontSize`를 붙이지 않는다. 같은 AST 스타일 객체의 직접 속성인지 확인하고, 직접 짝이 없으면
  `missing`으로 남겨 별도 역할 규칙에서 판단한다. `PRT-221` 재검수의 “15건 불일치”는 앞 2줄
  검색이 다른 `<Text>`를 붙인 결과였고, 동일 AST 객체로 다시 재니 72/72가 일치했다.
* 앞으로 **§8.2 계열 변경과 `scripts/design-token-seal.json` 갱신은 같은 커밋에 넣는다.**
  결정 구간만 먼저 커밋하면 다음 커밋에서 봉인할 예정이어도 중간 SHA는 대비 게이트가 실패해
  `git bisect`의 정상 기준을 깨뜨린다. 기존 `4224f1d`는 검수 과정에서 이미 발행됐으므로 바로
  다음 DS 봉인 커밋으로 닫고, 이후에는 이런 중간 커밋을 만들지 않는다.
* **폐기 커밋 기록** — `1c123f6` 은 폐기된 초안이고 브랜치에서 도달할 수 없다. `S1` 의 `R3`
  검수 대상은 `ee6d8ca` 였다. 페이블이 `1c123f6` 을 보고 낸 "S1 CLOSED" 판정은 검수 대상이
  아닌 SHA 를 본 것이라 무효이고, 페이블 스스로 철회했다. **검수 브랜치에 한 번 보고한
  커밋은 다시 쓰지 않는다** — 폐기할 초안이면 남겨 두고 다음 커밋으로 덮는다.

---

### 8.3 결정 뒤에 새로 열렸고 닫힌 것 (2026-09-05 · `PRT-209`~`PRT-221`)

열 문항을 닫고 규칙 표를 결정문대로 다시 쓰는 과정에서 검수 조건이 다섯 자리를 드러냈다.
①은 대비값 수정으로, ②~⑤는 새 제품 결정을 요구하지 않는 선언별 역할 대응으로 닫았다.

**① ~~`text.link` 와 `action.primary` 는 의무가 다르다 — 그런데 같은 값을 받았다.~~
→ `PRT-210` 에서 `#1465DB` 로 정정해 닫혔다(§8.2a).** 아래는 그 경위다.
결정 2-4 가 둘에 `#1470F5` 를 줬다. `action.primary` 는 **배경**이라 그 위 흰 글자(4.50)와
표면 대비만 지키면 되지만, `text.link` 는 **전경 글자**라 자기가 놓이는 모든 표면에서
4.5:1 이어야 한다. 실측은 흰 표면에서만 4.50 이고 나머지는 미달이다 —
`#F9FAFB` 위 **4.31** · `#F2F4F6` 위 **4.08** · 안내 배너 tint `#EBF3FE` 위 **4.03**.
`D-1′` 이 로고와 버튼의 역할 차이로 닫혔던 것과 **같은 모양의 질문**이 링크와 버튼 사이에 있다.
참고로 승인된 값 중 가장 진한 `#0A68EE` 도 배너 tint 위에서는 4.44 라 4.5 를 넘지 못한다 —
링크 색을 바꾸는 것만으로는 배너가 해결되지 않는다.
지금 코드(`T.blue` `#3182F6` on `#EBF3FE`)는 **3.32** 로 더 낮다.
> 대비 게이트는 이 셋을 `열린` 으로 표시하고 **크기까지 묶어 둔다** — 더 나빠지면 FAIL 이고,
> 목록에 없는 새 미달도 FAIL 이다.

**② ~~`rowMinHeight` 선언별 대응~~ → `PRT-220`에서 닫힘.** 24건 중 실제 ListRow 8건만
60/76의 `targetMap`으로 두고, 나머지 16건은 컴포넌트별 이름을 붙였다. ListRow는 S4에서
mixed 변화를 렌더로 확인한다.

**③ ~~`controlVisualHeight` 선언별 대응~~ → `PRT-220`에서 닫힘.** small 20선언→32,
medium 10선언→38, 헤더 action 24선언(공용 AppHeader의 기존 2 포함)과 그 밖의 고유 상자는
명명된 컴포넌트 소유값으로 분리했다. `minTouchTarget` 44는 계속 별도 터치 계약이다.

**④ ~~결정 5-3의 안내 본문 갈래~~ → `PRT-220`에서 닫힘.** 현행 감사의 4건은 문단 역할이라
`TYPE.body` 16/22로 배정하고 S3b의 폭 증가 검증을 받는다.

**⑤ ~~행간 역할의 선언별 대응~~ → `PRT-221`에서 구조를 재정정해 닫힘.** 재검수문은
`targetMap` 73건 중 15건이 확정표와 다르다고 했지만, AST로 같은 스타일 객체의 직접 짝을
다시 대조하니 **72/72가 일치**했다. `tokens.ts:69`를 display로 본 것은 실제 title 줄을 한 줄
밀어 읽은 것이고, `EmptyState:10`·`MyAccount:59`·`PurchaseOption:152`·`SalesStockCheck:78`은
앞선 별도 `<Text>`의 fontSize를 붙인 것이었다. 값 불일치는 재현되지 않았다.

다만 정본 정의 7건이 실행 우주에 섞여 있던 구조와 수기 표의 미래 변조 경로는 실제 결함이다.
현재는 정의 7건을 별도 보존하고, 사용처 66건 중 직접 fontSize 짝 65건을 사용자 확정 6-2
폐쇄표로 계산한다. 짝 없는 `EmptyState` 안내문 1건은 별도 규칙이다. 검사기가
current→target의 mixed 방향과 확정표 값 자체를 함께 검증한다.

**⑥ 음수 숫자 선언 누락 → `PRT-227`에서 감사 우주를 정정했다.** `PRT-226`까지의 감사기는
양수 `NumericLiteral`만 수집하고 `-0.3`·`-8`처럼 `PrefixUnaryExpression`인 음수 33건을
빠뜨렸다. 그래서 S1의 "실측 있는 자간은 titleTight -0.3뿐"이라는 근거도 불완전했다.
음수 숫자 파서를 독립 시험으로 고정하고, 승인된 -0.3 10건과 아직 결정되지 않은
-0.6/-0.5/-0.4/-0.2 15건을 구분해 W1에 복구했다. 측정기가 읽지 못한 AST 형태를 0건으로
해석하지 않는다.

---

### 8.4 `S2` 색 수렴 실행 기록 (`PRT-228`)

`W1` 종결 SHA `83f7ba438b2bbdf6ceb11014dcd5366ed67b2223`을 기준으로 앱과
프로토타입의 옛 팔레트 직접 참조를 의미 역할로 이관했다. 앱의 `T.ter` 319 · `T.blue` 232 ·
`T.blueTint` 52 · `T.bluePressed` 1, 합계 **604건**은 모두 0이 됐다. 파일별 이전 수보다
늘어나는 경우와 전체 0이 깨지는 경우를 각각 래칫한다. 이 수치는 추정이 아니라 AST 재측정값이다.

상태색은 흰 배경 하나가 아니라 실제 밝은 표면 셋과 자기 tint 중 가장 불리한 조합을 기준으로
정했다. 모두 같은 색상 계열을 유지하면서 AA 4.5:1을 넘는 실행값이다.

| 역할 | 실행값 | 가장 불리한 조합 | 원시 대비 |
|---|---|---|---:|
| `status.positive` | `#0B7F58` | on `#E7F7F0` | 4.52 |
| `status.negative` | `#DA1222` | on `#FEECEC` | 4.50 |
| `status.caution` | `#A16000` | on `#F2F4F6` | 4.55 |

초안의 `#A26100`은 amber tint에서는 4.55였지만 앱 배경 `#F2F4F6`에서 원시값
4.4856으로 미달이었다. `#A16000`은 같은 계열에서 그 배경 4.5473, amber tint 4.6134다.
실제 프로토타입 텍스트 6,974개 재측정 결과 AA 미달은 **0**이다.

PRT-228은 비활성 Primary를 `COLOR.action.primaryDisabled #6A7887`로 보정했으나,
독립 검수에서 Primary만 단색이고 나머지 variant는 opacity인 구조 불일치가 드러났다.
사용자는 2026-09-06 "버튼 역할마다 색이 다르므로 opacity가 맞다"고 결정했다. 따라서 별도
비활성 배경 역할은 철회하고 Primary·tint·gray·ghost·danger가 자기 역할색을 유지한 채
`opacity: 0.4`를 공통 적용한다. WCAG 1.4.3의 비활성 컴포넌트 면제를 명시적으로 사용한다.

`S2` 정적 기하 게이트는 앱 AST와 프로토타입 CSS의 `fontSize`·행간·자간·여백·간격·
폭·높이·반경·방향별 테두리·flex·aspectRatio·transform **5,718개 선언**을 기준 SHA와
대조한다. 색 변경만 통과하고 padding·height·fontSize·borderBottomWidth·flex를 바꾼
음성 시험은 실패한다. FAB는 배경과 그림자를 같은
`COLOR.action.primary`로 옮겼으며 기하 변화는 없다.

`PRT-228` 검수에서 값 수렴은 승인됐지만 옛 `T.blue`가 맡던 의미를 `text.link` 하나로
옮긴 것이 다시 혼용임이 드러났다. `PRT-229`에서 같은 `#1465DB`를
`COLOR.text.accent`(강조 값·양의 방향), `COLOR.state.selectedText`(선택 상태),
`COLOR.text.link`(내비게이션·행동 텍스트)로 분리했다. 값과 픽셀은 같고 이름의 변경 독립성만
회복한다. 링크 역할은 상호작용 JSX 안에서만 허용하는 AST 게이트로 고정한다.

상태색도 문서 역할을 팔레트 `T.*`에 암묵적으로 번역하지 않는다.
`COLOR.status.{positive,caution,negative,positiveTint,cautionTint,negativeTint}`가 앱 정본이고
`STATUS`·앱 대비 게이트·프로토타입 계약 투영이 같은 이름을 읽는다. `T.green`·`T.red`·
`T.amberText` 계열의 기존 사용처는 증가 금지 래칫으로 두고 `S3a`에서 0으로 만든다.
초록 `#15B374→#0B7F58` 등 상태색 값 정정은 팔레트 단순화가 아니라 **자기 tint 위 AA 최소
통과색**을 택한 결과다(positive 4.52 · negative 4.50 · caution 4.55).

### 8.5 `S3a` 폭 비증가 치환 실행 기록 (`PRT-230`)

S2 종결 SHA `f5cacba848e59f1e9b7e671d0c43d83f0efaa762`의 결속된 앱 선언 3,646건을
기준으로, 폭이 늘지 않는 규칙만 실행했다. 76파일의 1,042개 리터럴을 `TYPE`·`space`·
`radius`·`controlVisualHeight` 참조로 바꿨다. 선언별 현재값·목적지·축·표현식은
`scripts/design-token-s3a-known.json`에 전부 보존하고 전용 게이트가 양방향으로 대조한다.
여기서 `docs/token-adoption-audit.json`은 **S3a 이전 `f5cacba` 기준선 스냅샷**이다. S3a 후
사용처 선언 재측정은 S4 완료 SHA에서 W1을 최종 종결할 때 수행한다. 3,666→3,646의 20건 감소는
S2에서 MY 허브 타일 색 리터럴을 역할 토큰으로 바꾼 대차다(`MyCategoryHubScreen` −6 ·
`MyHomeScreen` −14, 전부 color 46→26; 타이포·간격·크기·반경 수는 동일).

치환 계획 1,046건으로 시작했으나 터치 감사를 함께 돌리자 34→32 action 9곳 중 직접 이웃한
4곳에서 44px 유효 폭을 만들 hitSlop이 형제 영역과 겹치는 계약 충돌이 드러났다. 그 4곳은
`R-SZ-CONTROL-SM-ADJACENT`로 갈라 S4로 미뤘다. `MaterialManage`의 행 gap 10→8도 같은
이유로 `COMPONENT.materialManage.rowActionGap` 10을 유지한다. 이는 예외 은폐가 아니라
접근성 계약이 숫자 수렴보다 우선한다는 기계 판독 배정이다.

나머지 고립 action 5곳은 32px로 줄이되 hitSlop을 함께 보정해 유효 44×44를 유지했다.
최종 터치 래칫은 기존과 같은 미달 2 · 형제중첩 1 · 판정불가 163이다. S3a 게이트는
1,042/1,042 치환, hitSlop 5/5 보정, 승인되지 않은 기하 변경 0, 가로 증가 0,
프로토타입 CSS 변경 0을 요구한다. 기준선 감사 수치는 실행 뒤 결과로 덮어쓰지 않는다.

### 8.6 `S4` 컴포넌트·레이아웃 계약 후보 (`PRT-232` · 검수 정정 `PRT-233`~`PRT-234`)

S3a 종결 `deeb767`에서 S4를 시작했다. 결정 3-1·4-1·7-2와 D-11을 다음처럼 실행했다.

- 하단 탭: custom 2줄 라벨, 실제 한 줄 초과 높이만 기본 60에 더하고 Safe Area는 한 번만 포함.
  fontScale이 바뀌면 높이를 초기화해 다시 잰다. 비활성 탭도 행동 가능하므로 tertiary 색을 쓴다.
- 스크롤: `scrollStart=2`, 일반-flow `scrollEnd=24`, FAB 화면은 `24+48+24=96`.
  별도 Modal 좌표계의 Sheet는 `scrollEnd + insets.bottom`으로 safe-area를 한 번 포함한다.
- 행: ListRow 8자리만 `rowMinHeight.oneLine/twoLine` 60/76으로 이관.
- 매출 메뉴 요약: 줄 수 제한을 없애고 2줄 행이 높이를 받아 준다.
- 터치: 최초 후보는 Button 시각 높이를 유지하고 variant별 **세로** hitSlop
  `sm 7 / md 1 / lg 0`으로 정적 하한 44를 만들었다. 이후 Android 부모 frame 실측에서 `sm`이
  반복해서 잘려 `minHeight 44 / hitSlop 0`으로 정정했다. 가로 hitSlop은 0으로 유지해 `OrdersHome`의
  간격 8인 취소·완료 행동처럼 인접한 버튼끼리 터치 영역이 겹치지 않게 한다. 인접 32px 행동은
  gap 12·hitSlop 6이다.
  카테고리 정렬은 단일 44×44 진입점과 웹에서도 두 방향이 동작하는 공용 Sheet로 재구성했다.

정적 결과는 미달 0·같은 부모 형제중첩 0·형제판정불가 12·일반 판정불가 163이다. 감사기는
공용 `Button`의 variant 계약을 소비처에 투영하며, 계약표에 없는 공용 조작 컴포넌트 형제는 통과시키지
않고 `형제판정불가` 목록에 양방향 래칫한다. S4 게이트는 S3a 기준선 대비 기하·hitSlop·줄 수의
허용 변경 108건을 `파일·속성·등장 순서·변경 전→후`로 직접 보존·대조한다. 허용 파일 목록과 양쪽
해시는 범위 결속을 위한 보조 증거이며 선언별 허용 목록을 대신하지 않는다.
다만 §7.2a 조건 6의 Android·iOS 실제 frame 측정은 `adb`·`xcrun`이 없는 현재 Windows 환경에서
수행할 수 없어 S4a와 S4를 최종 완료로 닫지 않는다. `S3b` 착수 전 독립 검수와 이 한계의 처리 기록을 남긴다.

### 8.7 `S3b` 폭 증가 치환 후보 (`PRT-235`)

PRT-234의 페이블 PASS 후 S2 기준선에서 분류한 S3b 180건을 현재 S4 나무에 투영했다.
S4가 `MyVendorsScreen` 행 gap `11→COMPONENT.adjacentActions.gap(12)` 1건을 이미 적용했으므로,
이 회차의 신규 치환은 56개 파일 179건이다. 승계 1건과 신규 179건을 합치면 원래
S3b 계약 180건과 일치한다.

`scripts/design-token-s3b-known.json`은 신규 179건의 기준선 위치·현재 위치·속성·값·
토큰 목적지를 전수 보존한다. `scripts/design-token-s3b-diff.mjs`는 `e3834bd` 기준선 대비
이 목록만 바뀌었는지, 전부 가로 또는 양축 증가인지, 목적지 토큰이 확정 숫자값을
유지하는지를 대조한다. S4 게이트는 S3b의 기하 변경 131건을 합친 239건을 잠그지만,
단계 소유권은 별도 S3b 계약이 유지한다.

S4a의 네이티브 frame 조건은 이 단계로 닫히지 않는다. Android 실측에서 정적 합산과 다른
부모 클리핑이 이미 관측됐으므로, S3b 독립 검수 후 별도 네이티브 증거 PRT로 정정한다.

### 8.8 `S4a`·`S4b` 네이티브 터치 Android 증거 (`PRT-236`~`PRT-237`)

S3b 종결 `33bf34e` 뒤 Android 15 에뮬레이터에서 Fabric의 `measure`·`measureInWindow`를 읽어
가장 가까운 native 부모 frame으로 `hitSlop`을 자른 실제 유효 영역을 측정했다. 계약은 9개
시나리오·17개 target ID·원래 소스 계보 18개를 보존하고, 화면에 함께 나타난 계약 밖 pressable과
같은 부모 형제 중첩도 별도 래칫한다. density는 실행기에서 읽고, 44dp 판정 오차는 물리 0.5px와
`1e-3px`만 허용한다.

진단 측정에서 드러난 부모 clipping을 따라 `Button sm`, `Chip`, `SortChip`, `InfoBtn`, 목록·판매
행동을 보정했다. 제품·감사기를 `f3279cf51db3eda719473f05fc59b82be2268b00`으로 먼저 고정한 뒤,
추적 파일이 깨끗한 같은 SHA에서 1×와 2×를 다시 측정했다. 두 exact 산출물 모두 계약 target
미달 0·계약 밖 관측 미달 0·물질적 형제 중첩 0·실패 0이다.

- `docs/prototypes/native-touch-android-1x.json` — SHA-256 `42F1C872…821ED849`
- `docs/prototypes/native-touch-android-2x.json` — SHA-256 `B159776E…1F11338`

누적 AST 계약은 `S4 107 / S3b 131 / S4a 16 / S4b 41`로 단계 소유권을 분리한다. Android
종결은 두 산출물을 커밋하고 `f3279cf` 제품판과 함께 독립 검수를 통과한 뒤 확정한다.

iOS는 Windows에 `xcrun`을 설치해 대체할 수 없다. exact Android 증거까지는 이 작업에서 닫되,
iOS는 macOS/Xcode 또는 실제 iPhone 실행기에서 같은 계약을 재현하기 전까지 열어 둔다. 따라서
그 전에는 `S4a`·`S4`·`W1` 전체를 최종 완료로 쓰지 않는다.

### 8.9 소유자 결정 12-4와 양 플랫폼 재측정 (`PRT-238`~)

2026-09-06 소유자는 시안집 별책 12의 **12-4**를 선택했다. 기존 Android 후보 중
`Chip`·`SortChip`을 시각 44로 만든 변경은 역할을 잘못 묶은 것이므로 철회하고, 기존 형상과
44px 이상인 소비 줄을 복원한다. Android 실측에서 사방 `hitSlop:6`은 높이 38.3px에 그치면서
이웃과 4px 겹쳤으므로 세로 9·가로 0으로 고쳐 실제 높이 44 이상과 중첩 0을 함께 만든다. `Button sm` 10개
소비처는 36→44, `ConditionRow` 3개 소비처는 38→44, `AppHeader` right slot 12개 소비처와
시트 닫기 action은 투명 44×44 상자를 쓴다. `FilterButton` 7개 소비처는 시각 32와 세로
`hitSlop:6`을 유지한다. 프로토타입 공용 CSS는 `.order-button 36→44`와 `.condition 38→44`만
같은 값으로 동기화한다.

실제 iPhone의 App Store Expo Go는 SDK 57이고 이 프로젝트는 고정 계약상 SDK 54라 직접 열 수
없었다. SDK를 올려 검증 대상을 바꾸지 않고, Expo 계정에 연결한 SDK 54
`expo-dev-client ~6.0.21` EAS development build를 실제 iPhone에 설치해 같은 측정기를 실행한다.
Android·iOS 모두 raw `measure`·`measureInWindow`에서 모든 host ancestor clipping과 형제 중첩을
검사기가 다시 계산하며, 저장된 파생값은 근거로 신뢰하지 않는다. 두 플랫폼 증거와 소유자 승인
외 화면 diff 0이 독립 검수를 통과하기 전에는 `S4a`·`S4`·`W1`을 닫지 않는다.

### 8.10 Android exact 증거와 W1 최종 배정 후보 (`PRT-240`~`PRT-243`)

부분 노출 판정은 모든 host ancestor를 동일하게 clipping 경계로 보지 않는다. 직접 부모와
`overflow:hidden` 등 실제로 자식을 자르는 조상만 유효 터치 영역을 제한한다. 일반 `View`의
`overflow:visible`은 경계가 아니며, `RNSScreen*`·`RCTModalHostView` 같은 플랫폼 wrapper는
`measureInWindow` 좌표계가 자식과 달라 raw frame만으로 제품 clipping 경계라고 단정하지 않는다.
scroll/root 경계 밖의 부분 노출 행은 측정 대상에서 제외하되, 비-scroll 조상에 잘린 행은 계속
판정한다. 물리 0.5px 허용오차와 이 분기들은 음성 시험으로 고정한다.

제품·감사기 SHA `d4fba6b1d0bbdc5e734a004b6e813e06273ef6e5`에서 Android 15/API 35,
density 2.625의 1×/2× exact 증거를 다시 만들었다. 두 산출물 모두 계약 target 19개·소스 계보
20개, 관측 판정불가 0·유효 터치 미달 0·물질적 형제 중첩 0이다. iOS 산출물 둘은 Apple
Developer Program 팀 활성화 전이라 `MISSING`이며, 검사기의 `--require=all`은 이를 명시적으로
실패시킨다.

같은 제품판에서 앱 선언을 다시 측정한 W1 최종 배정 후보는 정의 21건을 별도 집계하고 실행 우주
2,345건을 `primitive 2,064 / componentOwned 249 / defect 0 / pendingApproval 32 /
exception 0`으로 나눈다. 미분류 0, 다중 일치 774, 고유 충돌 체인 6이며 이전 배정과의 통 이동
대차를 기계 산출물에 보존한다. 다만 iOS exact 증거가 없으므로 이 수치는 최종 종결 후보일 뿐,
`S4a`·`S4`·`W1` 상태는 계속 `REOPEN`이다.

`PRT-248`은 제품 파일을 바꾸지 않고 이 후보의 열린 분류 15건을 닫았다. 다섯 `marginTop:2`는
행 내부 아이콘 간격이 아니라 앞 콘텐츠와 다음 블록 사이의 세로 관계라 `space.xs=4` defect로,
빈 상태·세션 중앙 정렬의 큰 여백 10건은 범용 space 확장 없이 역할별 컴포넌트 형상으로 배정했다.
따라서 Android 네이티브 증거의 제품 tree는 바뀌지 않는다.

페이블의 `PRT-240`~`PRT-244` 독립 재검수는 Android 부분 종결을 승인했다. 2× `sales-home`의
`sales-actions` phase에서 `판매 수량 수정`과 `판매 입력`이 각각 4건씩 판정돼 S4b 판매 보정이
1×에만 머물지 않았음을 확인했다. 작은 영수증은 `--verify-receipt`로 원시 1×/2× 파일과 검사기·
감사기·계약·known 해시를 매번 다시 계산해 exact 대조하며, 기본 verify에도 이 검사를 포함한다.
영수증의 `requirePlatforms`는 계약의 `closedPlatforms`와 같아야 한다. iOS 1×/2× exact 증거가
확보되면 `closedPlatforms`를 반드시 `[android, ios]`로 승격하고 `--require=all` PASS를 받아야
`S4a`를 최종 종결할 수 있다. 계산된 clipping 모델은 Android 실제 `adb tap` 3점으로도 실증했다.
유효 사각형 안은 `onPress 1`, 직접 부모 밖의 slop 가장자리는 `0`, `overflow:visible` 조부모
밖이지만 직접 부모 안인 점은 `1`이었다. 이 증거도 제품 commit·tree와 측정 스크립트·계약 해시에
결속하며 기본 verify가 재검산한다. iOS에서도 같은 세 점을 반복한다.

---

## 9. 알려진 측정기 한계

1. ~~대비(contrast)를 재지 않는다.~~ **해소됐다** — `PRT-198` 에서 WCAG 2.x 대비를 재고
   실패 쌍마다 최소 통과 색을 낸다(§4.1a). `PRT-209` 에서 **결정된 색의 조합표를 절대 기준
   (4.5:1 / 3:1)으로 매 커밋 재검산하는 게이트**를 붙였다 — 표면 색을 한 톤만 바꿔도
   그 커밋에서 바로 FAIL 한다. baseline 비교가 아니다(바뀐 값이 새 baseline 이 되면
   조용히 통과한다). 4.50~4.55 는 통과시키되 `boundary` 로 표시한다.
   남은 한계는 **AAA(7:1)를 목표로 삼을지**와, 아직 계약되지 않은 비텍스트 쌍
   (구분선 `#E5E8EB` · 비활성 Primary 배경 `#D1D6DB`)이 조합표에 없다는 것이다 —
   `S2` 에서 정해지면 들어온다. 계약하지 않은 것을 검사하는 척하지 않는다.
2. **번역 확대는 실제 번역이 만드는 줄바꿈 기회를 재현하지 않는다.** 자간만 늘리므로
   같은 폭에서 끊을 자리가 실제보다 적다. 이 방향의 오차는 넘침을 **과대** 보고하는 쪽이라
   보수적이다.
3. **확대 정확도는 `w130` 기준 97.8%, `w150` 기준 94.0%다.** ±1% 안에 든 노드는 분포로
   요약하고, **1% 를 넘어 어긋난 노드는 전부**(w130 156행 · w150 422행) target · selector ·
   원래 폭 · 목표 폭 · 실제 폭 · 부호 있는 오차와 함께 보존한다. `atRisk` 행도 전부 남긴다
   (w130 106 · w150 302). 표본만 남기면 "노드마다 보존" 이 아니다. 나머지는 `overflow:hidden`
   상자 안에서 Range 가 잘린 부분을 못 세거나, 자간이 커지며 줄바꿈이 바뀌어 폭이 단조롭지
   않은 자리다. **덜 늘어난 것의 "잘림 0" 은 낙관적**이므로 `atRisk` 로 따로 세고,
   `atRisk` 가 0 이 아닌 패스의 결론은 "관측되지 않음" 으로만 쓴다(§1.2).
4. **`i18n-stress` 의 글자 200% 는 아이콘을 키우지 않는다.** `render-audit` 의
   `mobile320t2` 는 키운다. 두 패스는 다른 계약이며 서로 대체하지 않는다.
   `KD-001` 은 `render-audit` 쪽에서만 잡힌다.
5. ~~앱 감사는 property/value/location 을 내지 않는다.~~ **해소됐다** — 선언 3,677건을
   `{group, prop, value, file, line, column, layer}` 로 전수 보존한다.
   드러난 것: **앱의 간격 고유 선언이 164종**으로 프로토타입(25종)보다 훨씬 흩어져 있고,
   팔레트에 없는 색이 섞여 있다(`#7C5CE0` 보라 · `#F0EDFB` · `#179E6B`).
6. **모션 · z-index 를 수집하지 않는다**(§7 `P1c`).
6b. **같은 값이라도 축과 관계가 다르면 다른 통이다.** 이 실수를 **세 번** 했다 —
   `PRT-196`(가로 2px 을 세로로 오해) · `PRT-204`(`marginTop:2` 를 값만 보고 광학 보정으로
   분류할 뻔) · `PRT-205`(`#000` 3건을 scrim 으로 읽었는데 전부 `shadowColor` 였다).
   세 번 다 **값을 보고 역할을 짐작**했고, 세 번 다 소스를 열어서야 관계가 드러났다.
   같은 회차가 "값이 아니라 관계로" 를 원칙으로 세워 놓고 그 원칙을 어겼다.
   `PRT-196` 에서
   `.edit-form-label{margin:0 2px 7px}` 의 2px 을 세로로 오해했고, `PRT-204` 에서
   앱의 `marginTop:2` 를 값만 보고 광학 보정으로 분류할 뻔했다. 소스를 열어 관계를 보니
   68건이 주값↔보조 줄이었고, UI 가이드가 그 관계를 이미 `space.xs` 로 잠가 놓았다.
   **값은 분류의 입력이지 분류가 아니다.**
6c. **판정을 코드로 옮기면 그 코드의 한계가 새 오류다.** `PRT-207` 이 "축은 값이 아니라
   속성으로" 를 세우고 판정기를 썼는데, 그 판정기가 두 경우를 못 읽어 **3건이 여전히 틀린
   방향**에 있었다(`PRT-209` 에서 발견). (가) `flexDirection` 이 문자열 리터럴이 아니라
   삼항식일 때 — `Button.tsx:56` 의 `iconRight ? 'row-reverse' : 'row'`. 리터럴만 읽는
   정규식이 이를 "선언 없음" 으로 접어 RN 기본값 `column` 으로 보냈다. **앱의 모든 버튼
   아이콘↔라벨 간격**이 세로(S3a)에 있었다. (나) 스타일 객체에 `flexDirection` 이 없어도
   **원소가 축을 정할 때** — `<ScrollView horizontal>` 의 `contentContainerStyle`.
   원칙을 세우는 것과 그 원칙을 정확히 재는 것은 다른 일이다. 그래서 판정기를
   **보존 스크립트**(`-axis-measure.mjs` · `-role-measure.mjs`)로 만들어 규칙 파일의
   `at` 목록이 그 산출물이 되게 했다 — 손으로 옮긴 목록은 다시 틀릴 수 있다.
6d. **확인하지 않은 문장은 근거가 아니다.** `PRT-208` 까지 `R-RD-SHAPE` 18건을 모두
   "짧은 변의 절반" 이라 적고 있었다. 페이블이 자리마다 실측을 요구해서 재 보니 **두 자리가
   아니었다** — 9×9 반경 3(원이려면 4.5) · 10×10 반경 3(원이려면 5). 둘을 `radius.full` 로
   보내면 RN 이 반경을 잘라 **둥근 사각형이 원으로 바뀐다**. 값 수렴이 아니라 모양 변경이다.
6a. **시뮬레이션은 원래 동작을 그대로 옮겨야지 그럴듯하게 옮기면 안 된다.**
   `PRT-202` 의 키 증명 첫 판이 관측 쪽을 접지 않아 옛 스킴을 실제보다 **유능하게** 그렸다
   (옛 판정이 FAIL 51건으로 나왔다). 옛 게이트는 관측도 같은 키로 접었다.
   같은 실수는 반대 방향으로도 가능하다 — 옛 스킴을 실제보다 **무능하게** 그려 새 스킴의
   공을 부풀리는 것. 양쪽을 같은 키로 접는 것이 그 방어다.
   과거 동작을 재현하는 코드는 과거 코드의 **덮어쓰기·순서·기본값까지** 옮겨야 한다.
7. **측정기 결함이 지금까지 다섯 번 나왔다**(`PRT-188` · `189` · `191` · `193` · `194`).
   `PRT-193` 은 이 문서의 핵심 결론 하나를 통째로 뒤집었고, `PRT-194` 는 두 독립 검수가
   같은 곳을 찍어 재는 단위 자체를 바꿨다. **도구 출력에 의존하는 구조 자체는 안전하다 —
   대안이 없기도 하다. 안전하지 않은 것은 (가) 어느 결정이 어느 측정 행에 의존하는지
   표시가 없어서 측정기가 고쳐질 때 무엇을 다시 봐야 하는지 알 수 없다는 점, (나) 손으로
   한 자리 검산하라는 권고가 권고에 머물러 있다는 점이다.** 그래서 이 2판은 결정마다
   의존 행을 적었고, **탭 라벨 5개의 320px 수동 측정을 `P1a` 완료 조건에 넣는다.**
